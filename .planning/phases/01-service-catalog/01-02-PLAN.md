---
phase: 01-service-catalog
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - API/ZachHairStudio.Api.Tests/CustomWebApplicationFactory.cs
  - API/ZachHairStudio.Api.Tests/Features/Services/ServicesServiceTests.cs
  - API/ZachHairStudio.Api.Tests/Features/Services/ServicesControllerTests.cs
  - API/ZachHairStudio.Shared/Features/Services/ServicesService.cs
  - API/ZachHairStudio.Api/Controllers/ServicesController.cs
  - API/ZachHairStudio.Api/Program.cs
  - API/ZachHairStudio.Shared/Db/BookingDbContext.cs
  - API/ZachHairStudio.Shared/Migrations
autonomous: true
requirements: [PLAT-01, PLAT-02, CAT-03]
must_haves:
  truths:
    - "ServicesController's constructor takes ServicesService and does NOT take BookingDbContext (PLAT-01, Success Criterion 4)"
    - "GET /api/services returns only IsActive services ordered by DisplayOrder (D-09, D-10)"
    - "GET /api/services/{slug} returns 200 with the matching active service, and 404 for an unknown or inactive slug (CAT-02, D-09)"
    - "POST /api/services with invalid data (empty name / negative price) returns 400 with an ASP.NET ProblemDetails/ModelState body (PLAT-02, D-17)"
    - "POST /api/services with valid data returns 201 and persists the row; ServicesService.CreateAsync returns Result<T>.Success (D-17)"
    - "After the AddServices migration is applied, GET /api/services returns the seeded catalog rows (CAT-01 data, D-12, D-13)"
    - "The Slug column has a unique index at the database level (D-02, Pitfall 3)"
  prohibitions:
    - statement: "ServicesController does not inject or reference BookingDbContext"
      status: resolved
      verification: "PLAT-01 reflection test asserts no BookingDbContext parameter on ServicesController constructors; ServicesController.cs shows no BookingDbContext usage"
    - statement: "Seeding does NOT use UseSeeding/UseAsyncSeeding (incompatible with Migrate())"
      status: resolved
      verification: "BookingDbContext.cs and Program.cs contain no UseSeeding/UseAsyncSeeding; seed rows are declared via HasData in OnModelCreating"
  artifacts:
    - API/ZachHairStudio.Shared/Features/Services/ServicesService.cs
    - API/ZachHairStudio.Api/Controllers/ServicesController.cs
    - API/ZachHairStudio.Api.Tests/CustomWebApplicationFactory.cs
    - API/ZachHairStudio.Shared/Migrations (AddServices migration)
  key_links:
    - "Program.cs must register AddValidatorsFromAssemblyContaining<ServiceCreateDtoValidator> and AddScoped<ServicesService> or DI resolution fails at request time"
    - "The seeded rows (slug/name/short/long) feed the /services and /services/[slug] pages in Plan 03 and the homepage subset in Plan 04"
    - "Program.cs gains a partial Program class marker so WebApplicationFactory can host it in tests"
---

## Phase Goal

**As a** salon client, **I want to** browse the studio's services with their descriptions, durations, and prices, **so that** I can decide what to book.

> Backend vertical, part 2 of 2. Builds on Plan 01's validated domain contract to deliver the working, seeded, queryable API: the `ServicesService` layer (PLAT-01), the thin translating `ServicesController`, DI wiring, and the `HasData` seed migration. After this plan, `GET /api/services` returns the real catalog — the backend of the walking-skeleton slice is complete.

<objective>
Implement the per-feature service layer and REST endpoints that PLAT-01 requires, wire validation and DI, and seed the catalog through an EF Core migration. Write the service/controller/architecture tests first (RED), implement to green (GREEN), then add the DbSet + config + HasData seed and apply the migration (a BLOCKING schema push before verification).

Purpose: Activate the Result<T> + service-layer template every later phase copies, and make the catalog data real.
Output: `ServicesService`, `ServicesController`, DI registrations, `DbSet<Service>` + unique slug index + seed, and the `AddServices` migration applied to the dev database.
</objective>

<execution_context>
@D:/repos/vct/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@D:/repos/vct/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-service-catalog/01-CONTEXT.md
@.planning/phases/01-service-catalog/01-RESEARCH.md
@.planning/phases/01-service-catalog/01-PATTERNS.md
@.planning/phases/01-service-catalog/SKELETON.md
@.planning/phases/01-service-catalog/01-01-SUMMARY.md

# Analog + integration files:
@API/ZachHairStudio.Api/Controllers/BookingsController.cs
@API/ZachHairStudio.Shared/Result.cs
@API/ZachHairStudio.Shared/Db/BookingDbContext.cs
@API/ZachHairStudio.Api/Program.cs
@landing-page/lib/data.ts
@landing-page/lib/api.ts
</context>

## Artifacts this phase produces (Plan 02)

New symbols introduced here (exclude from source-drift acknowledgement):
- Class: `ServicesService` with `GetActiveServicesAsync()`, `GetBySlugAsync(string)`, `CreateAsync(ServiceCreateDto)`, `UpdateAsync(int, ServiceUpdateDto)`
- Class: `ServicesController` (routes `GET /api/services`, `GET /api/services/{slug}`, `POST /api/services`, `PUT /api/services/{id}`)
- DbSet: `BookingDbContext.Services`
- Migration: `AddServices` (schema + unique slug index + HasData seed)
- Test infra: `CustomWebApplicationFactory`, `ServicesServiceTests`, `ServicesControllerTests`
- `Program.cs` DI registrations + partial `Program` class marker

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing service + controller + PLAT-01 architecture tests (RED)</name>
  <files>API/ZachHairStudio.Api/Program.cs, API/ZachHairStudio.Api.Tests/CustomWebApplicationFactory.cs, API/ZachHairStudio.Api.Tests/Features/Services/ServicesServiceTests.cs, API/ZachHairStudio.Api.Tests/Features/Services/ServicesControllerTests.cs</files>
  <read_first>
    - API/ZachHairStudio.Api/Controllers/BookingsController.cs (controller test-surface analog)
    - API/ZachHairStudio.Shared/Result.cs (Result<T> factory + predicate methods: IsSuccess, IsNotFound(), IsValidationError())
    - API/ZachHairStudio.Shared/Features/Services/ServiceCreateDto.cs (from Plan 01)
    - API/ZachHairStudio.Api/Program.cs (top-level statements; needs a partial Program marker for WebApplicationFactory)
    - .planning/phases/01-service-catalog/01-RESEARCH.md (Validation Architecture Test Map; Code Examples reflection test; Patterns 1 and 2)
  </read_first>
  <action>
    Add a minimal partial `Program` class marker (public partial class Program with an empty body) to the bottom of `API/ZachHairStudio.Api/Program.cs` so `WebApplicationFactory<Program>` can host the app in integration tests. No behavior change. Do NOT add the DI registrations yet — that is Task 2.

    Create `API/ZachHairStudio.Api.Tests/CustomWebApplicationFactory.cs`: a `WebApplicationFactory<Program>` subclass that, in `ConfigureWebHost`, removes the registered `DbContextOptions<BookingDbContext>` descriptor and re-registers `BookingDbContext` using `UseInMemoryDatabase` with a unique database name per factory instance, so integration tests never touch SQL Server. This is the repo's shared integration fixture (the conftest-equivalent from RESEARCH Wave 0).

    Write failing tests:
    - `ServicesServiceTests.cs` (unit; construct `ServicesService` with an InMemory `BookingDbContext` via `DbContextOptionsBuilder<BookingDbContext>().UseInMemoryDatabase(...)` plus real `ServiceCreateDtoValidator`/`ServiceUpdateDtoValidator`): `GetActiveServicesAsync` returns only `IsActive` rows ordered by `DisplayOrder`; `GetBySlugAsync` returns not-found for an unknown slug and success for a seeded active slug; `GetBySlugAsync` returns not-found for an inactive slug; `CreateAsync` with an invalid DTO returns a `Result` where `IsValidationError()` is true and no row is written; `CreateAsync` with a valid DTO returns success and persists.
    - `ServicesControllerTests.cs` (integration via `CustomWebApplicationFactory`): `GET /api/services` returns 200 and a JSON array; `GET /api/services/{unknown-slug}` returns 404; `POST /api/services` with an empty name returns 400 and a body containing an `errors` object (ProblemDetails/ModelState shape); PLUS the PLAT-01 reflection test named `ServicesController_DoesNotDependOnBookingDbContext` from RESEARCH Code Examples asserting no constructor parameter of `ServicesController` is of type `BookingDbContext`.

    These reference `ServicesService`/`ServicesController` which do not exist yet — expected RED. Do NOT implement them in this task.
  </action>
  <verify>
    <automated>test -f API/ZachHairStudio.Api.Tests/CustomWebApplicationFactory.cs && grep -q "UseInMemoryDatabase" API/ZachHairStudio.Api.Tests/CustomWebApplicationFactory.cs && grep -q "DoesNotDependOnBookingDbContext" API/ZachHairStudio.Api.Tests/Features/Services/ServicesControllerTests.cs && echo RED-READY</automated>
  </verify>
  <acceptance_criteria>
    - `Program.cs` ends with a public partial `Program` class marker
    - `CustomWebApplicationFactory` swaps `BookingDbContext` to `UseInMemoryDatabase` with a per-instance name
    - `ServicesServiceTests` asserts active-only + DisplayOrder ordering, not-found on unknown slug, not-found on inactive slug, validation-error on invalid create
    - `ServicesControllerTests` asserts GET list 200, unknown slug 404, POST invalid 400-with-errors-body, and the PLAT-01 reflection test
    - Tests reference not-yet-created `ServicesService`/`ServicesController` (RED)
  </acceptance_criteria>
  <behavior>
    - ServicesService.GetActiveServicesAsync: given active+inactive rows, returns only active, ordered by DisplayOrder ascending
    - ServicesService.GetBySlugAsync("nope"): Result.IsNotFound() == true
    - ServicesService.GetBySlugAsync(inactive slug): Result.IsNotFound() == true
    - ServicesService.CreateAsync(invalid): Result.IsValidationError() == true, row count unchanged
    - GET /api/services/{unknown}: HTTP 404
    - POST /api/services {empty name}: HTTP 400, body has errors
  </behavior>
  <done>Integration fixture + failing service/controller/architecture tests exist referencing not-yet-created types (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Implement ServicesService, ServicesController, and DI wiring (GREEN)</name>
  <files>API/ZachHairStudio.Shared/Features/Services/ServicesService.cs, API/ZachHairStudio.Api/Controllers/ServicesController.cs, API/ZachHairStudio.Api/Program.cs</files>
  <read_first>
    - API/ZachHairStudio.Shared/Result.cs (which factories/predicates to use)
    - API/ZachHairStudio.Api/Controllers/BookingsController.cs (controller shape to copy; do NOT copy the DbContext dependency)
    - API/ZachHairStudio.Api/Program.cs (DI block to extend)
    - API/ZachHairStudio.Shared/Features/Services/ServiceExtensions.cs (ToDto/ToEntity/ApplyTo from Plan 01)
    - API/ZachHairStudio.Shared/Features/Services/ServiceCreateDtoValidator.cs (injected validator)
    - .planning/phases/01-service-catalog/01-RESEARCH.md (Pattern 1 ServicesService; Pattern 2 controller Result<T> translation; Open Question 1 canonical flow)
    - .planning/phases/01-service-catalog/01-PATTERNS.md (ServicesService + ServicesController pattern assignments)
  </read_first>
  <action>
    Create `API/ZachHairStudio.Shared/Features/Services/ServicesService.cs` per RESEARCH Pattern 1: a plain class constructor-injected with `BookingDbContext`, `IValidator<ServiceCreateDto>`, and `IValidator<ServiceUpdateDto>`. Methods: `GetActiveServicesAsync()` returns `IEnumerable<ServiceResponseDto>` filtered `IsActive == true`, ordered by `DisplayOrder`, projected via `ToDto()` (D-09, D-10); `GetBySlugAsync(string slug)` returns `Result<ServiceResponseDto>` — `NotFoundError` when no active row matches, else `Success` (filters `IsActive == true`, so inactive slugs return 404 per D-09 and the slug-enumeration mitigation); `CreateAsync(ServiceCreateDto)` calls `ValidateAsync` defensively and returns `Result.ValidationError` when invalid, else adds via `ToEntity()`, saves, returns `Success`; `UpdateAsync(int id, ServiceUpdateDto)` loads the entity (return `NotFoundError` if missing), validates, applies via `ApplyTo`, saves, returns `Success`. This class owns ALL `BookingDbContext` access for the feature (PLAT-01).

    Create `API/ZachHairStudio.Api/Controllers/ServicesController.cs` per RESEARCH Pattern 2 — copy the `[ApiController]` + `[Route("api/[controller]")]` shape from `BookingsController` but inject `ServicesService` (plus `IValidator<ServiceCreateDto>`/`IValidator<ServiceUpdateDto>` for the ModelState path); NEVER inject `BookingDbContext` (PLAT-01, D-19). Actions: `GET` list returns `Ok(await service.GetActiveServicesAsync())`; `GET {slug}` (name the action `GetService`) translates `Result<T>` — `IsSuccess` returns `Ok(result.Data)`, else `NotFound()`; `POST` (D-16) uses the canonical validation flow — call `ValidateAsync`, on failure `validation.AddToModelState(ModelState)` then `return ValidationProblem(ModelState)` (rich ModelState body that `lib/api.ts` extractErrorMessage already parses), on success delegate to `service.CreateAsync` and translate `Result<T>` with `CreatedAtAction(nameof(GetService), new { slug = result.Data.Slug }, result.Data)`; `PUT {id}` (D-16) uses the same validation flow, delegates to `UpdateAsync`, and translates `Result<T>` (`NoContent()` on success, `NotFound()` when `IsNotFound()`). This dual-validation split — controller for rich HTTP errors (D-17), service for non-HTTP callers — is the canonical template; apply it consistently in every later phase.

    In `API/ZachHairStudio.Api/Program.cs`, add to the DI block (before `builder.Build()`): `builder.Services.AddValidatorsFromAssemblyContaining<ServiceCreateDtoValidator>();` and `builder.Services.AddScoped<ServicesService>();`. Do not change the existing `db.Database.Migrate()` call, CORS, or OpenAPI setup.

    After this task the service unit tests, the POST-invalid controller test, and the PLAT-01 reflection test pass. Integration read tests that expect seeded data may still return empty until Task 3.
  </action>
  <verify>
    <automated>cd API && dotnet test ZachHairStudio.Api.Tests/ZachHairStudio.Api.Tests.csproj --filter "FullyQualifiedName~ServicesServiceTests|FullyQualifiedName~DoesNotDependOnBookingDbContext" --nologo 2>&1 | grep -E "Passed!|Failed!|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `ServicesController` constructor parameters do NOT include `BookingDbContext` (PLAT-01 reflection test passes)
    - `ServicesService` exposes `GetActiveServicesAsync`, `GetBySlugAsync`, `CreateAsync`, `UpdateAsync`, returning projected DTOs or `Result<T>`
    - `Program.cs` calls `AddValidatorsFromAssemblyContaining<ServiceCreateDtoValidator>()` and `AddScoped<ServicesService>()`
    - POST invalid returns `ValidationProblem(ModelState)` (400 with `errors`)
    - `dotnet test` for ServicesServiceTests + the PLAT-01 reflection test passes green
  </acceptance_criteria>
  <done>Service layer + controller + DI implemented; PLAT-01 reflection test and service unit tests green; controllers never touch DbContext.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Add DbSet + unique slug index + HasData seed, generate and apply migration</name>
  <files>API/ZachHairStudio.Shared/Db/BookingDbContext.cs, API/ZachHairStudio.Shared/Migrations/</files>
  <read_first>
    - API/ZachHairStudio.Shared/Db/BookingDbContext.cs (OnModelCreating block to extend + DbSet pattern)
    - landing-page/lib/data.ts (source content for seed rows per D-12: the `services` array + Full Glam Package)
    - .planning/phases/01-service-catalog/01-RESEARCH.md (Pattern 3 HasData seeding; Pitfall 2 UseSeeding incompatibility; Pitfall 3 unique slug index)
    - .claude/skills/ef-migrations/SKILL.md (exact migration add/update commands and project/startup-project flags)
    - API/ZachHairStudio.Shared/Migrations/20260702131250_InitialSqlServerMigration.cs (existing migration shape)
  </read_first>
  <action>
    In `API/ZachHairStudio.Shared/Db/BookingDbContext.cs`: add `public DbSet<Service> Services => Set<Service>();` next to `Bookings`. In `OnModelCreating`, before `base.OnModelCreating(modelBuilder)`, add a `modelBuilder.Entity<Service>(entity => { ... })` block that sets max lengths mirroring the `Booking` config (Slug/Name 150, ShortDescription 200, LongDescription 2000, Category 50, ImageUrl a reasonable length) and declares `entity.HasIndex(e => e.Slug).IsUnique();` (D-02, Pitfall 3 — DB-level defense in depth). Then call `entity.HasData(...)` with the seed rows.

    Seed rows (D-12, D-13, D-15): migrate the five services from `landing-page/lib/data.ts` (the `services` array) plus the "Full Glam Package" (from `serviceOptions`/`Services.tsx`) into explicit `Service` rows with fixed integer `Id` values 1..6, a unique lowercase-kebab `Slug` each (for example `precision-cut`, `color-and-highlights`, `blowout-and-styling`, `keratin-treatment`, `scalp-treatment`, `full-glam-package`), the existing name and blurb as `Name`/`ShortDescription`, a longer `LongDescription` (expand the teaser into a couple of sentences), a `Category` (for example Cuts, Color, Styling, Treatments), plausible `DurationMinutes` and `Price` values (the existing prices 35/80/55/120/65/199 map directly to `Price`; choose durations such as 45/90/45/120/40/210), sequential `DisplayOrder`, `IsActive = true`, and `ImageUrl = null` (D-08). These prices/durations are Claude-chosen placeholders per D-15 — flag them in the SUMMARY as owner-reviewable, editable via Phase 4 CRUD. Use `HasData()` ONLY; do NOT use `UseSeeding`/`UseAsyncSeeding` (Pitfall 2: they never run under this project's `Migrate()` startup).

    Then generate and apply the migration (BLOCKING — must complete before phase verification): run `dotnet ef migrations add AddServices --project API/ZachHairStudio.Shared --startup-project API/ZachHairStudio.Api`, then `dotnet ef database update --project API/ZachHairStudio.Shared --startup-project API/ZachHairStudio.Api`. Confirm the generated migration contains a `CreateTable` for Services, a unique index on Slug, and `InsertData` rows. If the local `dotnet-ef` tool is older than EF Core 10, update it per the ef-migrations skill first.
  </action>
  <verify>
    <automated>cd API && dotnet test ZachHairStudio.Api.Tests/ZachHairStudio.Api.Tests.csproj --nologo 2>&1 | grep -E "Passed!|Failed!|error" | head -5 && ls ZachHairStudio.Shared/Migrations | grep -i AddServices && echo MIGRATION-OK</automated>
  </verify>
  <acceptance_criteria>
    - `BookingDbContext` exposes `DbSet<Service> Services` and configures a unique index on `Slug`
    - `BookingDbContext.cs` contains no `UseSeeding`/`UseAsyncSeeding`; seed rows are declared via `HasData`
    - A new `AddServices` migration exists under `API/ZachHairStudio.Shared/Migrations/` containing CreateTable(Services), a unique Slug index, and InsertData for 6 seeded rows
    - `dotnet ef database update` applied successfully to the dev database
    - The full `dotnet test` suite passes green (validator + service + controller integration incl. seeded-data reads)
  </acceptance_criteria>
  <done>Services table + unique slug index + 6 seeded rows exist in the dev DB via an applied migration; GET /api/services returns the seeded catalog; full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (write DTOs) | `POST`/`PUT /api/services` accept `ServiceCreateDto`/`ServiceUpdateDto` (dev-only, unauthenticated per D-16) |
| API → SQL Server | LINQ queries against `BookingDbContext.Services` |
| public read (slug) → API | `GET /api/services/{slug}` exposes only active services |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering (SQL injection) | slug/category query in ServicesService | high | mitigate | EF Core LINQ parameterizes all queries; no raw SQL string concatenation for slug lookup (Task 2) |
| T-01-04 | Information Disclosure (slug enumeration of inactive services) | GetBySlugAsync | low | mitigate | `GetBySlugAsync` filters `IsActive == true`; retired/inactive slugs return 404, not stale data (Task 2, D-09) |
| T-01-05 | Elevation of Privilege (unauthenticated writes) | POST/PUT /api/services | high | accept | Explicitly accepted per D-16 — same dev-only exposure as existing BookingsController; nothing deployed publicly; closed by Phase 3 auth gate. Do not add delete-all/admin-only ops here. Tracked in CONCERNS.md. |
| T-01-06 | Tampering (mass assignment on update) | ServiceUpdateDto binding | medium | mitigate | `Id` comes from the route, never the body; `ServiceUpdateDto` is the bound model, not the entity (Task 2) |
| T-01-SC | Tampering (supply chain) | NuGet packages (already audited in Plan 01) | high | mitigate | No new packages introduced in Plan 02; all installs covered by RESEARCH Package Legitimacy Audit (Approved/VERIFIED) |
</threat_model>

<verification>
- `dotnet build API/ZachHairStudio.slnx` succeeds
- `dotnet test API/ZachHairStudio.Api.Tests` passes green (validator + service unit + controller integration + PLAT-01 reflection)
- `AddServices` migration exists and `dotnet ef database update` applied
- With the API running (`dev` skill), `GET http://localhost:5236/api/services` returns the 6 seeded services ordered by DisplayOrder; `GET /api/services/precision-cut` returns 200; an unknown slug returns 404
- `ServicesController.cs` contains no reference to `BookingDbContext`
</verification>

<success_criteria>
- PLAT-01 satisfied: `ServicesController` handled entirely through `ServicesService`; controller never queries `BookingDbContext` (verified by reflection test)
- CAT-03 satisfied: `Service` entity persisted with list + detail endpoints; seeded via migration
- PLAT-02 satisfied: invalid write payloads rejected at the validation layer with a ProblemDetails body (Success Criterion 3)
- The catalog data is real and queryable — backend half of the browse slice complete
</success_criteria>

<output>
Create `.planning/phases/01-service-catalog/01-02-SUMMARY.md` when done. Record: the exact seeded slugs/prices/durations (flagged as owner-reviewable placeholders, D-15); the canonical controller/service dual-validation flow chosen (for later phases to copy); confirmation the migration applied and GET returns seeded rows.
</output>
