---
phase: 08-polish-launch-readiness
plan: 03
type: execute
wave: 3
depends_on: [08-01]
files_modified:
  - API/ZachHairStudio.Api/Program.cs
  - API/ZachHairStudio.Api/Controllers/AuthController.cs
  - API/ZachHairStudio.Shared/Features/Appointments/AppointmentsService.cs
  - API/ZachHairStudio.Shared/Features/Orders/OrdersService.cs
  - API/ZachHairStudio.Api/appsettings.json
autonomous: true
requirements: [LAUNCH-04]
must_haves:
  truths:
    - "Production logging uses JSON console formatter"
    - "Auth login/register, appointment create/cancel, and checkout/quote emit structured log events without passwords/tokens"
  artifacts:
    - API/ZachHairStudio.Api/Program.cs
  key_links:
    - "No Serilog package (D-06); scrub email in log scopes (D-07)"
---

## Phase Goal

Structured logs for requests and key operations (LAUNCH-04, D-06, D-07).

<objective>
Enable JSON console logging in Production and add structured ILogger events for auth, appointments create/cancel, and checkout/quote without logging secrets.
</objective>

<execution_context>
@/workspaces/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-polish-launch-readiness/08-CONTEXT.md
@API/ZachHairStudio.Api/Program.cs
@API/ZachHairStudio.Api/Controllers/AuthController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: JSON console logging in Production</name>
  <files>API/ZachHairStudio.Api/Program.cs, API/ZachHairStudio.Api/appsettings.json</files>
  <action>
    When Production, configure logging with `AddJsonConsole` (clear providers or add JSON formatter per ASP.NET Core docs). Keep Information default; Microsoft.AspNetCore Warning. Do not add Serilog.
  </action>
  <verify>
    <automated>grep -q "AddJsonConsole\|JsonConsole" API/ZachHairStudio.Api/Program.cs</automated>
  </verify>
  <done>Production JSON console logging enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Key-operation structured log events</name>
  <files>API/ZachHairStudio.Api/Controllers/AuthController.cs, API/ZachHairStudio.Shared/Features/Appointments/AppointmentsService.cs, API/ZachHairStudio.Shared/Features/Orders/OrdersService.cs</files>
  <action>
    Inject ILogger where missing. Log Information on successful register/login (user id + truncated email only), appointment create/cancel (appointment id, not password), checkout/quote (order id / item count, not Stripe secrets). Never log JWT, passwords, or full payment payloads.
  </action>
  <verify>
    <automated>grep -n "LogInformation\|ILogger" API/ZachHairStudio.Api/Controllers/AuthController.cs | head -20</automated>
  </verify>
  <done>Key ops emit structured logs without secrets.</done>
</task>

</tasks>

<success_criteria>
JSON logs in Production; key ops logged without PII secrets.
</success_criteria>

<output>
Create `.planning/phases/08-polish-launch-readiness/08-03-SUMMARY.md` when done
</output>
