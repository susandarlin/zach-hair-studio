---
phase: 08-polish-launch-readiness
plan: 04
type: execute
wave: 4
depends_on: [08-01]
files_modified:
  - API/ZachHairStudio.Api/Program.cs
  - API/ZachHairStudio.Api/Controllers/AuthController.cs
  - API/ZachHairStudio.Api/Controllers/OrdersController.cs
  - API/ZachHairStudio.Api.Tests/Features/Launch/RateLimitTests.cs
autonomous: true
requirements: [LAUNCH-05]
must_haves:
  truths:
    - "Auth endpoints are rate limited (~10/min per IP) and return 429 when exceeded"
    - "Checkout/quote endpoints are rate limited (~20/min per IP)"
    - "Rate limiting uses ASP.NET Core RateLimiter fixed window partitioned by IP"
  artifacts:
    - API/ZachHairStudio.Api/Program.cs
    - API/ZachHairStudio.Api.Tests/Features/Launch/RateLimitTests.cs
  key_links:
    - "Only /api/auth/* and /api/orders/checkout* — not global (D-08)"
---

## Phase Goal

Basic rate limiting on auth and checkout (LAUNCH-05, D-08).

<objective>
Add fixed-window per-IP RateLimiter policies for auth (10/min) and checkout (20/min) with 429 + Retry-After.
</objective>

<execution_context>
@/workspaces/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-polish-launch-readiness/08-CONTEXT.md
@API/ZachHairStudio.Api/Program.cs
@API/ZachHairStudio.Api/Controllers/AuthController.cs
@API/ZachHairStudio.Api/Controllers/OrdersController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: AddRateLimiter policies + UseRateLimiter</name>
  <files>API/ZachHairStudio.Api/Program.cs, API/ZachHairStudio.Api/Controllers/AuthController.cs, API/ZachHairStudio.Api/Controllers/OrdersController.cs, API/ZachHairStudio.Api.Tests/Features/Launch/RateLimitTests.cs</files>
  <action>
    AddRateLimiter with named policies `auth` (10/min fixed window per IP) and `checkout` (20/min). OnReject write 429 and Retry-After when possible. app.UseRateLimiter() in pipeline (after routing/CORS as appropriate). Apply [EnableRateLimiting("auth")] on AuthController; [EnableRateLimiting("checkout")] on checkout and quote actions (not GetById). Add RateLimitTests that hammer a limited endpoint and expect 429 (may use Testing env + factory).
  </action>
  <verify>
    <automated>grep -q "AddRateLimiter\|UseRateLimiter\|EnableRateLimiting" API/ZachHairStudio.Api/Program.cs API/ZachHairStudio.Api/Controllers/AuthController.cs API/ZachHairStudio.Api/Controllers/OrdersController.cs</automated>
  </verify>
  <done>Auth and checkout rate limited.</done>
</task>

</tasks>

<success_criteria>
Auth/checkout return 429 under burst; other endpoints unaffected by these policies.
</success_criteria>

<output>
Create `.planning/phases/08-polish-launch-readiness/08-04-SUMMARY.md` when done
</output>
