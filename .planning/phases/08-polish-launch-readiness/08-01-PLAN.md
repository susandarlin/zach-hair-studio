---
phase: 08-polish-launch-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - API/ZachHairStudio.Api/Program.cs
  - API/ZachHairStudio.Api/appsettings.json
  - API/ZachHairStudio.Api/appsettings.Development.json
  - API/ZachHairStudio.slnx
  - API/ZachHairStudio.Api.Tests/Features/Launch/CorsPolicyTests.cs
  - CLAUDE.md
autonomous: true
requirements: [LAUNCH-02]
must_haves:
  truths:
    - "In Production environment, CORS default policy does not use AllowAnyOrigin and only allows configured Cors:Origins"
    - "Development/Testing may remain permissive (AllowAnyOrigin or broad policy)"
    - "ZachHairStudio.Admin is removed from ZachHairStudio.slnx and the Admin project directory is deleted"
  artifacts:
    - API/ZachHairStudio.Api/Program.cs
    - API/ZachHairStudio.Api.Tests/Features/Launch/CorsPolicyTests.cs
    - API/ZachHairStudio.slnx
  key_links:
    - "Cors:Origins config/env drives WithOrigins in Production only (D-01)"
    - "Admin retirement is filesystem + solution only — no redirect stubs (D-02)"
---

## Phase Goal

**As a** salon owner, **I want** production CORS locked to known origins and the legacy Admin MVC gone, **so that** the public API is not wide-open and staff use `dashboard/` only.

<objective>
Implement Production CORS allowlist from `Cors:Origins` and delete ZachHairStudio.Admin from the solution (LAUNCH-02, D-01, D-02).
</objective>

<execution_context>
@/workspaces/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-polish-launch-readiness/08-CONTEXT.md
@.planning/phases/08-polish-launch-readiness/08-RESEARCH.md
@API/ZachHairStudio.Api/Program.cs
@API/ZachHairStudio.slnx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production CORS allowlist + tests</name>
  <files>API/ZachHairStudio.Api/Program.cs, API/ZachHairStudio.Api/appsettings.json, API/ZachHairStudio.Api/appsettings.Development.json, API/ZachHairStudio.Api.Tests/Features/Launch/CorsPolicyTests.cs</files>
  <action>
    Replace unconditional AllowAnyOrigin with: if Production, require non-empty Cors:Origins (semicolon-separated or string array), WithOrigins(...).AllowAnyMethod().AllowAnyHeader(); else keep AllowAnyOrigin for Development/Testing. Document sample Cors:Origins in appsettings.Development.json as localhost:3000 and :3001 (non-secret). Add CorsPolicyTests that assert Production host without AllowAnyOrigin when origins configured (WebApplicationFactory + environment Production if practical, or unit-test policy builder helper). Do not commit production HTTPS URLs as secrets.
  </action>
  <verify>
    <automated>grep -q "Cors:Origins\|GetSection(\"Cors\")\|WithOrigins" API/ZachHairStudio.Api/Program.cs &amp;&amp; ! grep -q "AllowAnyOrigin" API/ZachHairStudio.Api/Program.cs || grep -n "AllowAnyOrigin\|WithOrigins\|IsProduction\|Cors" API/ZachHairStudio.Api/Program.cs | head -40</automated>
  </verify>
  <done>Production CORS uses allowlist; Dev/Testing permissive.</done>
</task>

<task type="auto">
  <name>Task 2: Delete ZachHairStudio.Admin</name>
  <files>API/ZachHairStudio.slnx, API/ZachHairStudio.Admin/</files>
  <action>
    Remove Admin project entry from ZachHairStudio.slnx. Delete the API/ZachHairStudio.Admin directory. Grep repo for ZachHairStudio.Admin references and clear any leftovers (docs ok to note retirement). Confirm `dotnet build API/ZachHairStudio.slnx` succeeds.
  </action>
  <verify>
    <automated>! test -d API/ZachHairStudio.Admin &amp;&amp; ! grep -q "ZachHairStudio.Admin" API/ZachHairStudio.slnx &amp;&amp; cd API &amp;&amp; dotnet build ZachHairStudio.slnx --nologo -v q</automated>
  </verify>
  <done>Admin project gone; solution builds.</done>
</task>

</tasks>

<success_criteria>
1. Production CORS is origin-allowlist based.
2. Admin MVC project deleted from solution and disk.
</success_criteria>

<output>
Create `.planning/phases/08-polish-launch-readiness/08-01-SUMMARY.md` when done
</output>
