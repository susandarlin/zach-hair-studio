---
phase: 08-polish-launch-readiness
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - API/ZachHairStudio.Api/Program.cs
  - CLAUDE.md
  - API/ZachHairStudio.Api.Tests/Features/Launch/MigrationStartupTests.cs
autonomous: true
requirements: [LAUNCH-03]
must_haves:
  truths:
    - "Production does not call db.Database.Migrate() on startup"
    - "Development still migrates on startup for local convenience"
    - "Production fails fast when pending EF migrations exist"
    - "CLAUDE.md documents dotnet ef database update as the production schema path"
  artifacts:
    - API/ZachHairStudio.Api/Program.cs
    - CLAUDE.md
  key_links:
    - "Owner IdentitySeeder still runs in Production after external migrate (D-03/D-04)"
---

## Phase Goal

Controlled production migrations — no startup Migrate in Production (LAUNCH-03, D-03..D-05).

<objective>
Gate Migrate() to Development (keep Testing skip as today); in Production check pending migrations and fail fast; document ef database update in CLAUDE.md.
</objective>

<execution_context>
@/workspaces/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-polish-launch-readiness/08-CONTEXT.md
@API/ZachHairStudio.Api/Program.cs
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate Migrate + pending-migration fail-fast</name>
  <files>API/ZachHairStudio.Api/Program.cs, API/ZachHairStudio.Api.Tests/Features/Launch/MigrationStartupTests.cs</files>
  <action>
    Change startup block: run `db.Database.Migrate()` only when `IsDevelopment()`. In Production (not Testing), if `db.Database.GetPendingMigrations().Any()`, throw InvalidOperationException with clear message to run `dotnet ef database update`. Keep IdentitySeeder.SeedAsync for non-Testing (including Production). Add a focused test or document verification if host-env testing is hard — prefer a small helper testable method if needed.
  </action>
  <verify>
    <automated>grep -n "Migrate\|GetPendingMigrations\|IsDevelopment\|IsProduction" API/ZachHairStudio.Api/Program.cs | head -40</automated>
  </verify>
  <done>Prod skips Migrate; pending migrations fail fast.</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md production migration runbook</name>
  <files>CLAUDE.md, .claude/CLAUDE.md</files>
  <action>
    Add a short note under EF migrations / Commands: Production must apply schema via `dotnet ef database update` (deploy step); API no longer calls Migrate() in Production and will refuse to start if pending migrations exist. Point to ef-migrations skill.
  </action>
  <verify>
    <automated>grep -q "database update" CLAUDE.md || grep -q "database update" .claude/CLAUDE.md</automated>
  </verify>
  <done>Runbook documented.</done>
</task>

</tasks>

<success_criteria>
Production schema path is external migrate + fail-fast; Dev still auto-migrates.
</success_criteria>

<output>
Create `.planning/phases/08-polish-launch-readiness/08-02-SUMMARY.md` when done
</output>
