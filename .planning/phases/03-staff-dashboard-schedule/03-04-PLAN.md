---
phase: 03-staff-dashboard-schedule
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - dashboard/package.json
  - dashboard/next.config.ts
  - dashboard/tsconfig.json
  - dashboard/postcss.config.mjs
  - dashboard/app/layout.tsx
  - dashboard/app/globals.css
  - dashboard/app/page.tsx
  - dashboard/app/login/page.tsx
  - dashboard/lib/api/schema.d.ts
  - dashboard/lib/api/client.ts
  - dashboard/lib/auth.ts
autonomous: false
requirements: [DASH-05]
user_setup:
  - service: dashboard-env
    why: "The dashboard needs the API base URL; mirror landing-page's NEXT_PUBLIC_API_URL convention."
    env_vars:
      - name: NEXT_PUBLIC_API_URL
        source: "dashboard/.env.local — defaults to http://localhost:5236 when unset (same default as landing-page)."

must_haves:
  truths:
    - "The dashboard/ app builds and lints (next build / next lint) with the same Next.js 15 / React 19 / Tailwind 4 / TS 5.8 versions as landing-page (D-16 scaffold)."
    - "A staff member can open the dashboard login page, submit email+password, and — on success — is redirected to /schedule with the JWT stored client-side (D-03)."
    - "A wrong-credentials login shows an inline error and does not redirect."
    - "Visiting a protected route (/schedule) without a stored token redirects to /login (client-side guard); a 401 from the API clears the token and redirects to /login (DASH-05 frontend gate)."
    - "The typed API client is generated from the running API's OpenAPI document and attaches Authorization: Bearer <token> to requests (D-16, D-03)."
  artifacts:
    - "dashboard/ Next.js scaffold (package.json, next.config.ts, tsconfig.json, postcss.config.mjs, app/layout.tsx, app/globals.css, app/page.tsx)"
    - "dashboard/lib/api/schema.d.ts (generated) + dashboard/lib/api/client.ts (openapi-fetch, bearer attach)"
    - "dashboard/lib/auth.ts (token storage/attach/401 handling)"
    - "dashboard/app/login/page.tsx (staff login form)"
  key_links:
    - "client.ts reads NEXT_PUBLIC_API_URL (default http://localhost:5236) and, via a middleware/onRequest hook, adds the stored bearer token to every call; a 401 response triggers auth.ts's clear-token + redirect-to-login."
    - "login/page.tsx posts to /api/auth/login (03-02), stores the returned token via auth.ts, then routes to /schedule."
  prohibitions:
    - statement: "The dashboard session is bearer-token based, not a cookie/NextAuth/iron-session session (D-03 is explicit)."
      status: resolved
      verification: "Grep of dashboard/ shows no next-auth / iron-session dependency; auth.ts stores and attaches a bearer token manually."
---

## Phase Goal

**As a** salon staff member, **I want to** log into a private dashboard, see the day's and week's appointments, and update each appointment's status (including marking no-shows separately), **so that** I can run the salon's schedule from one authenticated place.

<objective>
Stand up the `dashboard/` Next.js app (currently an empty scaffold) and deliver its first end-to-end user capability: **staff log in**. Mirror landing-page's Next 15 / React 19 / Tailwind 4 setup with the salon brand tokens on a denser, tool-like surface (D-15), generate the typed OpenAPI client (D-16) against the real Wave-2 endpoints, and wire bearer-token auth with a login page, client-side route guard, and 401 handling (D-03, DASH-05 frontend).

Purpose: DASH-05 at the frontend — a staff can authenticate into the dashboard and protected routes reject the unauthenticated.
Output: dashboard scaffold + generated client + auth lib + login page, building and linting clean, verified by a human login walkthrough.
</objective>

<execution_context>
@D:/repos/vct/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@D:/repos/vct/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-staff-dashboard-schedule/03-CONTEXT.md
@.planning/phases/03-staff-dashboard-schedule/03-RESEARCH.md
@.planning/phases/03-staff-dashboard-schedule/03-PATTERNS.md
@.claude/skills/openapi-client/SKILL.md
@.claude/skills/dev/SKILL.md
@landing-page/package.json
@landing-page/app/layout.tsx
@landing-page/app/globals.css
@landing-page/lib/appointments.ts
@landing-page/components/Contact.tsx
</context>

## Artifacts this phase produces (this plan)

- New `dashboard/` app: `package.json`, `next.config.ts`, `tsconfig.json`, `postcss.config.mjs`, `app/layout.tsx`, `app/globals.css`, `app/page.tsx`, `app/login/page.tsx`
- `dashboard/lib/api/schema.d.ts` (generated OpenAPI types) + `dashboard/lib/api/client.ts` (openapi-fetch client with bearer attach)
- `dashboard/lib/auth.ts` (token storage helpers, `isUnauthorized` handling, route guard helper)
- npm deps: `swr`, `openapi-fetch` (dep), `openapi-typescript` (devDep)

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Nothing yet — this is the package-legitimacy gate that runs BEFORE the first npm install in this new app (per the package-legitimacy protocol; swr is flagged [SUS] "too-new" and all three package names are [ASSUMED] provenance in 03-RESEARCH.md).</what-built>
  <how-to-verify>
Confirm these three npm packages are the genuine, well-known libraries before they are installed into dashboard/:
1. swr — visit https://www.npmjs.com/package/swr — expect repo github.com/vercel/swr, ~12M weekly downloads, Vercel-owned (the "too-new" flag is a false positive on a routine point release, per 03-RESEARCH.md Package Legitimacy Audit).
2. openapi-fetch — visit https://www.npmjs.com/package/openapi-fetch — expect repo github.com/openapi-ts/openapi-typescript, multi-million weekly downloads ([OK] verdict).
3. openapi-typescript — visit https://www.npmjs.com/package/openapi-typescript — expect the same openapi-ts org ([OK] verdict).
If any package page does not match (typosquat, unknown author, near-zero downloads), STOP and do not install.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the npm installs in Task 2, or describe the mismatch to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold dashboard/ + install deps + generate OpenAPI client</name>
  <files>
dashboard/package.json,
dashboard/next.config.ts,
dashboard/tsconfig.json,
dashboard/postcss.config.mjs,
dashboard/app/layout.tsx,
dashboard/app/globals.css,
dashboard/app/page.tsx,
dashboard/lib/api/schema.d.ts,
dashboard/lib/api/client.ts
  </files>
  <read_first>
landing-page/package.json,
landing-page/next.config.ts,
landing-page/tsconfig.json,
landing-page/postcss.config.mjs,
landing-page/app/layout.tsx,
landing-page/app/globals.css,
.claude/skills/openapi-client/SKILL.md,
.claude/skills/dev/SKILL.md
  </read_first>
  <action>
Create the `dashboard/` app mirroring `landing-page/` conventions. `package.json`: same scripts (dev/build/start/lint) and pinned versions as landing-page (next ^15.3.0, react ^19.1.0, react-dom ^19.1.0, tailwindcss ^4.1.0, @tailwindcss/postcss, typescript ^5.8.0, @types/*); add dependency `swr` and `openapi-fetch`, and devDependency `openapi-typescript` (only after the Task 1 checkpoint approval). Copy `next.config.ts` (minimal), `tsconfig.json` (with the `@/*` path alias), and `postcss.config.mjs` verbatim. `app/layout.tsx`: reuse the Playfair_Display + Inter font wiring from landing-page but set a light, neutral, tool-like body surface (D-15: branded-but-utilitarian, not the dark marketing charcoal) and metadata title "Zach Hair Studio — Staff". `app/globals.css`: `@import "tailwindcss";` and copy the `@theme` gold/charcoal + font tokens from landing-page (same palette, D-15) — do not invent a second palette. `app/page.tsx`: a minimal entry that redirects to /schedule (or /login if unauthenticated) — the real schedule UI lands in 03-05.

Install deps (`npm install` in dashboard/). Start the API using the dev skill, then generate the typed client per the openapi-client skill: `npx -y openapi-typescript http://localhost:5236/openapi/v1.json -o lib/api/schema.d.ts`. Add `lib/api/client.ts` using `openapi-fetch` `createClient<paths>({ baseUrl })` where baseUrl = `process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:5236"` (strip trailing slash, matching landing-page/lib/appointments.ts). Register an onRequest middleware on the client that reads the stored token (from lib/auth.ts, Task 3) and sets the `Authorization: Bearer` header when present. Treat schema.d.ts as a build artifact (regenerate, never hand-edit — skill convention).
  </action>
  <acceptance_criteria>
    - `dashboard/package.json` pins next/react/tailwind/typescript to the same versions as landing-page and lists swr + openapi-fetch (deps) and openapi-typescript (devDep).
    - `dashboard/lib/api/schema.d.ts` exists and contains a path entry for `/api/auth/login` and `/api/schedule` (proving it was generated against the Wave-2 API).
    - `dashboard/lib/api/client.ts` reads `NEXT_PUBLIC_API_URL` and attaches a bearer header via onRequest.
    - `cd dashboard && npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && npm run build</automated>
  </verify>
  <done>The dashboard app scaffolds, installs, generates a live-schema typed client, and builds clean.</done>
</task>

<task type="auto">
  <name>Task 3: Bearer-token auth lib + staff login page + route guard</name>
  <files>
dashboard/lib/auth.ts,
dashboard/app/login/page.tsx
  </files>
  <read_first>
landing-page/lib/appointments.ts,
landing-page/components/Contact.tsx,
dashboard/lib/api/client.ts,
.planning/phases/03-staff-dashboard-schedule/03-PATTERNS.md,
.planning/phases/03-staff-dashboard-schedule/03-02-SUMMARY.md
  </read_first>
  <action>
Create `lib/auth.ts` (no auth/token precedent in the repo — follow RESEARCH's "JWT storage & attach-to-request" row). Provide: a token store (JWT storage location is Claude's discretion per CONTEXT — localStorage is acceptable for this internal tool; document the choice) with get/set/clear helpers that also retain expiresAt/displayName/role; an `attachToken`/`getToken` used by client.ts's onRequest; and a typed error helper mirroring landing-page's `AppointmentApiError` (status-carrying, with an `isUnauthorized` getter for 401) plus reuse of the ProblemDetails/ModelState `extractErrorMessage` parsing logic verbatim (backend error shape is identical). Add a `requireAuth()`/redirect helper the protected pages call on mount to bounce to /login when no valid token is present, and a `handleUnauthorized()` that clears the token and redirects to /login (used when the API returns 401).

Create `app/login/page.tsx` as a client component (`"use client"`) reusing the Contact.tsx `Field` wrapper + `inputClass` + controlled-input + `useState`/`handleSubmit` pattern, adapted to a dense utilitarian tone (D-15): email + password fields, submit calls the typed client's POST /api/auth/login (or a thin fetch to the same endpoint), on 200 stores the token via auth.ts and `router.push("/schedule")`, on 401 shows an inline error message and does not redirect, on network failure shows a transport error. Weave in loading/disabled state while submitting.
  </action>
  <acceptance_criteria>
    - `dashboard/lib/auth.ts` exports token get/set/clear, a bearer attach used by client.ts, an `isUnauthorized`/401 handler, and a route-guard helper.
    - `dashboard/app/login/page.tsx` is a client component that POSTs to /api/auth/login, stores the token on success, redirects to /schedule, and shows an inline error on 401 without redirecting.
    - No `next-auth` or `iron-session` dependency appears in dashboard/package.json (D-03 bearer-only).
    - `cd dashboard && npm run build && npm run lint` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && npm run build && npm run lint</automated>
  </verify>
  <done>Staff can log in against the real API; the token is stored and attached; protected routes and 401s redirect to /login.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The dashboard login flow end-to-end: scaffolded app, generated typed client, bearer auth, login page, and route guard, wired to the real API's /api/auth/login (03-02) with the seeded Owner account.</what-built>
  <how-to-verify>
1. Start the stack with the dev skill (API + dashboard). Ensure Jwt:SigningKey / Owner:Email / Owner:InitialPassword user-secrets are set (from 03-01 user_setup).
2. Visit the dashboard /schedule route directly while logged out — confirm you are redirected to /login.
3. Log in with a WRONG password — confirm an inline error appears and you stay on /login.
4. Log in with the seeded Owner email + password — confirm you are redirected to /schedule (the schedule UI itself is stubbed until 03-05; an empty/placeholder page is expected here).
5. Confirm the page uses the salon brand fonts/gold accent on a light, tool-like surface (D-15), not the dark marketing theme.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dashboard build | New third-party packages (swr, openapi-fetch, openapi-typescript) enter the frontend build. |
| browser-held JWT → API | The dashboard stores and attaches the bearer token; its storage and 401 handling define the client-side session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | npm installs (swr / openapi-fetch / openapi-typescript) | high | mitigate | Blocking-human package-legitimacy checkpoint (Task 1) verifies each on npmjs.com before install; swr's "too-new" flag documented as a false positive (03-RESEARCH.md). |
| T-03-11 | Information Disclosure | client-side JWT storage | medium | accept | The ~12h token is stored client-side for this internal staff tool; XSS-exfil risk is bounded by the short lifetime and the small internal surface. Refresh-token/session hardening is deferred to Phase 7/8 (D-03). Documented tradeoff. |
| T-03-12 | Spoofing | stale/expired token handling | medium | mitigate | A 401 response clears the stored token and redirects to /login (handleUnauthorized); the route guard blocks protected pages without a token. |
</threat_model>

<verification>
- `cd dashboard && npm run build && npm run lint` — clean.
- Generated `lib/api/schema.d.ts` includes /api/auth/login and /api/schedule paths.
- Human-verify checkpoint: logged-out /schedule redirects to /login; wrong password errors inline; correct Owner login reaches /schedule.
</verification>

<success_criteria>
The dashboard app builds on the shared Next.js stack, a staff member can log in against the real API and land on a protected route, and unauthenticated access is bounced to /login — all with a bearer-token (not cookie) session.
</success_criteria>

<output>
Create `.planning/phases/03-staff-dashboard-schedule/03-04-SUMMARY.md` when done.
</output>
