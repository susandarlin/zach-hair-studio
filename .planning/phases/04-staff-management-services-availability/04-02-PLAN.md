---
phase: 04-staff-management-services-availability
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - dashboard/lib/api/schema.d.ts
  - dashboard/components/DashboardNav.tsx
  - dashboard/app/schedule/page.tsx
  - dashboard/lib/useServices.ts
  - dashboard/app/services/page.tsx
  - dashboard/components/ServiceForm.tsx
  - dashboard/components/ImageUploadField.tsx
  - dashboard/components/ConfirmDialog.tsx
  - dashboard/components/icons.tsx
autonomous: true
requirements: [MGMT-01]
must_haves:
  truths:
    - "An Owner opening /services (Owner-only, D-01) sees a list of services (Name, Category, Duration, Price, Status, Edit/Retire actions) and can create, edit, soft-retire (D-02, IsActive=false), reactivate, and image (D-03) a service without a deploy."
    - "The Services nav link is rendered for Owner sessions and completely absent from the DOM for Staff sessions (D-16 hide, not disable); a Staff user hitting /services directly is redirected to /schedule (client guard) with the server [Authorize(Roles=Owner)] gate as the real backstop."
    - "DashboardNav shows Schedule, Services (Owner-only), Availability, and the existing Add staff / session / logout cluster; the active page link uses the gold-dark active treatment and is shared by the schedule page (so staff can navigate between all surfaces)."
    - "E1 Services list states: empty renders 'No Services Yet' + Add Service; loading shows muted skeleton rows; error shows 'Couldn't Load Services.' + Refresh; populated shows Active rows first then Retired; a service missing an image renders a placeholder and the row never collapses; long name/description truncates with ellipsis (full text in the edit form / title tooltip); 0/1/many all read correctly (no pagination)."
    - "E2 Service form states: create starts blank with Save disabled until required fields are filled; edit mode pre-fills every field via one shared ServiceForm; a failed network/500 save shows a top-of-card banner with Save re-enabled to retry; FluentValidation field errors surface on invalid submit; long-description textarea scrolls internally and fields never clip."
    - "E3 image upload states: empty shows the dashed 'No image yet' box + Upload Image; mid-upload shows a spinner and disables Save; invalid type/size shows the inline 'Couldn't upload image. Use a JPG, PNG, or WebP under 5MB.'; populated shows the thumbnail with Replace/Remove; a stored ImageUrl that 404s falls back to the placeholder via onError (never a broken-image icon)."
    - "E8 dashboard nav states: no async fetch so no loading state; Services link is role-hidden (not disabled); on narrow viewports the nav-link row wraps and all links stay reachable with no horizontal overflow."
    - "Retire uses the existing ConfirmDialog with new CONFIRM_COPY.Retired destructive copy; Reactivate is an immediate inline button with no dialog (D-15 discretion)."
  artifacts:
    - dashboard/components/DashboardNav.tsx
    - dashboard/lib/useServices.ts
    - dashboard/app/services/page.tsx
    - dashboard/components/ServiceForm.tsx
    - dashboard/components/ImageUploadField.tsx
    - "CONFIRM_COPY.Retired entry in dashboard/components/ConfirmDialog.tsx"
    - "plus / image / trash icons added to dashboard/components/icons.tsx"
  key_links:
    - "OpenAPI client (dashboard/lib/api/schema.d.ts) regenerated against the running API so the POST /api/services/{id}/image endpoint is typed before ImageUploadField calls it."
    - "ServiceForm reuses the inputClass/Field wrapper from dashboard/app/staff/new/page.tsx — no second input style defined."
    - "DashboardNav is the single shared header; schedule/page.tsx swaps its inline header for <DashboardNav /> so all three surfaces are cross-navigable."
  prohibitions:
    - statement: "The dashboard must not treat the client-side role check as the security boundary — the server [Authorize(Roles=Owner)] gate (Plan 01) is authoritative; hiding the Services link is UX only."
      status: unverified
      verification: flagged
---

<objective>
Deliver the Owner-facing Services management surface in `dashboard/` — the frontend half of the MGMT-01 vertical slice — plus the shared `DashboardNav` that unlocks navigation to every dashboard surface (D-16). After this plan an Owner can list, create, edit, retire, reactivate, and image a service end-to-end against the Plan 01 backend.

Purpose: Give the Owner a real, usable catalog editor matching Phase 3's utilitarian dashboard chrome, and stop duplicating the header markup on every page.
Output: `DashboardNav`, `useServices`, `/services` page, `ServiceForm`, `ImageUploadField`, a `CONFIRM_COPY.Retired` entry, three new icons, and a regenerated OpenAPI client.
</objective>

<execution_context>
@C:/repos/vct/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@C:/repos/vct/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-staff-management-services-availability/04-CONTEXT.md
@.planning/phases/04-staff-management-services-availability/04-UI-SPEC.md
@.planning/phases/04-staff-management-services-availability/04-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan (excluded from cross-plan drift verification):
- `DashboardNav` (component) — `dashboard/components/DashboardNav.tsx`
- `useServices` (hook) — `dashboard/lib/useServices.ts`
- `ServicesPage` default export — `dashboard/app/services/page.tsx`
- `ServiceForm` (component) — `dashboard/components/ServiceForm.tsx`
- `ImageUploadField` (component) — `dashboard/components/ImageUploadField.tsx`
- `CONFIRM_COPY.Retired` (copy entry) — `dashboard/components/ConfirmDialog.tsx`
- `PlusIcon`, `ImageIcon`, `TrashIcon` — `dashboard/components/icons.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate OpenAPI client + extract shared DashboardNav</name>
  <files>dashboard/lib/api/schema.d.ts, dashboard/components/DashboardNav.tsx, dashboard/app/schedule/page.tsx, dashboard/components/icons.tsx</files>
  <read_first>
    - dashboard/app/schedule/page.tsx (lines 154-181 inline header block = extraction source; getSession/handleLogout/isOwner usage)
    - dashboard/components/icons.tsx (existing icon shape: SVGProps function pattern, LogOutIcon)
    - dashboard/lib/auth.ts (getSession, AuthSession.role)
    - .claude/skills/openapi-client/SKILL.md (regeneration flow — API must be running)
    - .claude/skills/dev/SKILL.md (how to launch the API for the regen)
    - .planning/phases/04-staff-management-services-availability/04-UI-SPEC.md (Component Patterns: DashboardNav)
  </read_first>
  <action>
    Start the API (dev skill), then regenerate `dashboard/lib/api/schema.d.ts` per the openapi-client skill (`npx -y openapi-typescript http://localhost:5236/openapi/v1.json -o lib/api/schema.d.ts` from `dashboard/`) so the new `POST /api/services/{id}/image` endpoint from Plan 01 is typed. Create `dashboard/components/DashboardNav.tsx` by extracting the `<header>` block currently inline in `schedule/page.tsx`: keep the `bg-surface-alt border-b border-border` bar, the serif wordmark, and the right-side session-name / Add-staff (Owner-only) / logout cluster unchanged; insert a nav-link row between the wordmark and that cluster with `next/link` links Schedule, Services (rendered only when `session.role === "Owner"`, omitted from the DOM otherwise per D-16), and Availability. Active link uses `text-gold-dark font-semibold`; inactive `text-ink hover:text-gold-dark` (determine active via `usePathname`). Accept `session` (and any `onLogout`) as props or read via `getSession`, matching how the schedule header derives `isOwner`. Replace the inline header in `schedule/page.tsx` with `<DashboardNav />`. Add `PlusIcon`, `ImageIcon`, and `TrashIcon` to `dashboard/components/icons.tsx` following the existing `SVGProps<SVGSVGElement>` inline-SVG pattern.
  </action>
  <verify>
    <automated>cd dashboard && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/lib/api/schema.d.ts` contains a path entry for `/api/services/{id}/image`.
    - `DashboardNav.tsx` renders Schedule/Availability always and Services only for Owner (`role === "Owner"`); no Services element is emitted for Staff.
    - `schedule/page.tsx` imports and renders `<DashboardNav />` instead of an inline `<header>`.
    - `npm run build` (typecheck) passes.
  </acceptance_criteria>
  <done>The client is regenerated, the shared nav exists with correct role visibility, and the schedule page uses it.</done>
</task>

<task type="auto">
  <name>Task 2: useServices hook + /services list page + Retire copy</name>
  <files>dashboard/lib/useServices.ts, dashboard/app/services/page.tsx, dashboard/components/ConfirmDialog.tsx</files>
  <read_first>
    - dashboard/lib/useSchedule.ts (SWR + ApiError/handleUnauthorized/extractErrorMessage wrapper shape to mirror)
    - dashboard/app/schedule/page.tsx (requireAuth/getSession bootstrap, loading/error state rendering, ConfirmDialog wiring)
    - dashboard/app/staff/new/page.tsx (Owner-gate redirect: session.role !== "Owner" -> router.replace("/schedule"))
    - dashboard/components/ConfirmDialog.tsx (CONFIRM_COPY shape — Cancelled/NoShow entries)
    - dashboard/lib/api/client.ts (api.GET/POST/PUT typed client)
    - .planning/phases/04-staff-management-services-availability/04-UI-SPEC.md (Copywriting Contract + E1 Services list states)
  </read_first>
  <action>
    Create `dashboard/lib/useServices.ts` mirroring `useSchedule.ts` but keyed on `["services"]` with NO polling (`refreshInterval` omitted) — services rarely change mid-session. It fetches `GET /api/services` (returns all Active services; for retired-row rendering fetch strategy see below) and exposes `{ services, isLoading, error, mutate }` with the same `ApiError`/`handleUnauthorized`/`extractErrorMessage` handling. Create `dashboard/app/services/page.tsx`: Owner-gate bootstrap copied from `staff/new/page.tsx` (redirect Staff to /schedule), render `<DashboardNav />`, heading "Services" + a gold-dark "Add Service" button, and a list/table with columns Name, Category, Duration, Price, Status chip, Edit/Retire (or Reactivate) actions. Implement the E1 states from the UI-SPEC: empty → "No Services Yet" / "Add your first service to start taking bookings."; loading → muted skeleton rows; error → "Couldn't Load Services." + Refresh (same shape as schedule error). Active rows first, Retired rows after (muted left border + "Retired" chip, never red). Long values truncate with ellipsis and a `title` tooltip. Retire calls `PUT /api/services/{id}` with `IsActive=false` behind the existing `ConfirmDialog`; add a `CONFIRM_COPY.Retired` entry ("Retire Service" / "Retire {name}? It won't be bookable on the public site until you reactivate it." / "Retire Service"). Reactivate is an immediate inline button (PUT IsActive=true) with no dialog.
    Note on retired visibility: `GET /api/services` returns only active services; to show retired rows either add a client-side "Show retired" toggle that is acceptable as executor's choice per the UI-SPEC, or render only active rows this pass — do NOT invent a new API filter param. Pick one and keep the empty/one/many states correct.
  </action>
  <verify>
    <automated>cd dashboard && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `/services` redirects a Staff session to `/schedule` and renders for Owner.
    - Empty, loading, error, and populated states all render per the UI-SPEC copy.
    - `CONFIRM_COPY.Retired` exists and the retire action routes through `ConfirmDialog`; Reactivate has no dialog.
    - `npm run build` passes.
  </acceptance_criteria>
  <done>An Owner can view the services list with all E1 states and retire/reactivate a service.</done>
</task>

<task type="auto">
  <name>Task 3: ServiceForm (create/edit) + ImageUploadField</name>
  <files>dashboard/components/ServiceForm.tsx, dashboard/components/ImageUploadField.tsx</files>
  <read_first>
    - dashboard/app/staff/new/page.tsx (inputClass, Field wrapper, submit/try-catch/ApiError/extractErrorMessage handling, submitting state)
    - dashboard/components/ImageUploadField.tsx (this file — net-new; follow UI-SPEC prescriptive 160x160 dashed-box shape)
    - dashboard/lib/api/client.ts (api.POST/PUT; multipart via the typed client or a direct fetch to /api/services/{id}/image)
    - dashboard/lib/api/schema.d.ts (regenerated — ServiceResponseDto shape, image endpoint)
    - .planning/phases/04-staff-management-services-availability/04-UI-SPEC.md (ServiceForm + ImageUploadField Component Patterns; E2/E3 states)
  </read_first>
  <action>
    Create `dashboard/components/ServiceForm.tsx`: a single component with create AND edit modes (not two components), rendered in a `bg-surface-alt rounded-2xl p-8 border border-border` card, reusing the `inputClass`/`Field` wrapper from `staff/new/page.tsx` (import or lift into a shared spot — do not redefine a second input style). Fields in order: Name, Short description, Long description (textarea), Category, Duration (minutes, number), Price (currency number), Image (`ImageUploadField`), then a gold-dark Save button ("Add Service" / "Save Service"). On submit call `POST /api/services` (create) or `PUT /api/services/{id}` (edit); surface FluentValidation field errors from the ProblemDetails `errors` object via `extractErrorMessage`, and show a top-of-card banner for network/500 with Save re-enabled (E2 states). In edit mode pre-fill all fields; disable Save until required fields are present in create mode. Create `dashboard/components/ImageUploadField.tsx`: a 160x160 (`h-40 w-40`) dashed-border box (`border-2 border-dashed border-border rounded-xl`). Empty → centered "No image yet" caption + Upload Image button (accept `image/jpeg,image/png,image/webp`). On file pick, POST multipart to `/api/services/{id}/image` (Owner token attached by the client middleware); show a spinner overlay and disable Save while uploading; on 400 show the inline "Couldn't upload image. Use a JPG, PNG, or WebP under 5MB."; populated → thumbnail fills the box with Replace/Remove controls (Remove clears ImageUrl via `PUT /api/services/{id}` with `ImageUrl: null`). Add `onError` on the `<img>` to fall back to the "No image yet" placeholder when a stored URL 404s. For a brand-new (not-yet-created) service, gate image upload until the service exists (image endpoint needs an id) — either require save-first or upload after create.
  </action>
  <verify>
    <automated>cd dashboard && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `ServiceForm` handles both create and edit with a single component and reuses `inputClass`/`Field`.
    - Field-level validation errors and a network/500 banner both render (E2); Save re-enables after failure.
    - `ImageUploadField` shows empty/uploading/populated/error states and only accepts the three allowed types client-side; onError falls back to the placeholder (E3).
    - `npm run build` passes.
  </acceptance_criteria>
  <done>An Owner can create and edit a service including uploading, replacing, and removing its image, with validation and error states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staff session → /services route + Services nav link | A non-Owner staff session must not reach the Owner catalog editor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Information Disclosure / Elevation of Privilege | Services nav link + /services route visible to Staff | medium | mitigate | Services link omitted from DOM for Staff (D-16); client guard redirects Staff to /schedule; server `[Authorize(Roles=Owner)]` (Plan 01) is the authoritative gate — UI hiding is UX only |
| T-04-SC | Tampering | npm installs | low | accept | No new npm packages; only a client regen of an existing generated file |
</threat_model>

<verification>
- `cd dashboard && npm run build` passes (typecheck across all new pages/components).
- Behavioral proof for the write path lives in Plan 01's backend integration tests (no dashboard test runner is configured — RESEARCH Validation Architecture).
- Manual: as Owner, create → edit → upload image → retire → reactivate a service; as Staff, confirm no Services nav link and /services redirects to /schedule.
</verification>

<success_criteria>
- Owner can manage the full service catalog + images from /services.
- Shared DashboardNav with correct role visibility across all dashboard pages.
- All E1/E2/E3/E8 UI states implemented per the UI-SPEC.
</success_criteria>

<output>
Create `.planning/phases/04-staff-management-services-availability/04-02-SUMMARY.md` when done.
</output>
