---
phase: 04-staff-management-services-availability
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - dashboard/lib/api/schema.d.ts
  - dashboard/lib/useAvailability.ts
  - dashboard/components/StylistPicker.tsx
  - dashboard/components/WeekStripEditor.tsx
  - dashboard/components/TimeOffCalendar.tsx
  - dashboard/app/availability/page.tsx
autonomous: true
requirements: [MGMT-02]
must_haves:
  truths:
    - "Any authenticated staff opening /availability picks a stylist (D-14 picker-only, no stylist CRUD), edits that stylist's weekly hours on a visual week strip (D-05) and one-off time off on a month calendar (D-07), and saves both with a single Save Changes button that submits hours and time-off together in one save moment (D-12)."
    - "The week strip has 7 rows (Mon-Sun), a 06:00-22:00 default window at 24px/hour and 40px row height, 15-minute snap, and painting maps each segment 1:1 to a StylistWorkingHours row; a second non-contiguous segment on the same weekday is the D-06 gap-as-break; a weekday with no segments shows the muted 'Closed' overlay."
    - "The time-off calendar is a single-month 7-column grid with ChevronLeft/Right month nav; painting a date range creates a StylistTimeOff block rendered as a dashed-muted band (bg-muted/15 border-2 border-dashed border-muted, never gold, never red) with an optional reason and a Remove control."
    - "E4 stylist picker states: zero active stylists shows 'No stylists yet' and hides the editors; loading shows a muted placeholder; fetch error shares the page-level 'Couldn't Load Availability.' + Refresh; populated renders chips (select under 768px) with the selected stylist in gold-dark; many stylists wrap (no horizontal page scroll); exactly 1 still shows a pre-selected picker; long names truncate in-chip."
    - "E5 week-strip states: empty weekday = Closed overlay; loading = muted skeleton strip with drag disabled until loaded; error shares the page-level Couldn't-Load state; populated = gold segments 1:1 with rows; partial (some days closed) is the normal per-day state; 0/1/many segments read as Closed / single band / multiple non-contiguous bands with gaps as breaks."
    - "E6 time-off calendar states: empty shows 'No time off scheduled.' with the grid still fully rendered; populated shows dashed-muted bands with reason on hover/tap and a Remove control; a long reason truncates within its band and shows in full on hover/tap."
    - "Painting on the week strip snaps to the same 15-minute grid the public booking flow reads (BOOK-01) so saved hours always align to bookable slots."
    - { statement: "E5 overflow — multiple non-contiguous segments per weekday render without visual overlap (D-06 gap-as-break); proven by a rendering check once the drag/paint mechanic is finalized.", verification: backstop }
  artifacts:
    - dashboard/lib/useAvailability.ts
    - dashboard/components/StylistPicker.tsx
    - dashboard/components/WeekStripEditor.tsx
    - dashboard/components/TimeOffCalendar.tsx
    - dashboard/app/availability/page.tsx
  key_links:
    - "OpenAPI client regenerated so the PUT working-hours / POST+DELETE time-off endpoints from Plan 03 are typed before useAvailability calls them."
    - "The single Save Changes submits hours + time-off together (D-12) — one authoritative moment for the Plan 05 conflict check to evaluate the whole new state."
    - "/availability uses the shared DashboardNav (Plan 02) and requireAuth bootstrap (no Owner gate — any staff, D-13)."
  assumptions:
    - "E6 overflow (UI-SPEC unresolved): many overlapping/adjacent time-off ranges in one month may visually crowd the calendar; no upstream merge/truncation rule exists. ASSUMPTION for the executor: render each range as its own dashed band and, if crowding appears in practice, visually merge adjacent same-reason ranges. Flag to the owner if it becomes a usability problem — do not silently drop."
  prohibitions:
    - statement: "The availability editor must not introduce a second availability representation on the client — it reads/writes only the Plan 03 endpoints over the single StylistWorkingHours/StylistTimeOff model (D-08)."
      status: unverified
      verification: flagged
---

<objective>
Deliver the staff Availability editor in `dashboard/` — the frontend half of the MGMT-02 vertical slice. Any authenticated staff picks a stylist, paints weekly hours on a visual week strip and one-off time off on a month calendar, and saves both with one Save Changes button. This is the happy-path editor; the hard-block conflict panel arrives in Plan 05.

Purpose: Give staff a visual, tool-like availability editor (Phase 3 chrome) whose saves feed the same slot logic the public booking flow reads.
Output: `useAvailability`, `StylistPicker`, `WeekStripEditor`, `TimeOffCalendar`, the `/availability` page, and a regenerated OpenAPI client.
</objective>

<execution_context>
@C:/repos/vct/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@C:/repos/vct/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-staff-management-services-availability/04-CONTEXT.md
@.planning/phases/04-staff-management-services-availability/04-UI-SPEC.md
@.planning/phases/04-staff-management-services-availability/04-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan (excluded from cross-plan drift verification):
- `useAvailability` (hook) — `dashboard/lib/useAvailability.ts`
- `StylistPicker` (component) — `dashboard/components/StylistPicker.tsx`
- `WeekStripEditor` (component) — `dashboard/components/WeekStripEditor.tsx`
- `TimeOffCalendar` (component) — `dashboard/components/TimeOffCalendar.tsx`
- `AvailabilityPage` default export — `dashboard/app/availability/page.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate client + useAvailability hook + StylistPicker</name>
  <files>dashboard/lib/api/schema.d.ts, dashboard/lib/useAvailability.ts, dashboard/components/StylistPicker.tsx</files>
  <read_first>
    - dashboard/lib/useSchedule.ts (SWR + ApiError/handleUnauthorized/extractErrorMessage wrapper; compound-key pattern ["schedule", from, to])
    - dashboard/components/WeekChips.tsx (chip-row selection pattern to mirror for StylistPicker)
    - dashboard/app/schedule/page.tsx (how /api/Stylists is fetched for the picker; requireAuth bootstrap)
    - dashboard/lib/api/client.ts (typed client)
    - .claude/skills/openapi-client/SKILL.md + .claude/skills/dev/SKILL.md (regen flow, API must be running)
    - .planning/phases/04-staff-management-services-availability/04-UI-SPEC.md (E4 stylist picker states)
  </read_first>
  <action>
    Start the API (dev skill) and regenerate `dashboard/lib/api/schema.d.ts` so the Plan 03 endpoints (`PUT /api/availability/{stylistId}/working-hours`, `POST`/`DELETE /api/availability/{stylistId}/time-off`) are typed. Create `dashboard/lib/useAvailability.ts` mirroring `useSchedule.ts` but keyed on `["availability", stylistId]` (fetch that stylist's current working hours + time off; the read source is the existing GET endpoints — if a GET for a single stylist's hours/time-off is not present, derive it from what Plan 03 exposes or fetch via the availability routes; do NOT invent a second store). Expose `{ hours, timeOff, isLoading, error, mutate }` with the same ApiError handling. Create `dashboard/components/StylistPicker.tsx` following `WeekChips.tsx`: fetch active stylists (`GET /api/Stylists`), render name chips (collapse to a `<select>` under 768px), selected chip in gold-dark (accent item 11). Implement E4 states: zero stylists → "No stylists yet" and hide the editors; loading → muted placeholder; error → page-level "Couldn't Load Availability." + Refresh; many → wrap; long names truncate in-chip.
  </action>
  <verify>
    <automated>cd dashboard && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `schema.d.ts` contains the `/api/availability/{stylistId}/working-hours` and `/time-off` paths.
    - `useAvailability` is keyed on stylistId and returns hours + timeOff.
    - `StylistPicker` renders chips/select with the gold-dark selected state and all E4 states.
    - `npm run build` passes.
  </acceptance_criteria>
  <done>The client is regenerated and the stylist selection + availability data plumbing exist.</done>
</task>

<task type="auto">
  <name>Task 2: WeekStripEditor (drag-paint weekly hours)</name>
  <files>dashboard/components/WeekStripEditor.tsx</files>
  <read_first>
    - dashboard/components/DayGrid.tsx (proportional time-grid rendering, 15-min alignment precedent)
    - dashboard/lib/useAvailability.ts (hours shape from Task 1)
    - .planning/phases/04-staff-management-services-availability/04-UI-SPEC.md (Spacing Scale week-strip metrics; Component Patterns WeekStripEditor; E5 states; Color accent item 10)
  </read_first>
  <action>
    Create `dashboard/components/WeekStripEditor.tsx`: 7 horizontal rows Mon-Sun (Label-role weekday abbreviation left), each a 40px-tall (`h-10`) bar at 24px/hour (`w-6` per hour) spanning the default 06:00-22:00 window (16 hours; horizontally scrollable under 768px). The 06:00-22:00 window is a DEFAULT, not a locked salon-hours fact (UI-SPEC Open Question 1 / RESEARCH A1) — confirm against Phase 2 seed data (seed hours are 09:00-18:00) and either keep the wider window or make it configurable; note the choice. Staff click-drag to paint a working-hours segment (`bg-gold-dark/15 border border-gold-dark`) snapping to 15-minute increments (reuse Phase 2's grid so painted hours align to bookable slots, BOOK-01); dragging again on the same row paints an additional non-contiguous segment (D-06 gap-as-break). Hovering a segment reveals a remove control; a weekday with no segments shows the muted "Closed" overlay. Each segment maps 1:1 to a `StylistWorkingHours` row `{DayOfWeek, StartTime, EndTime}`. Expose the current segments to the parent page (controlled value + onChange) so the single Save Changes can serialize the whole week. Implement E5 states: loading skeleton with drag disabled until loaded; populated gold segments; Closed overlay for empty days. The exact drag mechanic (click-drag vs click-then-click) is the executor's choice per UI-SPEC Open Question 3 — click-drag with 15-min snap is recommended.
  </action>
  <verify>
    <automated>cd dashboard && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 7 rows at 40px height, 24px/hour, 15-minute snapping; painted segments use the gold-dark treatment.
    - Multiple non-contiguous segments per weekday are supported (gap-as-break) and render without overlap.
    - Empty weekday shows the "Closed" overlay; loading disables drag.
    - Segments are exposed to the parent as `{DayOfWeek, StartTime, EndTime}` values; `npm run build` passes.
  </acceptance_criteria>
  <done>Staff can visually paint a stylist's weekly hours with breaks-as-gaps, ready to save.</done>
</task>

<task type="auto">
  <name>Task 3: TimeOffCalendar + /availability page + single Save Changes</name>
  <files>dashboard/components/TimeOffCalendar.tsx, dashboard/app/availability/page.tsx</files>
  <read_first>
    - dashboard/components/icons.tsx (ChevronLeftIcon/ChevronRightIcon for month nav; TrashIcon added in Plan 02)
    - dashboard/components/AppointmentDetailPanel.tsx (salon-local time formatting precedent)
    - dashboard/app/schedule/page.tsx (page shell, DashboardNav usage, requireAuth, error/loading rendering)
    - dashboard/components/WeekStripEditor.tsx (controlled value/onChange from Task 2)
    - dashboard/lib/useAvailability.ts
    - .planning/phases/04-staff-management-services-availability/04-UI-SPEC.md (TimeOffCalendar Component Pattern; Copywriting Contract; E6 states; Color time-off derived treatment)
  </read_first>
  <action>
    Create `dashboard/components/TimeOffCalendar.tsx`: a single-month 7-column grid (40x40 day cells, 4px gutter, Label-role weekday headers, `ChevronLeftIcon`/`ChevronRightIcon` month nav). Staff click a start date then an end date (or drag a range) to paint a `StylistTimeOff` block rendered as the dashed-muted band (`bg-muted/15 border-2 border-dashed border-muted`, never gold, never red), with an optional reason input appearing once a range is painted and a Remove control on hover/tap. Empty → "No time off scheduled." caption with the grid still fully rendered (E6). Expose painted ranges to the parent (controlled value + onChange). Create `dashboard/app/availability/page.tsx`: requireAuth bootstrap (NO Owner gate — any staff, D-13), render `<DashboardNav />`, heading "Availability", the `StylistPicker`, then two stacked cards (`bg-surface-alt rounded-2xl p-6 border border-border`) — "Weekly Hours" (`WeekStripEditor`) and "Time Off" (`TimeOffCalendar`) — and a single gold-dark "Save Changes" button below both that submits the whole week's hours (`PUT /api/availability/{stylistId}/working-hours`) and the time-off changes (`POST`/`DELETE /api/availability/{stylistId}/time-off`) together as one save action via `useAvailability`. On success show the "Availability saved." flash and `mutate()`. On a non-conflict network/500 failure show a generic "Couldn't save availability. Try again." banner (the conflict-specific 409 panel is added in Plan 05 — leave a clearly-marked seam where the conflict payload will render). For the E6 overflow unresolved case, follow the assumption recorded in this plan's front-matter (per-range bands; flag if crowded).
  </action>
  <verify>
    <automated>cd dashboard && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `/availability` renders for any authenticated staff (no Owner redirect) using DashboardNav.
    - Time-off ranges render as dashed-muted bands with reason + Remove; empty shows "No time off scheduled." with a full grid.
    - A single Save Changes submits hours + time-off together and shows "Availability saved." on success and a generic error banner on failure, with a marked seam for the Plan 05 conflict panel.
    - `npm run build` passes.
  </acceptance_criteria>
  <done>Staff can edit and save a stylist's full availability (hours + time off) in one action; public slots reflect it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser → availability write endpoints | Any authenticated staff may write (D-13); anonymous must be rejected server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | Client-painted local times sent to the API | medium | mitigate | Server revalidates every segment (Plan 03 validator); the client never owns persistence truth |
| T-04-14 | Information Disclosure | Availability page reachable only behind auth | low | mitigate | `requireAuth` client guard + server `[Authorize]` (Plan 03); no Owner gate by design (D-13) |
| T-04-SC | Tampering | npm installs | low | accept | No new npm packages; only a regen of an existing generated file |
</threat_model>

<verification>
- `cd dashboard && npm run build` passes.
- Behavioral proof for the write path lives in Plan 03's integration tests (no dashboard test runner configured).
- Manual: as staff, pick a stylist, narrow their hours + add time off, Save Changes, then confirm the public /book slots for that stylist reflect the change.
</verification>

<success_criteria>
- Visual week-strip + calendar editor with a single combined Save Changes.
- All E4/E5/E6 UI states implemented; E5 overflow backstop and E6 overflow assumption recorded.
- Saves feed the same availability model the booking flow reads.
</success_criteria>

<output>
Create `.planning/phases/04-staff-management-services-availability/04-04-SUMMARY.md` when done.
</output>
