---
phase: 04-staff-management-services-availability
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - API/ZachHairStudio.Api.Tests/Features/Availability/WorkingHoursReplaceTests.cs
  - API/ZachHairStudio.Api.Tests/Features/Availability/TimeOffTests.cs
  - API/ZachHairStudio.Shared/Features/Availability/WorkingHoursReplaceDto.cs
  - API/ZachHairStudio.Shared/Features/Availability/WorkingHoursReplaceDtoValidator.cs
  - API/ZachHairStudio.Shared/Features/Availability/TimeOffCreateDto.cs
  - API/ZachHairStudio.Shared/Features/Availability/TimeOffCreateDtoValidator.cs
  - API/ZachHairStudio.Shared/Features/Availability/AvailabilityService.cs
  - API/ZachHairStudio.Api/Controllers/AvailabilityController.cs
  - API/ZachHairStudio.Api/Program.cs
autonomous: true
requirements: [MGMT-02]
must_haves:
  truths:
    - "PUT /api/availability/{stylistId}/working-hours replaces the whole week of StylistWorkingHours for that stylist (delete-existing-then-insert), and SlotService.GetOpenSlotsAsync immediately reflects the change against the SAME StylistWorkingHours table (D-08 same-model proof)."
    - "POST /api/availability/{stylistId}/time-off creates a StylistTimeOff range and DELETE removes it; the added range immediately blocks the corresponding open slots and removal unblocks them, read back through SlotService."
    - "Availability write endpoints require authentication: anonymous → 401; any authenticated staff (Owner OR Staff) → 200 (D-13, no per-stylist ownership restriction)."
    - "Adjacency: two touching working-hours segments on one weekday (e.g. 09:00-12:00 and 12:00-17:00) persist as two rows and yield contiguous slots; a gap between segments (12:00-13:00) yields no slots inside the gap — the D-06 gap-as-break, no Break entity."
    - "Empty: replacing a stylist's week with zero segments persists no StylistWorkingHours rows for that stylist and SlotService returns no slots for them (an all-closed week is valid)."
    - "Idempotency: submitting the same working-hours payload twice yields the same StylistWorkingHours rows with no duplicates (replace = delete-then-insert)."
    - { statement: "Ordering — working-hours segments are persisted/returned in a stable order (by StartTime) so equal-compare rows do not reorder between saves.", verification: backstop }
    - { statement: "Concurrency — two staff saving the same stylist's availability concurrently do not produce duplicated or interleaved StylistWorkingHours rows; the replace runs inside one transaction.", verification: backstop }
  artifacts:
    - API/ZachHairStudio.Shared/Features/Availability/WorkingHoursReplaceDto.cs
    - API/ZachHairStudio.Shared/Features/Availability/WorkingHoursReplaceDtoValidator.cs
    - API/ZachHairStudio.Shared/Features/Availability/TimeOffCreateDto.cs
    - API/ZachHairStudio.Shared/Features/Availability/TimeOffCreateDtoValidator.cs
    - API/ZachHairStudio.Shared/Features/Availability/AvailabilityService.cs
    - API/ZachHairStudio.Api/Controllers/AvailabilityController.cs
  key_links:
    - "AvailabilityService writes to the EXACT StylistWorkingHours / StylistTimeOff tables SlotService already reads — no parallel availability store (D-08)."
    - "AvailabilityController class-level [Authorize] (any staff, D-13); Result->ProblemDetails translation mirrors ScheduleController."
    - "AvailabilityService registered as scoped in Program.cs (same pattern as SlotService/ServicesService)."
  prohibitions:
    - statement: "The staff availability editor must never write to a table the open-slot query does not read — there is exactly one availability model (StylistWorkingHours + StylistTimeOff); no second/parallel 'editable schedule' store may be introduced (D-08)."
      status: unverified
      verification: flagged
---

<assumption_delta_decision>
Primary noun: the single stylist-availability model (`StylistWorkingHours` + `StylistTimeOff`).
Detected signal: pluralization keyword "second" matched in the phrase "same availability model, not a second one" (an assertion of singularity, not a request for a second store).
Decision: **no-change**.
Rationale: Phase 4 makes the EXISTING single availability model staff-editable; the write path targets the same tables `SlotService` reads (D-08 / roadmap constraint). No parallel/second availability representation is introduced.
Invariant guard: Task 1's `WorkingHoursReplaceTests`/`TimeOffTests` assert the change is observable through `SlotService.GetOpenSlotsAsync` output (not just the write endpoint's 200), so a future phase cannot silently fork a second availability system without breaking this contract test.
</assumption_delta_decision>

<objective>
Add the availability write path — the backend half of the MGMT-02 vertical slice — so any authenticated staff can replace a stylist's weekly working hours and manage one-off time off, and Phase 2's open-slot query reflects the change immediately against the same tables (D-08). No conflict check yet (that is Plan 05 / MGMT-03); this plan delivers the happy-path editor.

Purpose: Staff keep availability accurate from the dashboard without a deploy, feeding the existing SlotService rather than a second system.
Output: `WorkingHoursReplaceDto`/`TimeOffCreateDto` + validators, `AvailabilityService`, `AvailabilityController`, DI registration, and integration tests that prove writes are reflected by SlotService.
</objective>

<execution_context>
@C:/repos/vct/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@C:/repos/vct/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-staff-management-services-availability/04-CONTEXT.md
@.planning/phases/04-staff-management-services-availability/04-RESEARCH.md
@.planning/phases/04-staff-management-services-availability/04-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan (excluded from cross-plan drift verification):
- `WorkingHoursReplaceDto` (class), `WorkingHoursReplaceDtoValidator` (class)
- `TimeOffCreateDto` (class), `TimeOffCreateDtoValidator` (class)
- `AvailabilityService` (class) with a working-hours replace method + time-off create/remove methods
- `AvailabilityController` (routes: `PUT api/availability/{stylistId}/working-hours`, `POST api/availability/{stylistId}/time-off`, `DELETE api/availability/{stylistId}/time-off/{timeOffId}`)
- `WorkingHoursReplaceTests`, `TimeOffTests` (test classes)
- `AvailabilityService` scoped registration in `Program.cs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — working-hours replace + time-off tests proving SlotService reflection</name>
  <files>API/ZachHairStudio.Api.Tests/Features/Availability/WorkingHoursReplaceTests.cs, API/ZachHairStudio.Api.Tests/Features/Availability/TimeOffTests.cs</files>
  <read_first>
    - API/ZachHairStudio.Api.Tests/Features/Appointments/ScheduleControllerTests.cs (SeedStaffAndLoginAsync, CreateAuthenticatedClient, BookingDates usage, test-only Jwt override)
    - API/ZachHairStudio.Api.Tests/Features/Availability/SlotServiceTests.cs (how slots are asserted, seeded stylist/working-hours data)
    - API/ZachHairStudio.Api.Tests/TestSupport/BookingDates.cs (NextBookableDate/SlotOn helpers)
    - API/ZachHairStudio.Shared/Features/Availability/SlotService.cs (GetOpenSlotsAsync signature + grid math)
    - API/ZachHairStudio.Shared/Db/BookingDbContext.cs (seeded stylists + working hours ids 1-28)
  </read_first>
  <behavior>
    - Anonymous PUT /api/availability/{stylistId}/working-hours → 401; authenticated Staff → 200 (D-13).
    - After replacing a stylist's week with a narrower window, GET /api/appointments/slots (or SlotService directly) shows only slots inside the new window — same-model proof (D-08).
    - Two touching segments (09:00-12:00, 12:00-17:00) → contiguous slots; a gap (12:00-13:00) → no slots in the gap.
    - Replacing with zero segments → no slots for that stylist.
    - Submitting the identical payload twice → identical persisted rows, no duplicates.
    - POST time-off over a window → those slots disappear; DELETE that time-off → slots return.
  </behavior>
  <action>
    Create `WorkingHoursReplaceTests.cs` and `TimeOffTests.cs` under `API/ZachHairStudio.Api.Tests/Features/Availability/` using `IClassFixture<SqlServerWebApplicationFactory>` and the ScheduleControllerTests token-minting pattern (seed a Staff user, login for a bearer token, test-only Jwt config). Assert reflection through the real open-slot path — call `GET /api/appointments/slots` for the target service/stylist/date after each availability write and compare the returned slot set, so the test proves the write feeds SlotService (D-08), not merely that the write returned 200. Use `BookingDates` for in-horizon dates. These tests are RED until Tasks 2-3 land.
  </action>
  <verify>
    <automated>dotnet test API/ZachHairStudio.Api.Tests --filter "FullyQualifiedName~Availability.WorkingHoursReplaceTests|FullyQualifiedName~Availability.TimeOffTests"</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist and assert against `GET /api/appointments/slots` output after writes (not just the write status code).
    - Adjacency, empty-week, idempotency, and time-off block/unblock cases are present.
    - Tests are RED before implementation.
  </acceptance_criteria>
  <done>The availability write behavior is pinned by tests that prove SlotService reflection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — DTOs, validators, AvailabilityService (no conflict check yet)</name>
  <files>API/ZachHairStudio.Shared/Features/Availability/WorkingHoursReplaceDto.cs, API/ZachHairStudio.Shared/Features/Availability/WorkingHoursReplaceDtoValidator.cs, API/ZachHairStudio.Shared/Features/Availability/TimeOffCreateDto.cs, API/ZachHairStudio.Shared/Features/Availability/TimeOffCreateDtoValidator.cs, API/ZachHairStudio.Shared/Features/Availability/AvailabilityService.cs, API/ZachHairStudio.Api/Program.cs</files>
  <read_first>
    - API/ZachHairStudio.Shared/Features/Services/ServiceCreateDtoValidator.cs (RuleFor-per-field validator style)
    - API/ZachHairStudio.Shared/Features/Appointments/AppointmentsService.cs (transactional mutation + Result<T> outcomes)
    - API/ZachHairStudio.Shared/Features/Availability/StylistWorkingHours.cs + StylistTimeOff.cs (entity shapes)
    - API/ZachHairStudio.Shared/Result.cs (Success/NotFoundError/ValidationError factories)
    - API/ZachHairStudio.Shared/Db/BookingDbContext.cs (StylistWorkingHours / StylistTimeOff DbSets, Stylists)
  </read_first>
  <action>
    Create `WorkingHoursReplaceDto` carrying the full list of weekday segments for one stylist (each segment: DayOfWeek, StartTime, EndTime as TimeOnly). Create `WorkingHoursReplaceDtoValidator` mirroring `ServiceCreateDtoValidator`: each segment's EndTime greater than StartTime, times within a 00:00-24:00 range, and segments snapping to 15-minute increments (matching the SlotService grid). Create `TimeOffCreateDto` (StartsAt, EndsAt as DateTimeOffset, optional Reason max 200) and `TimeOffCreateDtoValidator` (EndsAt greater than StartsAt, Reason length). Create `AvailabilityService` (scoped) with: a `ReplaceWorkingHoursAsync(int stylistId, WorkingHoursReplaceDto)` that verifies the stylist exists (NotFoundError otherwise), validates, then inside a single transaction deletes all existing `StylistWorkingHours` for that stylist and inserts the new segments, `SaveChangesAsync`, returns `Result<...>.Success`; a `AddTimeOffAsync(int stylistId, TimeOffCreateDto)` that inserts a `StylistTimeOff` row; and a `RemoveTimeOffAsync(int stylistId, int timeOffId)` that deletes it (NotFound if absent or belongs to another stylist). Write only to `StylistWorkingHours` / `StylistTimeOff` — the same tables `SlotService` reads (D-08); do NOT add any new table or entity. Register `AvailabilityService` as scoped in `Program.cs` next to `SlotService`.
  </action>
  <verify>
    <automated>dotnet build API/ZachHairStudio.slnx</automated>
  </verify>
  <acceptance_criteria>
    - `AvailabilityService.ReplaceWorkingHoursAsync` performs delete-then-insert in one transaction against `StylistWorkingHours`.
    - Validators reject EndTime<=StartTime, non-15-min-aligned segments, and EndsAt<=StartsAt.
    - No new entity/DbSet/table is introduced (D-08).
    - `AvailabilityService` is registered scoped in `Program.cs`; the solution builds.
  </acceptance_criteria>
  <done>The service layer persists availability into the existing tables with validation; solution compiles.</done>
</task>

<task type="auto">
  <name>Task 3: AvailabilityController (any-staff gate) + Result translation</name>
  <files>API/ZachHairStudio.Api/Controllers/AvailabilityController.cs</files>
  <read_first>
    - API/ZachHairStudio.Api/Controllers/ScheduleController.cs (class-level [Authorize], IsNotFound/IsSystemError translation, InconsistentDataProblem helper)
    - API/ZachHairStudio.Api/Controllers/ServicesController.cs (validator injection + AddToModelState + ValidationProblem pattern)
    - API/ZachHairStudio.Shared/Features/Identity/StaffRoles.cs
    - .planning/phases/04-staff-management-services-availability/04-PATTERNS.md (AvailabilityController analog section)
  </read_first>
  <action>
    Create `AvailabilityController` with `[ApiController]`, `[Route("api/[controller]")]`, and class-level `[Authorize]` (any authenticated staff — D-13, NO Owner role and NO per-stylist ownership restriction). Inject `AvailabilityService` and the two validators. Actions: `PUT {stylistId}/working-hours` ([FromBody] WorkingHoursReplaceDto), `POST {stylistId}/time-off` ([FromBody] TimeOffCreateDto), `DELETE {stylistId}/time-off/{timeOffId}`. Validate DTOs with the injected validators (ValidationProblem + AddToModelState on failure) and translate `Result` states to HTTP exactly like ScheduleController: `IsNotFound` → NotFound ProblemDetails, `IsSystemError` → InconsistentDataProblem, success → Ok/NoContent. Do NOT emit a conflict/409 path here — that arrives in Plan 05.
  </action>
  <verify>
    <automated>dotnet test API/ZachHairStudio.Api.Tests --filter "FullyQualifiedName~Availability.WorkingHoursReplaceTests|FullyQualifiedName~Availability.TimeOffTests"</automated>
  </verify>
  <acceptance_criteria>
    - `AvailabilityController` has class-level `[Authorize]` and no Owner-role or per-stylist restriction.
    - Anonymous requests get 401; authenticated Staff get 200/204.
    - Result→ProblemDetails translation matches ScheduleController.
    - Task 1's availability tests are green.
  </acceptance_criteria>
  <done>Any authenticated staff can replace working hours and manage time off; SlotService reflects the changes; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard/browser → AvailabilityController | Untrusted callers may attempt availability writes |
| client-painted week-strip local times → server persistence | Client sends DayOfWeek/TimeOnly the server must revalidate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Elevation of Privilege | Availability write endpoints reachable anonymously | high | mitigate | Class-level `[Authorize]`; anonymous → 401 (WorkingHoursReplaceTests asserts) |
| T-04-07 | Tampering | Client echoes DayOfWeek/TimeOnly that could be inconsistent | medium | mitigate | Server revalidates every segment via `WorkingHoursReplaceDtoValidator` (15-min alignment, End>Start); persistence is server-authoritative |
| T-04-13 | Elevation of Privilege (accepted by design) | Any staff editing any stylist's availability | low | accept | D-13 explicitly allows any authenticated staff to edit any stylist; no per-stylist ownership check is added |
| T-04-SC | Tampering | NuGet installs | low | accept | No new packages this phase |
</threat_model>

<verification>
- `dotnet test API/ZachHairStudio.Api.Tests --filter "FullyQualifiedName~Availability"` green (includes existing SlotService/DST tests + new write tests).
- SlotService reflection is proven by asserting `GET /api/appointments/slots` output after writes (D-08 same-model proof).
- No EF migration added (no schema change — StylistWorkingHours/StylistTimeOff already exist).
</verification>

<success_criteria>
- Any staff can replace weekly hours and manage time off from the API.
- Public open-slot query reflects edits immediately against the same tables.
- Adjacency/empty/idempotency edges covered by tests.
</success_criteria>

<output>
Create `.planning/phases/04-staff-management-services-availability/04-03-SUMMARY.md` when done.
</output>
