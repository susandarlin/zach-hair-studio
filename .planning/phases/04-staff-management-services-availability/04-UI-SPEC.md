---
phase: 4
slug: staff-management-services-availability
status: approved
shadcn_initialized: false
preset: none
created: 2026-07-24
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

Scope: two new `dashboard/` surfaces — **Services** (MGMT-01, Owner-only CRUD + soft-retire + image
upload) and **Availability** (MGMT-02/MGMT-03, all-staff working-hours + time-off editor with a
hard-blocking conflict check) — plus a shared nav addition so staff can reach both from anywhere in
the dashboard (D-16). This phase does **not** introduce a new design system: it extends the exact
`dashboard/` visual language Phase 3 already shipped and locked in `03-UI-SPEC.md`. Every token,
type role, and component pattern below is inherited verbatim from Phase 3 unless a row is explicitly
marked **new (Phase 4)**.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — inherited from Phase 3. `components.json` still absent (root and `dashboard/`); confirmed via `ls dashboard/components.json` (not found) during this research pass. shadcn init gate is **not re-asked**: Phase 3 already made this call for the same app, and the codebase (`dashboard/components/*.tsx`, `dashboard/app/staff/new/page.tsx`) is three phases deep into a hand-rolled-Tailwind precedent. Re-litigating it here would contradict "don't re-ask what upstream already answered" and would fork the dashboard's component style mid-app. |
| Preset | not applicable |
| Component library | none — plain semantic HTML + Tailwind utility classes, following `dashboard/components/ConfirmDialog.tsx` / `dashboard/app/staff/new/page.tsx`'s existing `Field`-wrapper + controlled-input pattern. Phase 4 adds new small wrapper components (`ServiceForm`, `ImageUploadField`, `StylistPicker`, `WeekStripEditor`, `TimeOffCalendar`, `ConflictList`, `DashboardNav`) in the same style — no new dependency. |
| Icon library | none — hand-rolled inline SVG, extending `dashboard/components/icons.tsx`. Phase 4 needs three new icons: **plus** (Add Service / Add Time Off), **image/photo** (upload placeholder), **trash** (remove time-off range / remove uploaded image). Reuse existing `ChevronLeftIcon`/`ChevronRightIcon` (calendar month nav), `CheckIcon` (save success), `AlertIcon` (conflict banner), `XCircleIcon` (retired chip) — no other new icons needed. |
| Font | unchanged — `font-serif` (Playfair Display) for Display role only; `font-sans` (Inter) for everything else. Same `dashboard/app/layout.tsx` wiring, no changes required. |

**Theme tokens:** no new tokens needed. Phase 4 reuses every token already defined in
`dashboard/app/globals.css` (`--color-gold`, `--color-gold-dark`, `--color-ink`, `--color-surface`,
`--color-surface-alt`, `--color-border`, `--color-muted`, `--color-destructive`) verbatim. One new
**derived** (not new-token) usage: a muted dashed treatment for time-off ranges, built from the
existing `--color-muted` token (`border-muted`, `bg-muted/15`) — see Color section.

---

## Spacing Scale

Declared values (must be multiples of 4) — unchanged from Phase 3:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-text gaps, chip/badge inline padding, calendar-cell gutter |
| sm | 8px | Compact spacing — label-to-input, list-row action-icon gaps |
| md | 16px | Default panel/card padding, form field vertical rhythm |
| lg | 24px | Section padding, gap between page header and content |
| xl | 32px | Layout gaps — gap between Services list and its Add button row |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Not used at dashboard density (unchanged) |

Exceptions (inherited, still apply): **44px minimum touch target** on every icon button, toggle, and
form control (WCAG 2.5.5) — Edit/Retire/Reactivate row actions, calendar day cells' tap area, week-strip
segment handles.

**New (Phase 4) spacing exceptions:**

- **Week-strip row height = 40px** (`h-10`, a 4px-multiple), one row per weekday (Mon–Sun), matching
  the existing 20px-per-15-min proportional-sizing precedent from the day grid but at a coarser,
  "compact" grain appropriate to a weekly editor, not a daily one.
- **Week-strip hour width = 24px per hour** (`w-6`, a 4px-multiple) against a default business window
  of **06:00–22:00** (16 hours → 384px total bar width on desktop; horizontally scrollable under
  768px). The 06:00–22:00 window is a **default, not a locked salon-hours fact** — no upstream
  artifact specifies actual salon opening hours, so this is flagged for the executor to confirm
  against seed data or make configurable.
- **Segment drag/paint snaps to 15-minute increments**, reusing Phase 2's existing 15-minute slot
  grid (BOOK-01) so a stylist's painted hours always align to the same grid the public booking flow
  reads slots from.
- **Time-off calendar day cells = 40px × 40px** (`h-10 w-10`, 4px-multiples) with 4px gutter, standard
  7-column month grid, matching the week-strip's 40px row rhythm for visual consistency between the
  two paired editors.

---

## Typography

Unchanged from Phase 3 — exactly 4 sizes / 2 weights, reused verbatim (no new sizes needed for
Phase 4's forms/lists):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Label | 12px (`text-xs`) | Regular (400) | 1.4 — uppercase, `tracking-wider`: form field labels, weekday abbreviations on the week-strip, calendar weekday headers, status chips ("Active", "Retired") |
| Body | 14px (`text-sm`) | Regular (400) | 1.5 — service list rows, form inputs, conflict-list rows, time-off range captions |
| Heading | 18px (`text-lg`) | Semibold (600) | 1.3 — page/section titles ("Services", "Availability", "Weekly Hours", "Time Off", "Add Service", conflict panel title) |
| Display | 24px (`text-2xl`), `font-serif` | Semibold (600) | 1.2 — unchanged, top-bar wordmark only ("Zach Hair Studio") |

Weight notes unchanged: Regular (400) default for all body/label/list content; Semibold (600)
reserved for panel/section headings, the Display role, primary CTA button labels, and the active
nav-link/toggle-segment text.

---

## Color

Unchanged dominant/secondary/accent tokens from Phase 3:

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#faf8f4` (`bg-surface`) | Page background behind both new pages' content |
| Secondary (30%) | `#efe9df` (`bg-surface-alt`) | Card/panel surfaces (service form, availability editor panels, conflict panel), list-row zebra striping, header bar |
| Accent (10%) | `#a87f3d` (`text-gold-dark` / `bg-gold-dark`) | See "Accent reserved for" below — extends Phase 3's list, does not replace it |
| Destructive | `#e11d48` (`text-rose-600` / `bg-rose-600`) | Retire-service confirmation, conflict-blocked save banner, validation error text/banners |

**Accent reserved for** (Phase 3's 7-item list still applies verbatim; Phase 4 adds items 8–13 —
nothing outside this combined list may use gold):

1–7. *(unchanged — see `03-UI-SPEC.md`: Log In button, Add Staff Member button, Confirmed-appointment
left border, Completed checkmark badge, active Day/Week toggle + Today button, selected date in date
picker, focus rings)*

8. **"Add Service" / "Save Service" primary submit buttons** on the Services form — same treatment as
   item 2 ("Add Staff Member")
9. **"Save Changes" primary submit button** on the Availability page (the single combined save for
   both hours and time-off edits — see Component Patterns)
10. **Painted working-hours segments** on the week-strip (`bg-gold-dark/15 border border-gold-dark`) —
    represents "the salon is open," the direct weekly analog of item 3's Confirmed-appointment gold
    border
11. **The currently-selected stylist** in the stylist picker (Availability page)
12. **The active nav link** in the dashboard header (new — see Component Patterns: `DashboardNav`)
13. **Focus ring on the week-strip drag handles and calendar day cells**, same `focus:border-gold-dark`
    pattern as item 7, extended to the two new interactive-control surfaces

Never use gold for: body text, disabled states, the "Add Time Off" secondary button (kept neutral —
see Component Patterns), time-off painted ranges (explicitly **not** gold; time off is "unavailable,"
not "open" — see below), or any decorative fill outside the 13-item list above.

**Time-off painted ranges (new derived treatment, not a new color token):** `bg-muted/15
border-2 border-dashed border-muted`, `text-ink` for the optional reason label. Deliberately neither
gold (not "available") nor destructive-red (time off is routine, not an error/danger state) — reuses
the existing muted token the same way Phase 3 used it for Cancelled/No-show block backgrounds.

**Status chip color-coding for the Services list** (mirrors Phase 3's Confirmed/Cancelled pattern):

| Status | Chip treatment |
|--------|------------------|
| Active | no chip shown — the unmarked default state (consistent with "never use gold for... the default/inactive case"; Active needs no visual flag since it's the common case) |
| Retired | `bg-surface-alt/60` (muted), `text-muted` "Retired" label, row content at full opacity but the row itself gets a subtle muted left border (`border-l-4 border-border`, same neutral-gray treatment Phase 3 used for Cancelled appointments) — never red, since retiring is reversible and routine, not an error |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | **Add Service** (Services page) / **Save Changes** (Availability page) |
| Empty state heading | **No Services Yet** (Services list, only reachable pre-seed) |
| Empty state body | "Add your first service to start taking bookings." |
| Error state | **Couldn't Load Services.** / **Couldn't Load Availability.** "We couldn't reach the booking system. Try refreshing, or check your connection." + a **Refresh** retry button (same shape as Phase 3's schedule error state) |
| Destructive confirmation | **Retire Service**: "Retire {name}? It won't be bookable on the public site until you reactivate it." |

Additional Phase-4-specific copy (extends the template's five rows, matching Phase 3's precedent of a
richer table for multi-surface phases):

| Element | Copy |
|---------|------|
| Services page heading | "Services" |
| Add Service form heading | "Add Service" (new) / "Edit Service" (existing) |
| Service form field labels | Name · Short description · Long description · Category · Duration (minutes) · Price · Image |
| Service save success | "Service saved." (inline flash, no modal) |
| Retire success (no confirmation needed on reverse) | "Service reactivated." (Reactivate is immediate, no dialog — reversible, low-stakes, matches D-15's discretion note treating reactivate as non-destructive) |
| Image upload — empty placeholder | "No image yet" caption inside a dashed-border box, with an **Upload Image** button |
| Image upload — error | "Couldn't upload image. Use a JPG, PNG, or WebP under 5MB." |
| Image upload — populated controls | **Replace** · **Remove** |
| Only-remaining-service edge case | No blocking UI — retiring the last active service is allowed (per CONTEXT.md discretion default); no special warning copy this phase |
| Availability page heading | "Availability" |
| Stylist picker label | "Stylist" |
| Weekly hours section heading | "Weekly Hours" |
| Weekly hours closed-day label | "Closed" (muted overlay on a weekday row with zero painted segments) |
| Time off section heading | "Time Off" |
| Add Time Off CTA (secondary, not gold) | **Add Time Off** |
| Time off empty state | "No time off scheduled." |
| Time off remove control | **Remove** (no confirmation dialog — pre-save, low-stakes; only Save Changes is consequential) |
| Availability save success | "Availability saved." |
| Conflict panel heading | **Can't Save — Conflicting Appointments** |
| Conflict panel body | "These confirmed appointments fall outside the new hours or inside the new time off. Cancel or reschedule them first, then try again." |
| Conflict row shape | "{clientName} · {serviceName} · {stylistName} · {salon-local date/time}" (e.g. "Aria Chen · Balayage · Maya · Jul 28, 2:00 PM MMT") — matches D-11's required fields exactly |
| Nav labels (new — `DashboardNav`) | **Schedule** · **Services** (Owner-only, hidden for Staff per D-16) · **Availability** · **Add staff** (Owner-only, existing) |

---

## UI Considerations

> Populated by the ui-phase UI-consideration probe (Step 9.5) and lifted by plan-phase's
> `## UI Considerations` lift rule via the identical rule as SPEC `## Edge Coverage`. Shape-rooted UI *state*
> coverage (empty / loading / error / populated / partial / overflow / zero-one-many / long-text).
> Empty-state and error-state COPY live in `## Copywriting Contract` above — this section covers
> state coverage and REFERENCES those rows rather than restating the copy (de-dup).

Applicable state considerations resolved (probe engine proposed 49; reconciled with 3 E6 states the
classifier under-detected → 52 total): **44 covered, 1 backstop, 1 unresolved, 6 dismissed**. Resolved
under the `--auto` convention (Claude-authored truths, no user prompts — researcher ran auto with 0
questions). Covered rows the researcher already wrote are retained; the probe-surfaced gaps
(list loading, form submit-error, edit pre-fill, long-text on chips, etc.) are newly authored below.

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | E1 Services list (list-collection) | ✅ covered | Zero services renders the Copywriting Contract's "No Services Yet" heading below the Add Service CTA — only reachable pre-seed, not a normal runtime path. |
| loading | E1 Services list (list-collection) | ✅ covered | Initial fetch shows muted skeleton rows (same treatment as Phase 3's schedule load), replaced in place once data arrives. |
| error | E1 Services list (list-collection) | ✅ covered | Load failure renders "Couldn't Load Services." + Refresh, same shape as Phase 3's schedule error state. |
| populated | E1 Services list (list-collection) | ✅ covered | Rows show Name, Category, Duration, Price, Status chip, Edit/Retire (or Reactivate) actions — Active rows first, Retired rows after (or behind a "Show retired" toggle, executor's choice). |
| partial | E1 Services list (list-collection) | ✅ covered | A service missing an optional field (no image, empty long description) still renders a full row — the image cell falls back to a placeholder and the row layout never collapses. |
| overflow | E1 Services list (list-collection) | ✅ covered | Long name/description values truncate with ellipsis in the list row; full text is visible in the edit form, never clipped there. |
| zero-one-many | E1 Services list (list-collection) | ✅ covered | 0 → empty state; 1 → single row (no plural-copy mismatch); many → the list scrolls, no pagination this phase. |
| long-text | E1 Services list (list-collection) | ✅ covered | Same mechanic as overflow — single-line ellipsis with the full string available on the edit form (and as a `title` tooltip on the row). |
| empty | E2 Service form — create (form) | ✅ covered | New Service form starts fully blank; Save disabled until required fields (name, short/long description, category, duration, price) are filled, mirroring the Add Staff form's required-field pattern. |
| loading | E2 Service form (form) | ✅ covered | In edit mode the card renders in a muted/disabled state until the selected service's values finish loading into the fields, then becomes interactive. |
| error | E2 Service form (form) | ✅ covered | A failed save (network/500, distinct from field validation) shows a banner error at the top of the card with Save re-enabled to retry — never a silent drop. |
| populated | E2 Service form (form) | ✅ covered | Edit mode pre-fills every field from the selected service via the single create/edit `ServiceForm` in edit mode. |
| partial | E2 Service form (form) | ✅ covered | Invalid/incomplete submit shows field-level + banner validation errors (FluentValidation-backed via existing `ServiceCreateDtoValidator`/`ServiceUpdateDtoValidator`); Save stays enabled to allow retry. |
| overflow | E2 Service form (form) | ✅ covered | The long-description textarea scrolls internally; the card grows and the page scrolls, individual fields never clip. |
| long-text | E2 Service form (form) | ✅ covered | Textarea wraps; single-line inputs (name/category) scroll horizontally within the field rather than overflowing the card. |
| empty | E3 Service image upload (media) | ✅ covered | No image yet renders the dashed-border "No image yet" placeholder with an Upload Image button — never a broken-image icon. |
| loading | E3 Service image upload (media) | ✅ covered | Mid-upload shows a spinner/progress overlay on the placeholder; the form's Save button is disabled until the upload settles. |
| error | E3 Service image upload (media) | ✅ covered | Invalid type/size shows the Copywriting Contract's inline error; the rest of the form is unaffected and remains submittable without an image. |
| populated | E3 Service image upload (media) | ✅ covered | Existing/uploaded image renders as a thumbnail with Replace/Remove controls. |
| partial | E3 Service image upload (media) | ✅ covered | A stored image URL that 404s (missing file) falls back to the "No image yet" placeholder via an `onError` handler rather than a broken-image icon. |
| long-text | E3 Service image upload (media) | ⊘ dismissed | Media surface carries no user-authored long text; alt text is the service name, already handled by E1/E2 truncation. |
| empty | E4 Availability stylist picker (interactive-control) | ✅ covered | Zero active stylists (pre-seed edge) shows a "No stylists yet" message and hides the editors — staff are added via the existing Owner-only flow first. |
| loading | E4 Availability stylist picker (interactive-control) | ✅ covered | The stylist list loads into a muted placeholder before any chip is selectable. |
| error | E4 Availability stylist picker (interactive-control) | ✅ covered | A stylist-fetch failure surfaces the page-level "Couldn't Load Availability." + Refresh (shared with the availability page load error). |
| populated | E4 Availability stylist picker (interactive-control) | ✅ covered | Active stylists render as chips (or a select on narrow viewports); the selected stylist gets the gold-dark treatment (accent item 11). |
| partial | E4 Availability stylist picker (interactive-control) | ⊘ dismissed | Picker items are atomic (a stylist name chip) — there is no partial-field shape to render. |
| overflow | E4 Availability stylist picker (interactive-control) | ✅ covered | Many stylists wrap the chips onto multiple rows (or collapse to a select under 768px); no horizontal page scroll. |
| zero-one-many | E4 Availability stylist picker (interactive-control) | ✅ covered | Reads correctly at exactly 1 stylist (still shown as a picker, pre-selected, not hidden) and at many (wrap/scroll, no pagination). |
| long-text | E4 Availability stylist picker (interactive-control) | ✅ covered | An unusually long stylist display name truncates with ellipsis inside its chip. |
| empty | E5 Weekly hours week-strip (interactive-control) | ✅ covered | A weekday row with zero painted segments shows the muted "Closed" overlay — the explicit zero-hours state, never an ambiguous blank row. |
| loading | E5 Weekly hours week-strip (interactive-control) | ✅ covered | Existing `StylistWorkingHours` for the selected stylist load into a skeleton/muted placeholder strip before staff can drag; drag interaction is disabled until loaded. |
| error | E5 Weekly hours week-strip (interactive-control) | ✅ covered | A hours-fetch failure shares the page-level "Couldn't Load Availability." + Refresh state. |
| populated | E5 Weekly hours week-strip (interactive-control) | ✅ covered | Painted gold segments per weekday, each mapping 1:1 to a `StylistWorkingHours` row. |
| partial | E5 Weekly hours week-strip (interactive-control) | ✅ covered | A day with some hours painted and others closed is the normal per-day state; each weekday row is independent, no cross-day partial ambiguity. |
| overflow | E5 Weekly hours week-strip (interactive-control) | 🧪 backstop | Multiple non-contiguous segments per weekday (D-06's gap-as-break model) must render without visual overlap; verified by a rendering test once the executor finalizes the exact drag/paint mechanic — no explicit UI proof exists yet at spec time. |
| zero-one-many | E5 Weekly hours week-strip (interactive-control) | ✅ covered | 0 segments = Closed overlay; 1 = single painted band; many = multiple non-contiguous bands with the gaps read as breaks. |
| long-text | E5 Weekly hours week-strip (interactive-control) | ⊘ dismissed | The strip carries only fixed weekday abbreviations and time labels — no user-authored text that can run long. |
| empty | E6 Time-off calendar overlay (interactive-control) | ✅ covered | No time off scheduled shows the "No time off scheduled." caption near the calendar; the calendar grid itself always renders fully, never blank. |
| populated | E6 Time-off calendar overlay (interactive-control) | ✅ covered | Each painted range renders as a dashed-muted band across its date cells (see Color section), with the optional reason visible on hover/tap, and a Remove control. |
| overflow | E6 Time-off calendar overlay (interactive-control) | ⚠ unresolved | Many overlapping/adjacent time-off ranges within one month may visually crowd the calendar; no merge/truncation rule is specified upstream. **Planner must treat as assumption** (e.g., adjacent same-reason ranges could visually merge) and resolve if it becomes an issue during implementation. |
| long-text | E6 Time-off calendar overlay (interactive-control) | ✅ covered | A long optional reason label truncates within its band and shows in full on hover/tap. |
| empty | E7 Conflict list (list-collection) | ✅ covered | Zero conflicts means the Save Changes call succeeds immediately with the "Availability saved." flash — the conflict panel never renders when there is nothing to show. |
| loading | E7 Conflict list (list-collection) | ✅ covered | While the server-side conflict check runs, Save Changes shows an in-flight/disabled state; there is no separate list skeleton (the panel only materializes on a blocked response). |
| error | E7 Conflict list (list-collection) | ✅ covered | A save request that fails for a non-conflict reason (network/500) shows a generic "Couldn't save availability. Try again." banner, kept visually distinct from the conflict panel. |
| populated | E7 Conflict list (list-collection) | ✅ covered | 1+ conflicts render the "Can't Save — Conflicting Appointments" panel with one row per conflicting appointment, per the Copywriting Contract's row shape. |
| partial | E7 Conflict list (list-collection) | ⊘ dismissed | Every conflict row always carries its full D-11 field set (client/service/stylist/time); there is no partial-row state. |
| overflow | E7 Conflict list (list-collection) | ✅ covered | The conflict list scrolls internally past ~6 rows (`max-h-* overflow-y-auto`) rather than pushing the rest of the page layout, matching the detail-panel's existing scroll-containment pattern from Phase 3. |
| zero-one-many | E7 Conflict list (list-collection) | ✅ covered | 0 → no panel; 1 → single conflict row; many → internally scrolling list. |
| loading | E8 Dashboard nav (nav) | ⊘ dismissed | Nav is static links with no async fetch — there is no loading state to render. |
| error | E8 Dashboard nav (nav) | ✅ covered | No fetch to fail; the only conditional is role-based visibility — the Services link is fully hidden (not disabled) for Staff-role sessions per D-16, with server-side `[Authorize(Roles = "Owner")]` as the backstop if a Staff user navigates to `/services` directly. |
| overflow | E8 Dashboard nav (nav) | ✅ covered | On narrow viewports the nav-link row wraps (or collapses to a compact menu); all links stay reachable and the bar never overflows horizontally. |
| long-text | E8 Dashboard nav (nav) | ⊘ dismissed | Nav labels are fixed short strings (Schedule/Services/Availability/Add staff) — no user-authored text that can run long. |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized in this app (unchanged from Phase 3) | not applicable |
| third-party | none | not applicable |

No component registry is in play. Every new component this phase introduces (`ServiceForm`,
`ImageUploadField`, `StylistPicker`, `WeekStripEditor`, `TimeOffCalendar`, `ConflictList`,
`DashboardNav`) is hand-written Tailwind + semantic HTML, following the exact pattern already
established by `dashboard/components/ConfirmDialog.tsx` and `dashboard/app/staff/new/page.tsx`.

---

## Component Patterns

Prescriptive shapes for the pieces this phase introduces, so the planner/executor don't have to
invent visual structure mid-implementation.

**`DashboardNav` (new, shared across all dashboard pages):** each existing page currently hand-rolls
its own `<header>` (see `dashboard/app/schedule/page.tsx`'s header block). Rather than let a third and
fourth page duplicate that markup, extend the existing header pattern with a left-of-center nav-link
row between the wordmark and the right-side session/actions cluster: **Schedule · Services ·
Availability**. Services is omitted entirely from the DOM for Staff-role sessions (D-16 "hide," not
disable). The active page's link uses `text-gold-dark font-semibold`; inactive links use `text-ink
hover:text-gold-dark`. This is a refactor of the *existing* header markup into one shared piece, not a
new layout system — same `bg-surface-alt border-b border-border` bar, same right-side "Add staff" /
session name / logout cluster unchanged.

**Services page (`/services`, Owner-only, MGMT-01):** page heading "Services" + primary "Add Service"
button (top-right, gold-dark). Below: a list/table (`bg-surface-alt` rows on `bg-surface` page,
`border-b border-border` row dividers) with columns Name, Category, Duration, Price, Status,
Actions. Clicking a row or its Edit action opens the same `ServiceForm` used for creation, pre-filled
— a single form component in two modes (create/edit), not two separate components, mirroring the
Add Staff page's single-form precedent. Retire uses the existing `ConfirmDialog` component with new
`CONFIRM_COPY.Retired` copy (destructive-red styling, since it removes public bookability even though
it is reversible). Reactivate is a single inline button with no dialog (D-15 discretion: immediate,
reversible, low-stakes).

**`ServiceForm`:** vertical form in a `bg-surface-alt rounded-2xl p-8 border border-border` card,
identical shell to the Add Staff card. Fields in order: Name, Short description, Long description
(textarea), Category, Duration (minutes, number input), Price (currency input), Image
(`ImageUploadField`), then the primary Save button. Uses the same `inputClass`/`Field` wrapper already
defined in `dashboard/app/staff/new/page.tsx` — do not redefine a second input style.

**`ImageUploadField`:** a 160px×160px (4px-multiple) dashed-border box (`border-2 border-dashed
border-border rounded-xl`). Empty: centered "No image yet" caption + Upload Image button. Populated:
thumbnail fills the box, with Replace/Remove controls below it. Uploading: spinner overlay, box
interaction disabled. Accepts `image/jpeg`, `image/png`, `image/webp`, max 5MB (default limits — not
specified upstream, flagged as the executor's confirmed baseline unless product decisions override).

**Availability page (`/availability`, all staff, MGMT-02/MGMT-03):** page heading "Availability" +
`StylistPicker` (a horizontal row of stylist name chips, or a select on narrow viewports — selected
chip gets the gold-dark treatment per accent item 11). Below the picker, two paired sections stacked
vertically: **Weekly Hours** (`WeekStripEditor`) then **Time Off** (`TimeOffCalendar`), each in its own
`bg-surface-alt rounded-2xl p-6 border border-border` card. A single **Save Changes** button (gold-dark,
accent item 9) sits below both sections and submits both hours and time-off edits together in one
API call — not two separate saves — so the conflict check (D-12: runs on both shrinking hours and
adding time off) has one authoritative moment to evaluate the whole new state.

**`WeekStripEditor`:** 7 horizontal rows (Mon–Sun, Label-role weekday abbreviation on the left), each
row a 40px-tall, 24px/hour bar spanning the default 06:00–22:00 window (see Spacing Scale). Staff
click-drag across the bar to paint a working-hours segment (`bg-gold-dark/15 border border-gold-dark`,
snapping to 15-minute increments); dragging again elsewhere on the same row paints an additional
non-contiguous segment (the D-06 gap-as-break mechanic — a gap between two segments is the "lunch
break," with no separate Break entity). Hovering a painted segment reveals a small remove ("x")
control at its edge; clicking removes just that segment. A weekday with no segments shows the "Closed"
muted overlay. Each segment maps 1:1 to a `StylistWorkingHours` row for that `DayOfWeek`.

**`TimeOffCalendar`:** a standard single-month 7-column grid (`ChevronLeftIcon`/`ChevronRightIcon` for
month navigation, Label-role weekday headers, 40px×40px day cells). Staff click a start date then an
end date (or drag across a range) to paint a `StylistTimeOff` block — rendered as the dashed-muted
band described in the Color section, spanning the painted date cells, with an optional reason input
appearing once a range is painted. A Remove control appears on hover/tap of an existing range. Below
the calendar: "No time off scheduled." caption when empty (per Copywriting Contract).

**`ConflictList` / conflict panel:** rendered inline below the Save Changes button (not a modal — D-11
wants an actionable-in-place list, not a dead end that leaves the page). On a hard-blocked save
response, the panel appears with `border-2 border-rose-600 bg-rose-600/5 rounded-xl p-4`, the
Copywriting Contract's heading/body, and one row per conflicting appointment (client, service, stylist,
salon-local time — matching Phase 3's existing salon-local time formatting from the schedule/detail
panel). Rows scroll internally past ~6 (see UI Considerations). Save Changes stays enabled after a
block so staff can retry once conflicts are resolved elsewhere (no forced page reload).

---

## Open Questions

Carried forward for the planner/executor to resolve or flag to the owner — not blocking this spec:

1. **Default business-hours window (06:00–22:00) for the week-strip.** No upstream artifact states the
   salon's actual operating hours; this spec picked a plausible default purely for layout math. Confirm
   against Phase 2 seed data or make the window itself editable if the salon's real hours differ.
2. **Image storage backend** (local `wwwroot`/static folder vs. blob storage) is explicitly Claude's
   Discretion per `04-CONTEXT.md`. This spec assumes local static-folder storage for dev-simplicity
   consistency with the project's "LocalDB + next dev + dotnet run is enough" constraint, but does not
   lock the backend choice — that's a planning/architecture decision, not a visual one.
3. **Exact week-strip drag mechanic** (click-drag paint vs. click-then-click range select) — this spec
   recommends click-drag with 15-minute snapping as the more direct/discoverable interaction, but
   CONTEXT.md leaves the final mechanic to the executor.
4. **Conflict API response shape** (400 vs. 409 ProblemDetails) is Claude's Discretion per CONTEXT.md;
   this spec only prescribes the panel's visual/copy contract once a conflict payload arrives, not the
   HTTP status it arrives on.
5. **Retired-services visibility on the list** — this spec recommends "Retired rows shown after Active
   rows, or behind a toggle" but does not lock which; either satisfies the populated-state resolution
   above.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** VERIFIED (gsd-ui-checker, 2026-07-24) — 6/6 dimensions PASS, no FLAGs
