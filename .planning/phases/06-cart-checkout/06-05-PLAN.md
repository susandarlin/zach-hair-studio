---
phase: 06-cart-checkout
plan: 05
type: execute
wave: 4
depends_on: ["06-03", "06-04"]
files_modified:
  - API/ZachHairStudio.Shared/ZachHairStudio.Shared.csproj
  - API/ZachHairStudio.Shared/Features/Payments/StripePaymentProvider.cs
  - API/ZachHairStudio.Shared/Features/Orders/OrdersService.cs
  - API/ZachHairStudio.Api/Controllers/StripeWebhookController.cs
  - API/ZachHairStudio.Api/Program.cs
  - API/ZachHairStudio.Api.Tests/Features/Payments/StripeWebhookTests.cs
  - API/ZachHairStudio.Api.Tests/Features/Orders/StockConcurrencyTests.cs
  - API/ZachHairStudio.Api.Tests/Features/Orders/MarkFulfilledTests.cs
  - API/ZachHairStudio.Api.Tests/SqlServerWebApplicationFactory.cs
  - API/ZachHairStudio.Api.Tests/CustomWebApplicationFactory.cs
autonomous: false
requirements: [SHOP-02, SHOP-04, SHOP-05]
user_setup:
  - service: stripe
    why: "Test-mode Checkout Session create + webhook signature verification (SHOP-02/05)"
    env_vars:
      - name: Stripe:SecretKey
        source: "Stripe Dashboard → Developers → API keys (sk_test_...)"
      - name: Stripe:WebhookSecret
        source: "stripe listen --forward-to localhost:5236/api/stripe/webhook prints whsec_... (CLI) or Dashboard endpoint signing secret"
    dashboard_config:
      - task: "Install Stripe CLI and run listen for local webhook forwarding (UAT)"
        location: "https://docs.stripe.com/stripe-cli"
must_haves:
  truths:
    - "StripePaymentProvider creates Checkout Session Mode=payment with price_data UnitAmount from order snapshots and returns Session.Url (SHOP-02, D-01, COVERAGE.md INTEGRATE)"
    - "POST /api/stripe/webhook reads raw body (no FromBody model bind), EventUtility.ConstructEvent verifies Stripe-Signature; bad/missing sig → 400 (SHOP-05, D-06)"
    - "Verified checkout.session.completed with payment_status paid calls MarkFulfilledAsync; Pending→Fulfilled once; redeivery no-ops 200 (SHOP-05, Pattern 8)"
    - "No code path on success_url handler or OrdersController checkout action sets Fulfilled (SHOP-05)"
    - "StockConcurrencyTests on SqlServerWebApplicationFactory: two parallel checkouts for last unit → exactly one success and one 409; final Stock==0 (SHOP-04, D-04)"
    - "Program registers StripePaymentProvider for non-Testing; Testing keeps FakePaymentProvider; Stripe options ValidateOnStart for SecretKey/WebhookSecret in Development"
  prohibitions:
    - "Webhook action must not use FromBody for the Stripe event payload"
    - "Do not fulfill orders from success_url or client redirect alone"
    - "Do not run StockConcurrencyTests on EF InMemory"
    - "Do not put Stripe secrets in tracked appsettings.json"
    - "Do not use Stripe.net beta packages — pin 52.2.0"
  artifacts:
    - API/ZachHairStudio.Shared/Features/Payments/StripePaymentProvider.cs
    - API/ZachHairStudio.Api/Controllers/StripeWebhookController.cs
    - API/ZachHairStudio.Api.Tests/Features/Orders/StockConcurrencyTests.cs
    - API/ZachHairStudio.Api.Tests/Features/Payments/StripeWebhookTests.cs
  key_links:
    - "MarkFulfilledAsync is the only Fulfilled writer"
    - "SqlServerWebApplicationFactory must honor ConnectionStrings__DefaultConnection override for Linux/Azure SQL (RESEARCH Environment)"
    - "COVERAGE.md OPT-OUTs remain: Elements, Connect, Subscriptions, async_payment_succeeded"
---

## Phase Goal

**As a** client, **I want to** add recommended products to a cart and check out as a guest with trustworthy, server-verified pricing and stock, **so that** I can complete a real purchase without creating an account.

> MVP slice 5 — real Stripe + webhook fulfillment + concurrency proof. After this plan, payment sessions are real Stripe Checkout, fulfillment is signature-verified only, and last-unit concurrency is proven on SQL Server.

<objective>
Add Stripe.net 52.2.0, StripePaymentProvider (D-01), StripeWebhookController raw-body ConstructEvent path that calls Plan 03 MarkFulfilledAsync (SHOP-05 — wire caller only; do not reimplement the flip), StockConcurrencyTests with SqlServer factory connection override (SHOP-04), and blocking human Stripe CLI verify.

Purpose: Close SHOP-02/04/05 production trust boundaries.
Output: Real Stripe session create + webhook fulfill + concurrency proof + UAT checkpoint.
</objective>

<execution_context>
@/workspaces/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cart-checkout/06-03-PLAN.md
@.planning/phases/06-cart-checkout/06-04-PLAN.md
@.planning/phases/06-cart-checkout/COVERAGE.md
@.planning/phases/06-cart-checkout/06-RESEARCH.md
@.planning/phases/06-cart-checkout/06-PATTERNS.md
@.planning/phases/06-cart-checkout/06-CONTEXT.md

@API/ZachHairStudio.Shared/Features/Payments/IPaymentProvider.cs
@API/ZachHairStudio.Shared/Features/Orders/OrdersService.cs
@API/ZachHairStudio.Api/Program.cs
@API/ZachHairStudio.Api.Tests/Features/Appointments/ConcurrencyTests.cs
@API/ZachHairStudio.Api.Tests/SqlServerWebApplicationFactory.cs
@API/ZachHairStudio.Shared/Features/Identity/JwtOptions.cs
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan:
- Package reference: Stripe.net 52.2.0 on Shared.csproj
- Class: `StripePaymentProvider` : IPaymentProvider
- Class: `StripeWebhookController` route `api/stripe/webhook`
- Test classes: `StripeWebhookTests`, `StockConcurrencyTests`, `MarkFulfilledTests` (if separate)
- SqlServerWebApplicationFactory connection override behavior
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stripe.net + StripePaymentProvider + webhook RED tests</name>
  <files>API/ZachHairStudio.Shared/ZachHairStudio.Shared.csproj, API/ZachHairStudio.Shared/Features/Payments/StripePaymentProvider.cs, API/ZachHairStudio.Api.Tests/Features/Payments/StripeWebhookTests.cs, API/ZachHairStudio.Api.Tests/Features/Orders/MarkFulfilledTests.cs</files>
  <read_first>
    - .planning/phases/06-cart-checkout/COVERAGE.md
    - .planning/phases/06-cart-checkout/06-RESEARCH.md (Pattern 3 webhook; Package Legitimacy; Standard Stack 52.2.0)
    - API/ZachHairStudio.Shared/Features/Payments/IPaymentProvider.cs
    - API/ZachHairStudio.Shared/Features/Appointments/ResendEmailService.cs (options + HttpClient external call style)
    - API/ZachHairStudio.Api/Program.cs (Jwt ValidateOnStart precedent)
  </read_first>
  <behavior>
    - Webhook missing/invalid Stripe-Signature → 400
    - Valid ConstructEvent checkout.session.completed + paid → order Fulfilled
    - Second identical delivery → still 200 and single Fulfilled (idempotent)
    - MarkFulfilledAsync on already Fulfilled is no-op success
  </behavior>
  <action>
    `dotnet add API/ZachHairStudio.Shared package Stripe.net --version 52.2.0` (D-01; legitimacy OK in RESEARCH — not ASSUMED/SUS).

    Implement StripePaymentProvider: inject IOptions StripeOptions + StripeClient (or set StripeConfiguration.ApiKey from SecretKey); CreateCheckoutSessionAsync builds SessionCreateOptions Mode=payment, SuccessUrl with `{CHECKOUT_SESSION_ID}`, CancelUrl, ClientReferenceId=orderId, Metadata order_id, CustomerEmail, LineItems with PriceData Currency=usd UnitAmount=(long)(unitPrice*100m), ProductData.Name — per COVERAGE INTEGRATE rows. Optional Idempotency-Key order-{orderId}.

    Write RED StripeWebhookTests + MarkFulfilledTests describing signature reject, paid→Fulfilled, idempotent redeivery. Use synthetic signed payloads with a test webhook secret (Stripe.net test helpers or known signed fixture) — CLI not required for automated gate.
  </action>
  <verify>
    <automated>grep -n "PackageReference Include=\"Stripe.net\"" API/ZachHairStudio.Shared/ZachHairStudio.Shared.csproj &amp;&amp; test -f API/ZachHairStudio.Shared/Features/Payments/StripePaymentProvider.cs &amp;&amp; test -f API/ZachHairStudio.Api.Tests/Features/Payments/StripeWebhookTests.cs &amp;&amp; echo READY</automated>
  </verify>
  <acceptance_criteria>
    - Shared.csproj pins Stripe.net Version 52.2.0 exactly
    - StripePaymentProvider implements IPaymentProvider
    - StripeWebhookTests and MarkFulfilledTests files exist
  </acceptance_criteria>
  <done>Stripe package + provider + failing webhook/fulfill tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — StripeWebhookController + DI swap + StockConcurrencyTests</name>
  <files>API/ZachHairStudio.Api/Controllers/StripeWebhookController.cs, API/ZachHairStudio.Shared/Features/Orders/OrdersService.cs, API/ZachHairStudio.Api/Program.cs, API/ZachHairStudio.Api.Tests/Features/Orders/StockConcurrencyTests.cs, API/ZachHairStudio.Api.Tests/SqlServerWebApplicationFactory.cs, API/ZachHairStudio.Api.Tests/CustomWebApplicationFactory.cs, API/ZachHairStudio.Api.Tests/Features/Payments/StripeWebhookTests.cs</files>
  <read_first>
    - API/ZachHairStudio.Api.Tests/Features/Appointments/ConcurrencyTests.cs (two-parallel-POST exactly-one-201-one-409 shape)
    - API/ZachHairStudio.Api.Tests/SqlServerWebApplicationFactory.cs (hardcoded LocalDB — add env override)
    - .planning/phases/06-cart-checkout/06-RESEARCH.md (Pattern 3, 8; Pitfalls 1/3/4; Environment Availability)
    - API/ZachHairStudio.Shared/Features/Orders/OrdersService.cs (MarkFulfilledAsync from Plan 03)
  </read_first>
  <action>
    Implement StripeWebhookController `[Route("api/stripe/webhook")]` `[AllowAnonymous]`: read raw Request.Body as string; EventUtility.ConstructEvent(json, Stripe-Signature header, WebhookSecret); on StripeException return BadRequest; on EventTypes.CheckoutSessionCompleted cast Session; if PaymentStatus == paid call the existing Plan 03 OrdersService.MarkFulfilledAsync(ClientReferenceId, Session.Id); return Ok. Bind the payload only via raw stream read — do not model-bind the Stripe event JSON (Pitfall 1 / SHOP-05).
    <!-- planner-discipline-allow: FromBody -->

    Do not reimplement MarkFulfilledAsync here — Plan 03 already ships the thin idempotent Pending→Fulfilled flip. This plan only wires the webhook caller. Touch OrdersService.cs only if a tiny signature/arg plumbing fix is required for Session.Id (persist StripeSessionId if the Plan 03 method already accepts it); do not rewrite fulfillment logic.

    Program.cs: ValidateOnStart StripeOptions requiring SecretKey/WebhookSecret in Development (mirror Jwt); register StripeClient singleton; `AddScoped&lt;IPaymentProvider, StripePaymentProvider&gt;()` for non-Testing; Testing/CustomWebApplicationFactory keeps FakePaymentProvider via ConfigureTestServices RemoveAll/Add. SuccessUrl/CancelUrl point at landing-page checkout success/cancel routes.

    SqlServerWebApplicationFactory: if env `ConnectionStrings__DefaultConnection` or `TEST_SQLSERVER_CONNECTION` set, use it; else LocalDB default — documents Linux/Azure SQL path (RESEARCH A4).

    StockConcurrencyTests : IClassFixture&lt;SqlServerWebApplicationFactory&gt; — seed product Stock=1; two parallel POST /api/orders/checkout qty=1 with FakePaymentProvider; assert status set is one success (2xx) and one 409; reload product Stock==0 (SHOP-04, D-04). Mirror ConcurrencyTests structure.
  </action>
  <verify>
    <automated>cd API &amp;&amp; dotnet test ZachHairStudio.Api.Tests/ZachHairStudio.Api.Tests.csproj --filter "FullyQualifiedName~StripeWebhook|FullyQualifiedName~MarkFulfilled|FullyQualifiedName~StockConcurrency" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - StripeWebhookController reads Request.Body via StreamReader and calls ConstructEvent
    - Controller file has no FromBody parameter on the webhook action
    - StockConcurrencyTests uses SqlServerWebApplicationFactory
    - Webhook + MarkFulfilled + StockConcurrency tests pass when SQL is reachable
    - Program registers StripePaymentProvider for non-test environments
  </acceptance_criteria>
  <done>SHOP-05 webhook fulfill + SHOP-04 concurrency proof automated; real Stripe provider registered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — Stripe CLI checkout + webhook fulfillment</name>
  <files>none (verification-only — no files modified)</files>
  <read_first>
    - .planning/phases/06-cart-checkout/06-VALIDATION.md
    - .planning/phases/06-cart-checkout/COVERAGE.md
    - .claude/skills/dev/SKILL.md
  </read_first>
  <what-built>
    StripePaymentProvider Checkout Session create, raw-body webhook at POST /api/stripe/webhook, MarkFulfilledAsync, guest UI checkout redirect, success page display-only.
  </what-built>
  <action>
    Run the stack via the dev skill and complete the how-to-verify Stripe CLI UAT steps below. This checkpoint is not auto-approvable — it confirms real hosted Checkout + signature-verified fulfillment (SHOP-02/05) that synthetic webhook unit tests do not fully replace.
  </action>
  <how-to-verify>
    1. Set user-secrets: Stripe:SecretKey (sk_test_...), Stripe:WebhookSecret from CLI listen, plus existing RESEND_API_KEY / Jwt:SigningKey.
    2. Run API + landing-page (dev skill). In another terminal: stripe listen --forward-to localhost:5236/api/stripe/webhook.
    3. Add a product to cart, checkout with Email, pay with test card 4242 4242 4242 4242.
    4. Confirm Stripe Dashboard/CLI shows checkout.session.completed; API order Status becomes Fulfilled (query DB or GET order) without relying on success page alone.
    5. Confirm success page shows Order Received and does not itself change status if webhook is stopped (optional negative check).
    6. Confirm Cancel path leaves cart intact and order not Fulfilled.
  </how-to-verify>
  <verify>
    <human-check>All six steps pass: secrets set, CLI listen forwards, test-card Checkout completes, order becomes Fulfilled only via webhook, success page is display-only, cancel leaves cart saved and order not Fulfilled.</human-check>
  </verify>
  <done>Human approved SHOP-02/05 Stripe CLI UAT (or defect list filed).</done>
  <resume-signal>Type "approved" or list issues (SHOP-02/05 UAT)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe → Webhook API | Untrusted HTTP; authenticity via signature only |
| OrdersService → Stock/Order rows | Authoritative fulfillment + inventory |
| Browser success_url | Non-authoritative display |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-13 | Spoofing | StripeWebhookController | high | mitigate | EventUtility.ConstructEvent + WebhookSecret; 400 on failure (SHOP-05) |
| T-06-14 | Tampering | MarkFulfilledAsync | high | mitigate | Require payment_status paid; idempotent Pending→Fulfilled; filtered unique StripeSessionId |
| T-06-15 | Spoofing | success_url | high | mitigate | No fulfill from redirect; webhook-only writer (SHOP-05) |
| T-06-16 | Tampering | concurrent checkout | high | mitigate | ExecuteUpdateAsync WHERE Stock>=qty + StockConcurrencyTests (SHOP-04) |
| T-06-17 | Information Disclosure | Stripe secrets | high | mitigate | user-secrets/env + ValidateOnStart; never tracked appsettings; gitleaks |
| T-06-SC | Tampering | Stripe.net NuGet | low | accept | Official SDK 52.2.0 OK per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
StripeWebhook/MarkFulfilled/StockConcurrency tests pass on reachable SQL; human Stripe CLI UAT approved.
</verification>

<success_criteria>
Real Stripe Checkout + signature-verified fulfillment only; last-unit concurrency proven; secrets not in git.
</success_criteria>

<output>
Create `.planning/phases/06-cart-checkout/06-05-SUMMARY.md` when done
</output>
