---
phase: 06-cart-checkout
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - API/ZachHairStudio.Shared/Features/Products/ProductsService.cs
  - API/ZachHairStudio.Api/Controllers/ProductsController.cs
  - API/ZachHairStudio.Api.Tests/Features/Products/RecommendedForCheckoutTests.cs
  - landing-page/lib/cart.ts
  - landing-page/lib/products.ts
  - landing-page/app/checkout/page.tsx
  - landing-page/app/checkout/success/page.tsx
  - landing-page/app/checkout/cancel/page.tsx
  - landing-page/components/CartPageClient.tsx
  - landing-page/components/CheckoutForm.tsx
autonomous: true
requirements: [SHOP-02, SHOP-05, SHOP-07]
must_haves:
  truths:
    - " /checkout guest form requires Zod-valid Email; Continue to Payment disabled until valid; submit POSTs checkout and sets window.location to checkoutUrl; in-flight label Redirecting to payment… (SHOP-02, SHOP-06 UI, UI-SPEC)"
    - "Checkout create failure shows Couldn't Start Checkout rose banner with Try Again; cart contents remain (UI Considerations error — covered)"
    - " /checkout/success displays Order Received confirmation UI and never calls an API that sets Fulfilled — display/poll only (SHOP-05, D-06)"
    - "Missing/invalid order reference on success calls notFound() (UI Considerations not-found — covered)"
    - " /checkout/cancel shows Checkout Cancelled + Return to Cart (UI-SPEC)"
    - "Cart page Complete Your Routine chips load from GetRecommendedForCheckout (ServiceRecommendedProduct join), exclude in-cart product ids, Take(4); section omitted when empty (SHOP-07, D-07, UI Considerations empty chips — covered)"
    - "Chip add uses cart upsert; Added state uses border-gold CheckIcon; out-of-stock chips disabled (UI-SPEC)"
  prohibitions:
    - "success page must not POST/PUT order status to Fulfilled"
    - "Do not invent a general-purpose recommendation engine — reuse ServiceRecommendedProduct only"
    - "Do not collect payment card fields on /checkout — redirect to hosted Checkout URL only"
  artifacts:
    - landing-page/app/checkout/page.tsx
    - landing-page/app/checkout/success/page.tsx
    - landing-page/app/checkout/cancel/page.tsx
    - ProductsService.GetRecommendedForCheckoutAsync
  key_links:
    - "createCheckout MUST send required header X-Cart-Session-Id (from getCartSessionId); body sessionKey is optional mirror only — locked Plan 03 contract"
    - "Success page may GET order by id for display; Fulfilled flip is Plan 05 webhook only"
---

## Phase Goal

**As a** client, **I want to** add recommended products to a cart and check out as a guest with trustworthy, server-verified pricing and stock, **so that** I can complete a real purchase without creating an account.

> MVP slice 4 — checkout UI + SHOP-07 chips. After this plan a guest can proceed from cart → checkout form → payment redirect URL, see success/cancel pages that never fulfill, and add stylist-recommended chips on the cart.

<objective>
Add GetRecommendedForCheckoutAsync (D-07/SHOP-07), cart suggestion chips, /checkout (Email+Name → create checkout → redirect), and success/cancel pages that honor SHOP-05 (display only).

Purpose: Complete the human checkout click-path against FakePaymentProvider URL until Plan 05 swaps real Stripe.
Output: Recommended-for-checkout API + cart chips + checkout/success/cancel routes.
</objective>

<execution_context>
@/workspaces/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cart-checkout/06-02-PLAN.md
@.planning/phases/06-cart-checkout/06-03-PLAN.md
@.planning/phases/06-cart-checkout/06-UI-SPEC.md
@.planning/phases/06-cart-checkout/06-RESEARCH.md
@.planning/phases/06-cart-checkout/06-PATTERNS.md

@API/ZachHairStudio.Shared/Features/Services/ServicesService.cs
@API/ZachHairStudio.Shared/Features/Products/ProductsService.cs
@API/ZachHairStudio.Api/Controllers/ProductsController.cs
@landing-page/components/CartPageClient.tsx
@landing-page/lib/cart.ts
@landing-page/components/AppointmentBookingForm.tsx
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan:
- Method: `ProductsService.GetRecommendedForCheckoutAsync(IReadOnlyCollection&lt;int&gt; cartProductIds)`
- Action: `ProductsController` GET recommended-for-checkout (query: cart product ids)
- Test class: `RecommendedForCheckoutTests`
- Routes/components: checkout page/form, success page, cancel page
- Cart chips UI section Complete Your Routine in CartPageClient
- lib/cart or lib/products helpers: fetchRecommendedForCheckout, createCheckout
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GetRecommendedForCheckoutAsync + tests (SHOP-07)</name>
  <files>API/ZachHairStudio.Shared/Features/Products/ProductsService.cs, API/ZachHairStudio.Api/Controllers/ProductsController.cs, API/ZachHairStudio.Api.Tests/Features/Products/RecommendedForCheckoutTests.cs</files>
  <read_first>
    - API/ZachHairStudio.Shared/Features/Services/ServicesService.cs (ServiceRecommendedProduct join in GetBySlugAsync)
    - API/ZachHairStudio.Shared/Features/Products/ProductsService.cs
    - API/ZachHairStudio.Shared/Features/Products/ServiceRecommendedProduct.cs
    - .planning/phases/06-cart-checkout/06-RESEARCH.md (Pattern 5; Open Question 3 — join first, omit when empty)
    - .planning/phases/06-cart-checkout/06-CONTEXT.md (D-07)
  </read_first>
  <behavior>
    - Given cart product ids linked via ServiceRecommendedProduct to other active products, returns those others excluding in-cart ids, max 4
    - When join yields nothing, returns empty list (not error)
    - Inactive products excluded
  </behavior>
  <action>
    Add `GetRecommendedForCheckoutAsync(IReadOnlyCollection&lt;int&gt; cartProductIds)` on ProductsService (D-07): query ServiceRecommendedProduct rows whose ProductId is in cart OR whose ServiceId is shared with services that recommend cart products — RESEARCH intent: reuse join to suggest add-ons related to cart picks. Concrete algorithm: find ServiceIds linked to any cartProductId; select other active Products linked to those ServiceIds; exclude cartProductIds; distinct; OrderBy Name; Take(4). Empty cart → empty list.

    Expose GET `/api/products/recommended-for-checkout?productIds=1&amp;productIds=2` (or comma-separated — pick one and document) anonymous on ProductsController.

    Tests in RecommendedForCheckoutTests cover exclude-in-cart, Take 4, empty omit case.
  </action>
  <verify>
    <automated>cd API &amp;&amp; dotnet test ZachHairStudio.Api.Tests/ZachHairStudio.Api.Tests.csproj --filter "FullyQualifiedName~RecommendedForCheckout" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - GetRecommendedForCheckoutAsync exists on ProductsService
    - RecommendedForCheckoutTests pass
    - Implementation joins ServiceRecommendedProduct (grep ServiceRecommendedProduct in method body)
  </acceptance_criteria>
  <done>SHOP-07 recommendation API green.</done>
</task>

<task type="auto">
  <name>Task 2: Cart suggestion chips + createCheckout client helper</name>
  <files>landing-page/components/CartPageClient.tsx, landing-page/lib/cart.ts, landing-page/lib/products.ts</files>
  <read_first>
    - landing-page/components/CartPageClient.tsx (from Plan 02)
    - landing-page/lib/cart.ts
    - .planning/phases/06-cart-checkout/06-UI-SPEC.md (Suggestion chips section + copy)
  </read_first>
  <action>
    Add fetchRecommendedForCheckout(productIds) and createCheckout({email, name?, items}) to lib (Zod-validated). createCheckout POSTs to /api/orders/checkout with required header `X-Cart-Session-Id` from getCartSessionId() (Plan 03 locked contract); optional body sessionKey may mirror the same id — never omit the header; returns checkoutUrl + orderId.

    On cart page below line items: if recommendations non-empty, render Complete Your Routine heading/subtitle and chips (Phase 2 stylist-chip anatomy, gold price, + add; Added state border-gold + CheckIcon; out-of-stock disabled). Omit section when empty (UI Considerations). Chip add calls upsertCartItem and refreshes cart + recommendations.
  </action>
  <verify>
    <automated>grep -q "Complete Your Routine" landing-page/components/CartPageClient.tsx &amp;&amp; grep -qE "createCheckout" landing-page/lib/cart.ts landing-page/lib/products.ts &amp;&amp; grep -qE "recommended-for-checkout" landing-page/lib/cart.ts landing-page/lib/products.ts &amp;&amp; grep -q "X-Cart-Session-Id" landing-page/lib/cart.ts</automated>
  </verify>
  <acceptance_criteria>
    - Complete Your Routine string exists in cart UI code
    - createCheckout helper exists, posts to orders/checkout, and sets header X-Cart-Session-Id
    - Recommendations fetch path references recommended-for-checkout
  </acceptance_criteria>
  <done>SHOP-07 chips on cart; checkout client helper ready.</done>
</task>

<task type="auto">
  <name>Task 3: /checkout + success + cancel pages (SHOP-02 UI, SHOP-05 display-only)</name>
  <files>landing-page/app/checkout/page.tsx, landing-page/app/checkout/success/page.tsx, landing-page/app/checkout/cancel/page.tsx, landing-page/components/CheckoutForm.tsx</files>
  <read_first>
    - landing-page/components/AppointmentBookingForm.tsx (inputClass, rose alert, Confirming… label swap, success ring)
    - .planning/phases/06-cart-checkout/06-UI-SPEC.md (Checkout page, Order confirmation, Cancel, Copywriting, SHOP-05 caption)
    - API OrdersController GET shape if Plan 03 added order-by-id — if missing, add minimal GET /api/orders/{id} in this task on OrdersController (anonymous by id for guest success display; accept MVP IDOR per CONTEXT Phase 7 defer)
  </read_first>
  <action>
    Implement `/checkout` client page: compact summary from fetchCart; Email required + Name optional (Zod); helper We'll send your order confirmation here.; Continue to Payment disabled until email valid; on submit createCheckout then window.location.href=checkoutUrl; button label Redirecting to payment… while in flight; error Couldn't Start Checkout + Try Again.

    `/checkout/success`: read order id and/or session_id from query; load order for display; gold CheckIcon ring; Order Received copy; detail rows; caption Payment confirmed. You'll receive a confirmation email when your order is ready. (SHOP-05-honest — display/poll GET only; never POST/PUT order status or invoke any fulfillment writer). Invalid/missing → notFound().

    `/checkout/cancel`: Checkout Cancelled copy + Return to Cart gold button.

    If OrdersController lacks GET by id, add thin OrdersService.GetByIdAsync + GET action returning snapshot for success page only (no status mutation).
  </action>
  <verify>
    <automated>test -f landing-page/app/checkout/page.tsx &amp;&amp; test -f landing-page/app/checkout/success/page.tsx &amp;&amp; test -f landing-page/app/checkout/cancel/page.tsx &amp;&amp; grep -q "Order Received" landing-page/app/checkout/success/page.tsx &amp;&amp; grep -q "Checkout Cancelled" landing-page/app/checkout/cancel/page.tsx &amp;&amp; grep -q "Redirecting to payment" landing-page/app/checkout/page.tsx &amp;&amp; ! grep -qRE "MarkFulfilled|Status\s*=\s*Fulfilled|OrderStatus\.Fulfilled|fulfillOrder|setFulfilled" landing-page/app/checkout/success</automated>
  </verify>
  <acceptance_criteria>
    - checkout/success/cancel page files exist
    - Success page contains Order Received
    - Negated grep -qE over landing-page/app/checkout/success fails the gate if MarkFulfilled or Status=Fulfilled mutation references appear
    - Checkout page contains Redirecting to payment
    - Cancel page contains Return to Cart or Checkout Cancelled
  </acceptance_criteria>
  <done>Guest can check out through UI to payment URL; success/cancel never fulfill; SHOP-07 chips live on cart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → checkout POST | Untrusted email/items |
| Browser → success page | Must not be a fulfillment trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Spoofing | /checkout/success | high | mitigate | Page is display/poll only; no fulfill API calls (SHOP-05, D-06) |
| T-06-11 | Information Disclosure | GET /api/orders/{id} | medium | accept | Guest order lookup by id is nonce-like MVP; IDOR hardening Phase 7 per CONTEXT Deferred Ideas |
| T-06-12 | Tampering | Recommendation query | low | accept | Read-only curated join; cannot alter prices |
| T-06-SC | Tampering | npm | low | accept | No new packages |
</threat_model>

<verification>
RecommendedForCheckout tests pass; checkout routes exist; success page has no fulfill mutation.
</verification>

<success_criteria>
Guest completes cart → checkout form → redirect URL; sees SHOP-05-honest success/cancel; can add recommended chips on cart (SHOP-07).
</success_criteria>

<output>
Create `.planning/phases/06-cart-checkout/06-04-SUMMARY.md` when done
</output>
