---
phase: 06-cart-checkout
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - landing-page/lib/cartSession.ts
  - landing-page/lib/cart.ts
  - landing-page/components/icons.tsx
  - landing-page/components/Navbar.tsx
  - landing-page/app/products/[slug]/page.tsx
  - landing-page/components/AddToCartPanel.tsx
  - landing-page/app/cart/page.tsx
  - landing-page/components/CartPageClient.tsx
  - landing-page/lib/data.ts
autonomous: true
requirements: [SHOP-01]
must_haves:
  truths:
    - "Product detail aside exposes Add to Cart with quantity stepper; success updates navbar cart count (SHOP-01, UI-SPEC)"
    - " /cart client page shows line items with server UnitPrice/LineTotal, quantity steppers, Remove link, Order Summary Subtotal/Total, and Proceed to Checkout CTA (SHOP-01, UI-SPEC copy)"
    - "Empty cart renders Your Cart Is Empty panel with Browse Products CTA (UI Considerations empty — covered)"
    - "Cart load failure shows Couldn't Load Your Cart rose banner with Try Again (UI Considerations error — covered)"
    - "Navbar Cart link shows gold count badge when count >= 1 and hides badge at 0 (UI Considerations nav — covered)"
    - "Out-of-stock product detail disables Add to Cart (opacity-40 cursor-not-allowed) (UI Considerations error — covered)"
    - { statement: "Cart in-flight fetch shows charcoal pulse skeleton panels (first loading boundary in landing-page) — UI Considerations loading backstop", verification: backstop }
    - { statement: "Long product names in cart rows use line-clamp-2 so row flex does not overflow — UI Considerations overflow backstop", verification: backstop }
  prohibitions:
    - "Do not add-to-cart on catalog list cards (UI-SPEC Open Question 3 — detail-only)"
    - "Do not introduce shadcn or new icon libraries — extend icons.tsx only"
    - "Do not use credentialed cookies for cart session — localStorage + X-Cart-Session-Id only"
  artifacts:
    - landing-page/lib/cart.ts
    - landing-page/lib/cartSession.ts
    - landing-page/app/cart/page.tsx
    - landing-page/components/AddToCartPanel.tsx
  key_links:
    - "Every cart fetch must send X-Cart-Session-Id from cartSession.ts"
    - "Proceed to Checkout links to /checkout (Plan 04 implements payment page; link may 404 until then — acceptable interim or stub route)"
---

## Phase Goal

**As a** client, **I want to** add recommended products to a cart and check out as a guest with trustworthy, server-verified pricing and stock, **so that** I can complete a real purchase without creating an account.

> MVP slice 2 — guest cart UI. After this plan a real browser user can add from product detail, see navbar count, and review/update the cart on /cart (SHOP-01 complete for humans).

<objective>
Wire landing-page cart session + Zod fetch layer to Plan 01 APIs; ship Add to Cart on product detail, Navbar cart badge, and /cart review page per 06-UI-SPEC (D-02 session header discretion, SHOP-01).

Purpose: Close the human-visible SHOP-01 loop without waiting for Stripe.
Output: cartSession/cart libs, AddToCartPanel, /cart page, Navbar Cart entry, icons.
</objective>

<execution_context>
@/workspaces/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cart-checkout/06-01-PLAN.md
@.planning/phases/06-cart-checkout/06-UI-SPEC.md
@.planning/phases/06-cart-checkout/06-PATTERNS.md
@.planning/phases/06-cart-checkout/06-CONTEXT.md

@landing-page/lib/products.ts
@landing-page/lib/appointments.ts
@landing-page/app/products/[slug]/page.tsx
@landing-page/components/Navbar.tsx
@landing-page/components/icons.tsx
@landing-page/components/SectionHeading.tsx
@landing-page/lib/data.ts
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan:
- Module: `getOrCreateCartSessionId` / related helpers — `landing-page/lib/cartSession.ts`
- Module: cart Zod schemas + `fetchCart` / `upsertCartItem` / `removeCartItem` / `CartApiError` — `landing-page/lib/cart.ts`
- Components: `CartIcon`, `PlusIcon`, `MinusIcon` — `landing-page/components/icons.tsx`
- Component: `AddToCartPanel` — `landing-page/components/AddToCartPanel.tsx`
- Component: `CartPageClient` — `landing-page/components/CartPageClient.tsx` (or colocated)
- Route: `landing-page/app/cart/page.tsx`
- Navbar Cart link + count badge updates in `Navbar.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: cartSession + lib/cart Zod fetch layer</name>
  <files>landing-page/lib/cartSession.ts, landing-page/lib/cart.ts</files>
  <read_first>
    - landing-page/lib/appointments.ts (AppointmentApiError, throw-on-error mutating fetch, cache no-store)
    - landing-page/lib/products.ts (Zod ProductSchema, API_BASE_URL)
    - .planning/phases/06-cart-checkout/06-RESEARCH.md (Pattern 4 header session; Pattern 5)
    - .planning/phases/06-cart-checkout/06-PATTERNS.md (lib/cart.ts assignment)
  </read_first>
  <action>
    Create `cartSession.ts`: generate UUID v4 if missing; persist under a single localStorage key (e.g. `zhs-cart-session`); export `getCartSessionId(): string`. Never use cookies (CORS AllowAnyOrigin — RESEARCH Pattern 4 / Claude Discretion).

    Create `cart.ts` mirroring appointments throw-on-error style: Zod schemas for CartResponse matching Plan 01 DTO field names (camelCase JSON); every request sets header `X-Cart-Session-Id` from getCartSessionId(); `cache: "no-store"` on reads. Export fetchCart, upsertCartItem({productId, quantity}), removeCartItem(productId), updateQuantity helper. Export CartApiError with status + isConflict/isValidation/isNetwork getters. Do not swallow errors to empty arrays (unlike fetchProducts list). Write DTOs must not include price/total fields (D-05).
  </action>
  <verify>
    <automated>test -f landing-page/lib/cart.ts &amp;&amp; test -f landing-page/lib/cartSession.ts &amp;&amp; grep -q "X-Cart-Session-Id" landing-page/lib/cart.ts &amp;&amp; grep -q "productId" landing-page/lib/cart.ts &amp;&amp; grep -q "quantity" landing-page/lib/cart.ts &amp;&amp; ! grep -qiE '\b(price|total)\b' landing-page/lib/cart.ts</automated>
  </verify>
  <acceptance_criteria>
    - cartSession.ts reads/writes localStorage for a session UUID
    - cart.ts attaches X-Cart-Session-Id on cart API calls
    - Upsert request body type/fields are productId and quantity only
    - CartApiError exposes HTTP status for 409/400 handling
  </acceptance_criteria>
  <done>Frontend cart API client + session helper ready for UI wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Add to Cart on product detail + Navbar cart badge</name>
  <files>landing-page/components/icons.tsx, landing-page/components/AddToCartPanel.tsx, landing-page/app/products/[slug]/page.tsx, landing-page/components/Navbar.tsx, landing-page/lib/data.ts</files>
  <read_first>
    - landing-page/app/products/[slug]/page.tsx (aside Price/Stock dl, sticky card classes)
    - landing-page/components/Navbar.tsx (Book Now pill placement — Cart goes left of it)
    - landing-page/components/icons.tsx (CheckIcon SVGProps pattern)
    - .planning/phases/06-cart-checkout/06-UI-SPEC.md (Navbar cart entry; product-detail Add to Cart; Copywriting)
  </read_first>
  <action>
    Add CartIcon, PlusIcon, MinusIcon to icons.tsx matching CheckIcon stroke pattern (UI-SPEC).

    Create client `AddToCartPanel` for product detail aside: quantity stepper (min 1, max stock, 44px buttons, aria-labels Decrease/Increase quantity); full-width gold Add to Cart button; stock===0 disables button; success swaps label to Added with CheckIcon ~2s; failure shows inline rose Couldn't Add to Cart alert (UI-SPEC). Wire into products/[slug] aside below Price/Stock — convert aside region to client child as needed while keeping page data fetch as Server Component.

    Update Navbar: Cart link + CartIcon left of Book Now (desktop) and mobile menu row above Book Now; gold count badge when count>=1 (client-rendered from cart fetch or light cart-count helper); no badge at 0. Dispatch/listen a simple custom event or shared small store so AddToCartPanel increments refresh Navbar count without full reload.

    Ensure nav data in lib/data.ts includes Cart path if nav links are data-driven — keep Products/Book patterns.
  </action>
  <verify>
    <automated>test -f landing-page/components/AddToCartPanel.tsx &amp;&amp; grep -q "CartIcon" landing-page/components/Navbar.tsx &amp;&amp; grep -q "/cart" landing-page/components/Navbar.tsx &amp;&amp; grep -q "Add to Cart" landing-page/components/AddToCartPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AddToCartPanel.tsx exists and calls upsertCartItem
    - Navbar contains a link to /cart and CartIcon
    - products/[slug]/page.tsx renders AddToCartPanel (or equivalent) in the aside
    - icons.tsx exports CartIcon PlusIcon MinusIcon
  </acceptance_criteria>
  <done>User can add products from detail and see live navbar cart count.</done>
</task>

<task type="auto">
  <name>Task 3: /cart review page (line items, summary, empty/error/loading)</name>
  <files>landing-page/app/cart/page.tsx, landing-page/components/CartPageClient.tsx</files>
  <read_first>
    - landing-page/app/products/[slug]/page.tsx (lg:grid-cols-[1fr_320px] gap-8 layout)
    - landing-page/components/AppointmentBookingForm.tsx (rose alert classes, Confirming label swap pattern)
    - landing-page/components/SectionHeading.tsx
    - .planning/phases/06-cart-checkout/06-UI-SPEC.md (Cart page, Quantity stepper, Copywriting Contract, UI Considerations)
  </read_first>
  <action>
    Implement `/cart` as client-driven page (pt-32 main) with SectionHeading eyebrow Your Selection, title Your Cart (gold on Cart), subtitle Review your picks before checking out.

    Two-column layout per UI-SPEC: line-item rows (thumbnail or Z-monogram, name with line-clamp-2, gold unit price via same Intl.NumberFormat priceFormatter as catalog, quantity stepper, gold line total, quiet gray Remove link — no confirm dialog). Sticky Order Summary: Subtotal, Total, Proceed to Checkout (link to /checkout), Continue Shopping to /products.

    States: empty → Your Cart Is Empty + Browse Products; load error → Couldn't Load Your Cart + Try Again; loading → charcoal pulse skeleton panels (first loading UX in app — UI Considerations backstop). Quantity updates call cart API; 409/validation reverts to last good qty and shows rose banner.

    Suggestion chips (SHOP-07 / D-07) are Plan 04 — omit Complete Your Routine section here or leave a clearly gated TODO only if it would violate scope-reduction; prefer omit until Plan 04 endpoint exists (not a stub chip UI with fake data).
  </action>
  <verify>
    <automated>test -f landing-page/app/cart/page.tsx &amp;&amp; test -f landing-page/components/CartPageClient.tsx &amp;&amp; grep -q "Your Cart Is Empty" landing-page/components/CartPageClient.tsx &amp;&amp; grep -q "Order Summary" landing-page/components/CartPageClient.tsx &amp;&amp; grep -q "Proceed to Checkout" landing-page/components/CartPageClient.tsx &amp;&amp; grep -q "line-clamp-2" landing-page/components/CartPageClient.tsx &amp;&amp; grep -q "Couldn't Load Your Cart" landing-page/components/CartPageClient.tsx</automated>
  </verify>
  <acceptance_criteria>
    - /cart page file exists under landing-page/app/cart
    - Empty-state copy Your Cart Is Empty is present
    - Proceed to Checkout affordance present
    - line-clamp-2 applied to cart line product names
    - No Complete Your Routine section yet unless Plan 04 already shipped (this plan omits chips)
  </acceptance_criteria>
  <done>SHOP-01 human path complete: add → navbar count → cart review with empty/error/loading states per UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage → API | Session nonce is not auth; still untrusted input |
| Browser → displayed prices | Display-only; charged amount decided server-side at checkout |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering | lib/cart.ts upsert body | high | mitigate | Client sends productId+quantity only; never sends price (D-05) |
| T-06-05 | Spoofing | localStorage session | low | accept | Nonce obscurity MVP; Phase 7 accounts harden ownership |
| T-06-SC | Tampering | npm packages | low | accept | No new npm dependencies this plan |
</threat_model>

<verification>
Manual smoke acceptable for frontend (no test script): start stack via dev skill, add product, confirm /cart and navbar badge. Automated gates are file/grep checks above.
</verification>

<success_criteria>
A guest can add a product from /products/[slug], see cart count, and review/update lines on /cart.
</success_criteria>

<output>
Create `.planning/phases/06-cart-checkout/06-02-SUMMARY.md` when done
</output>
