---
phase: 02-booking-core
plan: 05
type: execute
wave: 4
depends_on: [02-03, 02-04]
files_modified:
  - landing-page/lib/appointments.ts
  - landing-page/components/icons.tsx
  - landing-page/components/AppointmentBookingForm.tsx
  - landing-page/app/book/page.tsx
  - landing-page/components/Contact.tsx
  - landing-page/lib/api.ts
autonomous: true
requirements: [BOOK-02, BOOK-03, BOOK-06]
user_setup: []
must_haves:
  truths:
    - "The /book page is a single progressive-reveal flow: choose service → choose stylist (Any Available Stylist default) → pick date & time → enter contact details → Confirm Appointment (D-15, UI-SPEC)"
    - "The slot grid renders real open times fetched from GET /api/appointments/slots as the selected date changes, each labelled with the salon zone, never the browser's timezone (D-16, BOOK-01/06)"
    - "On success the form shows an on-screen confirmation carrying full details — service, concrete stylist name, salon-local date/time with zone label, duration, price — self-sufficient because the email is best-effort (D-11, BOOK-03)"
    - "A 409 'slot taken' response does NOT clear the client's details; it returns focus to the slot step, marks the taken slot unavailable, re-fetches the grid, and shows the destructive banner (UI-SPEC 409 recovery, BOOK-04 surfaced)"
    - "The ?service={slug} deep link from the Phase-1 service-detail CTA still preselects the service on /book (D-15)"
    - "No frontend code calls the retired /api/bookings endpoint: BookingRequestForm is deleted and Contact.tsx no longer calls createBooking (D-14)"
  prohibitions:
    - statement: "Appointment times are never rendered in the browser's local timezone (no bare Intl.DateTimeFormat with implicit zone)"
      status: pending
      verification: "AppointmentBookingForm renders times using the offset embedded in the API's DateTimeOffset and the salon zone label, not the browser zone; grep shows no timeZone-less date formatting of slot times"
    - statement: "createAppointment surfaces a 409 distinctly from a 400 (does not swallow errors like fetchServices does)"
      status: pending
      verification: "lib/appointments.ts createAppointment throws/returns a typed result exposing HTTP 409 vs 400 vs network failure; fetchOpenSlots must NOT be wrapped in Next revalidate caching"
    - statement: "No createBooking / BookingRequest / BookingResponse remains in the frontend (retired with the backend, D-14)"
      status: pending
      verification: "grep for createBooking/BookingRequest/BookingResponse across landing-page returns nothing; BookingRequestForm.tsx is deleted"
  artifacts:
    - landing-page/lib/appointments.ts
    - landing-page/components/AppointmentBookingForm.tsx
  key_links:
    - "fetchOpenSlots/createAppointment in lib/appointments.ts are the sole client bridge to GET /api/appointments/slots and POST /api/appointments"
    - "app/book/page.tsx keeps the Promise.all([searchParams, fetchServices()]) RSC pattern and swaps BookingRequestForm → AppointmentBookingForm, preserving ?service deep-linking"
    - "Contact.tsx's homepage booking form routes into /book (the real flow) instead of POSTing free text — the last consumer of the retired endpoint"
---

## Phase Goal

**As a** client, **I want to** pick a service and book a real open slot with my chosen stylist, **so that** my appointment is confirmed and never double-booked.

> Vertical slice: the public UI. This is where the whole phase becomes a thing a real person can do on the site — walk the progressive-reveal /book flow, see real open times in salon-local time, confirm, and get an on-screen confirmation (plus a best-effort email). It also removes the last traces of the retired free-text booking path from the frontend (D-14).

<objective>
Build the client data layer (lib/appointments.ts) and the /book progressive-reveal booking UI per the UI-SPEC and D-15, wire it into the existing RSC page (preserving the ?service deep link), and retire the legacy frontend booking path — delete BookingRequestForm, repoint the homepage Contact form into the /book flow, and remove the dead createBooking client.

Purpose: Deliver BOOK-02 (book end-to-end on the public site), the on-screen half of BOOK-03 (D-11 self-sufficient confirmation), and the stylist picker of BOOK-06, while honoring D-14's wholesale retirement on the frontend.
Output: A working /book page, an appointments API client, a repointed homepage, and no remaining calls to /api/bookings.
</objective>

<execution_context>
@D:/repos/vct/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@D:/repos/vct/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-booking-core/02-CONTEXT.md
@.planning/phases/02-booking-core/02-UI-SPEC.md
@.planning/phases/02-booking-core/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/appointments.ts (fetchOpenSlots, createAppointment, Zod schemas) + new icons</name>
  <files>
    landing-page/lib/appointments.ts,
    landing-page/components/icons.tsx
  </files>
  <read_first>
    landing-page/lib/services.ts,
    landing-page/lib/api.ts,
    landing-page/components/icons.tsx,
    API/ZachHairStudio.Shared/Features/Availability/OpenSlotDto.cs,
    API/ZachHairStudio.Shared/Features/Appointments/AppointmentResponseDto.cs
  </read_first>
  <action>
    Create lib/appointments.ts mirroring lib/services.ts's API_BASE_URL + Zod pattern (D-18), but:
      - OpenSlotSchema: { startsAt: z.string() (ISO with offset), stylistId: z.number().nullable().optional(), stylistName: z.string().nullable().optional() }; OpenSlotListSchema = array.
      - AppointmentResponseSchema matching AppointmentResponseDto (id, serviceId, serviceName, stylistId, stylistName, startsAt, durationMinutes, price, status, first/last/email/phone).
      - fetchOpenSlots(serviceId: number, stylistId: number | null, date: string /*YYYY-MM-DD*/): GET `${API_BASE_URL}/api/appointments/slots?serviceId=&stylistId=&date=`. Do NOT wrap in Next `next: { revalidate }` — slot data must always be fresh (D-15). Return parsed slots; on failure throw a typed error the component can show as the "Couldn't Load Times" state (do NOT silently swallow to empty like fetchServices, because the UI needs to distinguish empty-day from load-failure).
      - createAppointment(dto): POST `${API_BASE_URL}/api/appointments`. On 201 return the parsed AppointmentResponse. On failure, surface the status distinctly: throw a typed error exposing `status` so the component can branch 409 (slot taken → recovery UX) vs 400 (validation) vs network. Reuse the ProblemDetails/ModelState message-extraction shape from the old lib/api.ts extractErrorMessage.

    Add the icons the UI-SPEC calls for to icons.tsx following the existing ArrowRightIcon pattern (function returning `<svg fill="none" stroke="currentColor" strokeWidth={2} viewBox="0 0 24 24">` taking SVGProps): CheckIcon (checkmark), CalendarIcon, ClockIcon, AlertIcon. Keep the existing exports intact.
  </action>
  <verify>
    <automated>npm --prefix landing-page run lint</automated>
  </verify>
  <acceptance_criteria>
    - lib/appointments.ts exports fetchOpenSlots and createAppointment plus OpenSlotSchema/AppointmentResponseSchema Zod schemas.
    - fetchOpenSlots contains no `revalidate` cache option; createAppointment exposes the HTTP status so a 409 is distinguishable from a 400.
    - icons.tsx additionally exports CheckIcon, CalendarIcon, ClockIcon, AlertIcon and still exports ArrowRightIcon/StarIcon/PersonIcon/MapPinIcon.
    - `npm --prefix landing-page run lint` passes.
  </acceptance_criteria>
  <done>The appointments API client exists with fresh (uncached) slot fetching and status-aware create; the new icons are available.</done>
</task>

<task type="auto">
  <name>Task 2: AppointmentBookingForm progressive-reveal UI + confirmation + 409 recovery; wire into /book; delete BookingRequestForm</name>
  <files>
    landing-page/components/AppointmentBookingForm.tsx,
    landing-page/app/book/page.tsx,
    landing-page/components/BookingRequestForm.tsx
  </files>
  <read_first>
    landing-page/components/BookingRequestForm.tsx,
    landing-page/app/book/page.tsx,
    landing-page/lib/appointments.ts,
    landing-page/lib/services.ts,
    .planning/phases/02-booking-core/02-UI-SPEC.md
  </read_first>
  <action>
    Create AppointmentBookingForm.tsx ("use client") reusing BookingRequestForm's Field sub-component, inputClass, priceFormatter, and the submitted/submitting/error state pattern, extended into the D-15 progressive reveal per the UI-SPEC "Additional Notes":
      - Step 1 "1. Choose a Service": select from the passed services (preselect initialServiceSlug via ?service deep link).
      - Step 2 "2. Choose a Stylist": chip row, "Any Available Stylist" first and default-selected; fetch stylists from GET /api/stylists (or accept as prop). Selected chip uses gold border/checkmark; unselected border-white/10. Hidden until a service is chosen.
      - Step 3 "3. Pick a Date & Time": a date input (same-day through 60 days ahead — owner-reviewable per UI-SPEC/RESEARCH A4, dates outside disabled), and a slot grid (`grid grid-cols-3 sm:grid-cols-4 gap-4`, 44px min touch target). On date change call fetchOpenSlots(serviceId, stylistId, date); show "Finding open times…" loading, "No Openings This Day" empty state, and the "Couldn't Load Times" + Try Again state on fetch failure. Render each time in salon-local time using the offset in the returned DateTimeOffset with a single shared zone caption ("All times shown in salon local time (Eastern)") — NEVER the browser zone (D-16). Hidden until stylist is resolved.
      - Step 4 "4. Your Details": FirstName/LastName/Email/Phone guest fields (no account — D-15). Hidden until a slot is picked.
      - Primary CTA "Confirm Appointment" (bg-gold), label swaps to "Confirming…" while submitting; enabled only when service + slot + required contact fields are valid.
      - On success: replace the form with the confirmation panel ("You're All Set!") showing the full AppointmentResponse details — service name, concrete stylist name (never "Any"), salon-local date/time with zone label, duration, price — the five self-sufficient fields required by D-11. The "Save or screenshot this confirmation — email delivery isn't guaranteed" caption was intentionally removed at owner request. `[RECONCILED 02-09 — caption removed at owner request; see 02-06-SUMMARY.md Deviations + 02-VERIFICATION.md Concern 5]`
      - On 409: do NOT clear the details; scroll/focus to Step 3, show the destructive inline banner ("Slot taken — pick another time."), mark the attempted slot unavailable (line-through/opacity-40), and re-fetch the slot list for the selected date (UI-SPEC 409 recovery).
      Changing an earlier step resets later selections (e.g. new service clears the chosen slot). Follow the UI-SPEC color discipline (gold reserved for the listed elements only) and typography/spacing scales.

    Update app/book/page.tsx: keep the Promise.all([searchParams, fetchServices()]) RSC pattern and the services.length === 0 empty state; swap `BookingRequestForm` for `AppointmentBookingForm` (pass services + initialServiceSlug={service}); update the SectionHeading copy toward the UI-SPEC ("Book Your Appointment" + subtitle) if trivial.

    Delete landing-page/components/BookingRequestForm.tsx (retired, D-14).
  </action>
  <verify>
    <automated>npm --prefix landing-page run build</automated>
  </verify>
  <acceptance_criteria>
    - AppointmentBookingForm.tsx implements the four progressive-reveal steps with the stylist chips (Any Available Stylist default), a slot grid bound to fetchOpenSlots, and the confirmation panel with the five self-sufficient details (service, concrete stylist, salon-local date/time with zone label, duration, price); the email-not-guaranteed caption was intentionally removed at owner request. `[RECONCILED 02-09 — caption removed at owner request; see 02-06-SUMMARY.md Deviations + 02-VERIFICATION.md Concern 5]`
    - The 409 path re-fetches slots and shows the destructive banner without clearing contact details (grep shows the createAppointment 409 branch triggers a slots re-fetch, not a full reset).
    - app/book/page.tsx imports AppointmentBookingForm (not BookingRequestForm) and preserves the ?service deep link; BookingRequestForm.tsx no longer exists.
    - Slot times are formatted from the DateTimeOffset offset + salon zone label, with no browser-timezone formatting of slot times.
    - `npm --prefix landing-page run build` succeeds (types + compile).
  </acceptance_criteria>
  <done>The /book page presents the full progressive-reveal booking flow with real slots, salon-local times, a self-sufficient confirmation, and 409 recovery; the legacy form is deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint the homepage Contact form into /book and remove the dead createBooking client (D-14)</name>
  <files>
    landing-page/components/Contact.tsx,
    landing-page/lib/api.ts
  </files>
  <read_first>
    landing-page/components/Contact.tsx,
    landing-page/lib/api.ts,
    landing-page/app/page.tsx
  </read_first>
  <action>
    Contact.tsx currently calls the retired createBooking → POST /api/bookings, which no longer exists after Plan 01. Repoint it minimally (do NOT redesign the section): remove the `createBooking` import and the POST submission; on submit (or via the CTA button), navigate the client to `/book?service={selectedSlug}` using next/navigation useRouter, preserving the existing service preselect so the real booking flow opens with that service chosen. Keep the contact info column (branches, hours, email) and the section styling as-is. Remove the now-unused submitted/submitting/error POST states if they become dead. This is a REQUIRED consequence of D-14 (no free-text booking POST may remain); flag in the SUMMARY that the homepage "Book Your Appointment" quick form now routes into the real /book flow and that its exact treatment is owner-reviewable (the UI-SPEC scoped only /book, not this section).

    lib/api.ts: remove the createBooking function and the BookingRequest / BookingResponse types (dead once BookingRequestForm and Contact's POST are gone). If nothing else imports from lib/api.ts after this, delete the file; otherwise keep only what is still used. Verify via grep that no file imports the removed symbols before finishing.
  </action>
  <verify>
    <automated>npm --prefix landing-page run build</automated>
  </verify>
  <acceptance_criteria>
    - grep for `createBooking`, `BookingRequest`, `BookingResponse` across landing-page returns no matches.
    - Contact.tsx no longer imports from lib/api.ts and routes to `/book?service=` on submit/CTA, preserving the selected service.
    - lib/api.ts either no longer exists or contains no Booking-related exports, and nothing imports removed symbols.
    - `npm --prefix landing-page run build` succeeds.
  </acceptance_criteria>
  <done>The homepage booking form routes into the real /book flow, the dead createBooking client is gone, and the frontend has no remaining reference to the retired /api/bookings endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | Client fetches slots and posts a guest booking; server is authoritative (client never computes slot validity) |
| rendering | DateTimeOffset from the API is rendered with the salon zone, not the browser zone (correctness boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Spoofing/Tampering | Client-side slot validity | medium | mitigate | UI treats the server as authoritative; a stale slot yields a 409 handled by the recovery UX, and the server re-validates + the unique index backstops it |
| T-02-12 | Information Disclosure | Zod parse of API responses | low | mitigate | Response schemas (D-18) reject malformed payloads before render; createAppointment surfaces status without leaking internals |
| T-02-05 | Tampering | Browser-timezone misrender | medium | mitigate | Times rendered from the DateTimeOffset offset + salon zone label, never the browser zone (D-16) |
| T-02-SC | Tampering | npm installs | low | accept | No new npm packages (Zod/next already present); no install step |
</threat_model>

<verification>
- `npm --prefix landing-page run build` succeeds (types + compile) and `npm --prefix landing-page run lint` passes.
- grep confirms no createBooking/BookingRequest/BookingResponse and no BookingRequestForm.tsx remain.
- The /book flow renders the four progressive steps, real slots in salon-local time, a full-detail confirmation, and 409 recovery (functional proof deferred to Plan 06's human-verify).
</verification>

<success_criteria>
A client can complete a booking end-to-end on the public /book page — service → stylist → date/slot → details → confirm — see real open times in salon-local time, and get a self-sufficient on-screen confirmation, with a clear 409 recovery path. The homepage routes into the same flow and no frontend code references the retired booking endpoint.
</success_criteria>

## Artifacts this phase produces (Plan 05)

- Client: `lib/appointments.ts` (`fetchOpenSlots`, `createAppointment`, `OpenSlotSchema`, `AppointmentResponseSchema`)
- Component: `AppointmentBookingForm` (progressive reveal + confirmation + 409 recovery)
- Icons: `CheckIcon`, `CalendarIcon`, `ClockIcon`, `AlertIcon`
- Repoint: `Contact.tsx` → routes to `/book`; `app/book/page.tsx` → uses `AppointmentBookingForm`
- Removed (retired, D-14): `BookingRequestForm.tsx`, `createBooking`/`BookingRequest`/`BookingResponse`

<output>
Create `.planning/phases/02-booking-core/02-05-SUMMARY.md` when done. Flag the homepage Contact repoint and the 60-day booking horizon as owner-reviewable, and note anything the human-verify (Plan 06) should specifically check.
</output>
