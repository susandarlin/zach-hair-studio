---
phase: 02-booking-core
plan: 06
type: execute
wave: 5
depends_on: [02-05]
files_modified: []
autonomous: false
requirements: [BOOK-02, BOOK-03, BOOK-04, BOOK-05, BOOK-06]
user_setup: []
must_haves:
  truths:
    - "A human confirms the /book progressive-reveal flow works end-to-end in a browser and that the on-screen confirmation carries full self-sufficient details with the salon zone label (BOOK-02, D-11, D-16)"
    - "A human confirms a real confirmation email arrives and contains service, stylist, salon-local time + zone, duration, and price (BOOK-03, D-12 manual-only)"
    - "A human confirms the 409 'slot taken' recovery path reads clearly when a second client claims the slot mid-flow (BOOK-04 surfaced in the UI)"
  prohibitions:
    - statement: "This checkpoint is not auto-approvable — it verifies real browser UX and real email delivery that automated tests deliberately do not assert (D-12)"
      status: pending
      verification: "Task is checkpoint:human-verify with gate=blocking; resume requires an explicit human signal"
  artifacts: []
  key_links:
    - "This gate closes the Manual-Only Verifications listed in 02-VALIDATION.md (email delivery, /book UX + 409 recovery, verified sending domain)"
    - "Requires the full stack running via the `dev` skill (API + landing-page) and RESEND_API_KEY + verified domain from Plan 02"
---

## Phase Goal

**As a** client, **I want to** pick a service and book a real open slot with my chosen stylist, **so that** my appointment is confirmed and never double-booked.

> Final gate. The automated suite proves SC1–SC5 mechanically; this plan closes the three checks that only a human can make — the browser UX quality, the 409 recovery experience, and that a real confirmation email actually lands in an inbox (D-12's whole point). No code ships here.

<objective>
Drive the complete booking experience in a browser against the running stack and confirm the manual-only behaviors that 02-VALIDATION.md flags: progressive-reveal UX, self-sufficient confirmation, live 409 recovery, and real email delivery from the verified Resend domain.

Purpose: Verify the human-judgment behaviors (visual/interaction quality + real third-party email delivery) that automated tests intentionally do not assert.
Output: A human sign-off (or a defect list) closing the phase's Manual-Only Verifications.
</objective>

<execution_context>
@D:/repos/vct/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@D:/repos/vct/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-booking-core/02-VALIDATION.md
@.planning/phases/02-booking-core/02-UI-SPEC.md
@.planning/phases/02-booking-core/02-CONTEXT.md
@.claude/skills/dev/SKILL.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 [BLOCKING HUMAN]: Drive the full /book flow in a browser and confirm real email delivery + 409 recovery</name>
  <files>none (verification-only — no files modified)</files>
  <read_first>
    .planning/phases/02-booking-core/02-VALIDATION.md,
    .planning/phases/02-booking-core/02-UI-SPEC.md,
    .claude/skills/dev/SKILL.md
  </read_first>
  <what-built>
    The full booking stack: GET /api/appointments/slots + POST /api/appointments (double-booking-safe, 409 on conflict, best-effort Resend email) and the /book progressive-reveal UI with salon-local time display, self-sufficient confirmation, and 409 recovery. Automated tests already prove SC1–SC5 (grid math, DST offsets, concurrency 201/409, datetimeoffset round-trip). This gate covers only what a human must judge.
  </what-built>
  <action>
    Run the stack via the `dev` skill and manually verify the six behaviors in the how-to-verify steps below (progressive reveal, salon-local time labels, self-sufficient confirmation, real email receipt, live 409 recovery, and the ?service deep link). This is not auto-approvable — it verifies real browser UX and real third-party email delivery that automated tests deliberately do not assert (D-12).
  </action>
  <how-to-verify>
    Start the stack with the `dev` skill (.NET API + landing-page). Ensure RESEND_API_KEY is set and the sending domain shows Verified (from Plan 02). Then:

    1. Visit http://localhost:3000/book. Confirm the progressive reveal: Step 2 (stylist) appears only after picking a service; Step 3 (date/slots) only after a stylist choice ("Any Available Stylist" counts); Step 4 (details) only after picking a slot.
    2. Confirm the slot grid shows real open times labelled in salon-local time (e.g. "10:00 AM" with the shared "salon local time (Eastern)" caption) — NOT your browser's timezone. Pick a different date and confirm the grid refreshes; pick a fully-booked/closed date and confirm the "No Openings This Day" empty state.
    3. Complete a booking with a REAL email address you control. Confirm the on-screen confirmation shows the five self-sufficient fields: service, the concrete assigned stylist name (never "Any"), salon-local date/time with the zone label, duration, and price. The "email delivery isn't guaranteed" caption was intentionally removed at owner request. `[RECONCILED 02-09 — caption removed at owner request; see 02-06-SUMMARY.md Deviations + 02-VERIFICATION.md Concern 5]`
    4. Confirm a real confirmation email arrives in that inbox and contains service, stylist, salon-local time + zone, duration, and price (BOOK-03 / D-12).
    5. 409 recovery: open /book in two browsers, get both to the same slot for the same stylist, confirm in the first, then confirm in the second. Verify the second shows the "Slot taken — pick another time." banner, keeps the entered details, marks that slot unavailable, and refreshes the grid.
    6. Deep link: visit http://localhost:3000/book?service=precision-cut and confirm Precision Cut is preselected; confirm the homepage "Book Your Appointment" section routes into /book.
  </how-to-verify>
  <verify>
    <human-check>All six steps pass: progressive reveal gates correctly, slot times show salon-local labels, the on-screen confirmation is self-sufficient, a real email arrives with full details, the 409 recovery keeps details and refreshes the grid, and the ?service deep link preselects.</human-check>
  </verify>
  <acceptance_criteria>
    - A real confirmation email arrives containing service, stylist, salon-local time + zone, duration, and price.
    - The 409 recovery path shows the "Slot taken" banner, preserves entered details, and refreshes the slot grid.
    - The on-screen confirmation shows the concrete assigned stylist (never "Any") with the salon zone label.
    - Any failing check is captured as a defect list for a `/gsd-plan-phase 2 --gaps` pass.
  </acceptance_criteria>
  <done>A human has driven the full flow, received a real confirmation email, and confirmed the 409 recovery UX — closing the Manual-Only Verifications in 02-VALIDATION.md.</done>
  <resume-signal>Type "approved" if all six checks pass, or list the specific failures (with screenshots/steps) to route into a --gaps closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human ↔ running stack | Manual verification of behaviors automated tests deliberately omit (real email, visual UX) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Repudiation | Undelivered confirmation email | medium | mitigate | On-screen confirmation is self-sufficient (D-11); this gate confirms real delivery from the verified domain, and a failure routes to a gap plan rather than silently passing |
| T-02-SC | Tampering | npm/NuGet installs | low | accept | No installs in a verification-only plan |
</threat_model>

<verification>
- Human confirms: progressive reveal, salon-local time labels, self-sufficient confirmation, real email receipt, live 409 recovery, and ?service deep link.
- Any failure is captured as a defect list for a `/gsd-plan-phase 2 --gaps` closure pass.
</verification>

<success_criteria>
A human has driven the full booking flow in a browser, received a real confirmation email with all required details, and confirmed the 409 recovery experience reads clearly — closing the Manual-Only Verifications in 02-VALIDATION.md and the phase's user-facing acceptance.
</success_criteria>

## Artifacts this phase produces (Plan 06)

- No code artifacts (verification-only). Produces a human sign-off closing the phase's Manual-Only Verifications.

<output>
Create `.planning/phases/02-booking-core/02-06-SUMMARY.md` when done, recording the sign-off (or the defect list) and whether the sending domain was verified at run time.
</output>
