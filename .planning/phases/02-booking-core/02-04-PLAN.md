---
phase: 02-booking-core
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - API/ZachHairStudio.Shared/Features/Appointments/AppointmentCreateDto.cs
  - API/ZachHairStudio.Shared/Features/Appointments/AppointmentCreateDtoValidator.cs
  - API/ZachHairStudio.Shared/Features/Appointments/AppointmentResponseDto.cs
  - API/ZachHairStudio.Shared/Features/Appointments/AppointmentExtensions.cs
  - API/ZachHairStudio.Shared/Features/Appointments/AppointmentsService.cs
  - API/ZachHairStudio.Shared/Features/Appointments/IEmailService.cs
  - API/ZachHairStudio.Shared/Features/Appointments/ResendEmailService.cs
  - API/ZachHairStudio.Api/Controllers/AppointmentsController.cs
  - API/ZachHairStudio.Api/Program.cs
  - API/ZachHairStudio.Api/appsettings.json
  - API/ZachHairStudio.Api.Tests/Features/Appointments/AppointmentsControllerTests.cs
  - API/ZachHairStudio.Api.Tests/Features/Appointments/AppointmentCreateDtoValidatorTests.cs
  - API/ZachHairStudio.Api.Tests/Features/Appointments/AnyStylistAssignmentTests.cs
  - API/ZachHairStudio.Api.Tests/Features/Appointments/ConcurrencyTests.cs
  - API/ZachHairStudio.Api.Tests/Features/Appointments/DstRoundTripTests.cs
autonomous: true
requirements: [BOOK-02, BOOK-03, BOOK-04, BOOK-06]
user_setup: []
must_haves:
  truths:
    - "POST /api/appointments with a valid open slot returns 201 with full appointment details (service, concrete stylist, salon-local start, status) (BOOK-02)"
    - "Two near-simultaneous POSTs for the same stylist/slot yield exactly one 201 and one 409, and exactly one AppointmentSlot row exists afterward (BOOK-04, SC4)"
    - "The 409 is produced by catching the DB unique-index violation (SqlException.Number 2601 OR 2627) and mapping to Result.DuplicateRecordError → ProblemDetails 409 — never a 500 (BOOK-04, D-03)"
    - "With no stylist chosen, the server deterministically assigns one free active stylist inside the create (Any stylist), and the 201 names the concrete assigned stylist (BOOK-06, D-07)"
    - "A booked appointment's StartsAt is stored as datetimeoffset and round-trips correctly across the 2026 spring-forward (Mar 8) and fall-back (Nov 1) boundaries (BOOK-05, SC5)"
    - "The confirmation email is sent through a real transactional provider (ResendEmailService — a live Resend REST call, not a dev-only sink), attempted AFTER the appointment commits, with a send failure logged but never rolling back the appointment (BOOK-03, D-09, D-11)"
  prohibitions:
    - statement: "No manual BeginTransactionAsync is opened for the insert (incompatible with EnableRetryOnFailure; SaveChangesAsync is already atomic)"
      status: pending
      verification: "AppointmentsService.cs contains no BeginTransaction; atomicity relies on a single SaveChangesAsync per candidate"
    - statement: "Duplicate-key detection catches BOTH 2601 and 2627 (not 2627 alone)"
      status: pending
      verification: "The IsDuplicateKeyViolation check references 2601 and 2627"
    - statement: "The Resend HTTP call is never made inside a DB transaction, and never rethrows (best-effort, D-11)"
      status: pending
      verification: "ResendEmailService.SendConfirmationAsync is called after SaveChangesAsync returns; its body catches and logs, never rethrows"
    - statement: "Client-supplied name/email are HTML-encoded before interpolation into the email body (no header/HTML injection)"
      status: pending
      verification: "ResendEmailService HTML-encodes interpolated client fields (e.g. System.Net.WebUtility.HtmlEncode)"
    - statement: "The client-submitted StartsAt is re-validated server-side against the open-slot grid before insert (never trust the echoed slot)"
      status: pending
      verification: "AppointmentsService validates the requested StartsAt is a currently-open slot via SlotService before building rows"
  artifacts:
    - API/ZachHairStudio.Shared/Features/Appointments/AppointmentsService.cs
    - API/ZachHairStudio.Shared/Features/Appointments/ResendEmailService.cs
    - API/ZachHairStudio.Api.Tests/Features/Appointments/ConcurrencyTests.cs
    - API/ZachHairStudio.Api.Tests/Features/Appointments/DstRoundTripTests.cs
  key_links:
    - "AppointmentsService reuses SlotService (Plan 03) for server-side slot validation and SalonTimeZone for the instant, so the slot a client saw maps to the exact stored instant (Pitfall 5)"
    - "The unfiltered unique index from Plan 01 is what the retry loop relies on to produce the 409 under concurrency"
    - "AppointmentResponseDto carries every detail the on-screen confirmation (Plan 05) needs, because the email is best-effort (D-11)"
---

## Phase Goal

**As a** client, **I want to** pick a service and book a real open slot with my chosen stylist, **so that** my appointment is confirmed and never double-booked.

> Vertical slice: the write side and the heart of the phase. After this plan a client (via the API) can confirm a booking that the database guarantees is not double-booked, gets a best-effort email, and — crucially — the two hardest correctness proofs (SC4 concurrency, SC5 DST round-trip) are green against real SQL Server. BOOK-02/03/04 and the assignment half of BOOK-06.

<objective>
Implement AppointmentsService with the candidate-stylist try-insert retry loop (no manual transaction), the create DTO + validator + response DTO + mapper, the Resend best-effort email service, and the POST /api/appointments endpoint that maps success→201, validation→400, and unique-index conflict→409. Prove it: validator tests, Any-stylist assignment, and — on the real SQL Server fixture — the two-concurrent-requests SC4 test and the DST round-trip SC5 test.

Purpose: Deliver the double-booking-safe booking (SC4/BOOK-04), the end-to-end booking write (BOOK-02), the best-effort confirmation email (BOOK-03/D-11), and deterministic Any-stylist assignment (BOOK-06/D-07).
Output: AppointmentsService + ResendEmailService + POST endpoint + the SC4 and SC5 tests, all green.
</objective>

<execution_context>
@D:/repos/vct/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@D:/repos/vct/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-booking-core/02-CONTEXT.md
@.planning/phases/02-booking-core/02-RESEARCH.md
@.planning/phases/02-booking-core/02-PATTERNS.md
@.planning/phases/02-booking-core/02-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AppointmentCreateDto + validator + response DTO/mapper + AppointmentsService retry loop (test-first)</name>
  <files>
    API/ZachHairStudio.Shared/Features/Appointments/AppointmentCreateDto.cs,
    API/ZachHairStudio.Shared/Features/Appointments/AppointmentCreateDtoValidator.cs,
    API/ZachHairStudio.Shared/Features/Appointments/AppointmentResponseDto.cs,
    API/ZachHairStudio.Shared/Features/Appointments/AppointmentExtensions.cs,
    API/ZachHairStudio.Shared/Features/Appointments/AppointmentsService.cs,
    API/ZachHairStudio.Api.Tests/Features/Appointments/AppointmentCreateDtoValidatorTests.cs,
    API/ZachHairStudio.Api.Tests/Features/Appointments/AnyStylistAssignmentTests.cs
  </files>
  <read_first>
    API/ZachHairStudio.Shared/Features/Services/ServiceCreateDto.cs,
    API/ZachHairStudio.Shared/Features/Services/ServiceCreateDtoValidator.cs,
    API/ZachHairStudio.Shared/Features/Services/ServicesService.cs,
    API/ZachHairStudio.Shared/Features/Services/ServiceExtensions.cs,
    API/ZachHairStudio.Shared/Result.cs,
    API/ZachHairStudio.Shared/Features/Availability/SlotService.cs,
    API/ZachHairStudio.Shared/Features/Availability/SalonTimeZone.cs,
    API/ZachHairStudio.Shared/Features/Appointments/Appointment.cs,
    API/ZachHairStudio.Shared/Features/Appointments/AppointmentSlot.cs,
    .planning/phases/02-booking-core/02-RESEARCH.md
  </read_first>
  <behavior>
    - Validator rejects: ServiceId ≤ 0; StylistId present but ≤ 0; empty FirstName/LastName; non-email Email; FirstName/LastName > 100 chars; Phone > 30 chars; StartsAt not in the future; StartsAt off the 15-minute grid.
    - Validator accepts a well-formed guest booking (name/email/phone, valid future on-grid StartsAt, optional stylistId).
    - Any-stylist (stylistId null): given two active stylists both free at the slot, CreateAsync assigns one deterministically (lowest StylistId) and the response StylistId/StylistName name that concrete stylist.
    - Any-stylist where the lowest-id stylist's slot is already taken: the loop tries the next candidate and succeeds (assignment falls through to the free stylist).
    - When all candidates are taken: CreateAsync returns Result.DuplicateRecordError (no exception escapes).
    - A requested StartsAt that is not currently an open slot returns a validation/NotFound Result, not a 201.
  </behavior>
  <action>
    Write validator + assignment tests first (RED), then implement (GREEN).

    AppointmentCreateDto: int ServiceId; int? StylistId; DateTimeOffset StartsAt; string FirstName; string LastName; string Email; string? Phone. (Guest booking — no account fields, D-15.)

    AppointmentCreateDtoValidator (AbstractValidator, mirror ServiceCreateDtoValidator; picked up automatically by AddValidatorsFromAssemblyContaining): rules per the behavior block. For the on-grid/future rules use .Must(...) predicates (StartsAt minutes ∈ {0,15,30,45}, seconds 0; StartsAt > DateTimeOffset.UtcNow). Field-length bounds match Appointment (100/100/150/30). Exact lead-time/horizon bounds are Claude's discretion — apply sensible owner-reviewable defaults (e.g. no same-day cutoff, ≤ 60 days ahead) and flag them in the SUMMARY.

    AppointmentResponseDto: Id; ServiceId; ServiceName; int StylistId; string StylistName (concrete assigned stylist — never "Any"); DateTimeOffset StartsAt; int DurationMinutes; decimal Price; string Status; FirstName; LastName; Email; Phone. Carries every detail the on-screen confirmation needs (D-11). AppointmentExtensions.ToDto(this Appointment, Service, Stylist) builds it.

    AppointmentsService: constructor injects BookingDbContext, IValidator&lt;AppointmentCreateDto&gt;, SlotService, IEmailService, and SalonTimeZone/config. CreateAsync(request):
      1. Validate → Result.ValidationError on failure.
      2. Re-validate the requested StartsAt against SlotService.GetOpenSlotsAsync (server-side; never trust the echoed slot) → Result.NotFoundError/ValidationError if not currently open.
      3. Resolve candidate stylists: [request.StylistId] if set, else active stylists free at that slot ordered deterministically by StylistId (D-07).
      4. For each candidate: build one Appointment + N AppointmentSlot rows (N = ceil(Service.DurationMinutes/15), consecutive 15-min cells from StartsAt), Add, single await SaveChangesAsync() (NO manual transaction — Pitfall 2). On success: fire IEmailService.SendConfirmationAsync(...) AFTER the commit (best-effort, D-11), return Result.Success(dto). On DbUpdateException where inner SqlException.Number is 2601 OR 2627 (IsDuplicateKeyViolation): detach the Appointment + its slots, try next candidate.
      5. Candidates exhausted → Result.DuplicateRecordError("This slot was just booked by someone else. Please choose another time.").
    Reuse SalonTimeZone for any wall-clock math so the stored instant matches what the slot query produced (Pitfall 5). Email interface (IEmailService) is created here as a stub dependency; the concrete ResendEmailService is Task 2.
  </action>
  <verify>
    <automated>dotnet test API/ZachHairStudio.Api.Tests --filter "FullyQualifiedName~AppointmentCreateDtoValidatorTests|FullyQualifiedName~AnyStylistAssignmentTests"</automated>
  </verify>
  <acceptance_criteria>
    - AppointmentsService.cs contains an `IsDuplicateKeyViolation` check referencing both `2601` and `2627`, and contains no `BeginTransaction`.
    - AppointmentCreateDtoValidatorTests proves rejection of non-future StartsAt, off-grid StartsAt, invalid email, and over-length names, and acceptance of a valid guest booking.
    - AnyStylistAssignmentTests proves deterministic assignment (lowest free StylistId) and fall-through to the next candidate when the first is taken.
    - `dotnet test --filter "FullyQualifiedName~AppointmentCreateDtoValidatorTests|FullyQualifiedName~AnyStylistAssignmentTests"` exits 0.
  </acceptance_criteria>
  <done>The create DTO, validator, response DTO/mapper, and the retry-loop AppointmentsService exist and pass their unit/InMemory tests; the loop catches 2601/2627 and never opens a manual transaction.</done>
</task>

<task type="auto">
  <name>Task 2: ResendEmailService (best-effort, HTML-encoded) + POST /api/appointments endpoint + DI wiring</name>
  <files>
    API/ZachHairStudio.Shared/Features/Appointments/IEmailService.cs,
    API/ZachHairStudio.Shared/Features/Appointments/ResendEmailService.cs,
    API/ZachHairStudio.Api/Controllers/AppointmentsController.cs,
    API/ZachHairStudio.Api/Program.cs,
    API/ZachHairStudio.Api/appsettings.json,
    API/ZachHairStudio.Api.Tests/Features/Appointments/AppointmentsControllerTests.cs
  </files>
  <read_first>
    API/ZachHairStudio.Api/Controllers/ServicesController.cs,
    API/ZachHairStudio.Api/Controllers/AppointmentsController.cs,
    API/ZachHairStudio.Api/Program.cs,
    API/ZachHairStudio.Shared/Result.cs,
    .planning/phases/02-booking-core/02-RESEARCH.md,
    .planning/phases/02-booking-core/02-PATTERNS.md
  </read_first>
  <action>
    IEmailService: Task SendConfirmationAsync(Appointment appointment, ServiceResponseDto service, string stylistName) (or an equivalent shape carrying the details needed for the body). ResendEmailService : IEmailService using a typed HttpClient: build a JSON payload { from, to, subject, html } and POST "emails" to the Resend base address; if !IsSuccessStatusCode log a warning; wrap the whole body in try/catch that only logs (never rethrow) so a Resend outage never rolls back the appointment (D-11). HTML-encode every client-supplied value (name, email) via System.Net.WebUtility.HtmlEncode before interpolating into the html body (email/HTML injection mitigation). The from address requires the verified sending domain (D-10) — read a non-secret Resend:FromEmail from appsettings; read the API key from configuration key Resend:ApiKey which is sourced from user-secrets/env (D-13), NEVER hardcoded.

    Program.cs: register builder.Services.AddScoped&lt;AppointmentsService&gt;() and builder.Services.AddHttpClient&lt;IEmailService, ResendEmailService&gt;(client =&gt; { client.BaseAddress = new Uri("https://api.resend.com/"); client.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", Configuration["Resend:ApiKey"]); }). appsettings.json: add a "Resend": { "FromEmail": "bookings@<verified-domain>" } section (non-secret) — do NOT add ApiKey here.

    AppointmentsController: add [HttpPost] CreateAppointment([FromBody] AppointmentCreateDto request). Mirror ServicesController's validate→ValidationProblem flow, then call AppointmentsService.CreateAsync. Map: result.IsValidationError() → ValidationProblem(400); result.IsNotFound() → NotFound(404); result.IsDuplicateRecord() → Conflict(new ProblemDetails { Title = "Slot taken", Detail = result.Message, Status = 409 }); success → CreatedAtAction/Created(201) with the AppointmentResponseDto. The 2601/2627→409 mapping must hold in ALL environments (including Development) so no SqlException stack trace leaks in a 500 (V7).

    AppointmentsControllerTests (InMemory fixture; register a fake IEmailService in the test host ONLY to assert the send was ATTEMPTED — this is not the D-12 real-send path, it is asserting call behavior, which RESEARCH permits for CI): POST a valid booking → 201 with full details; POST an off-grid/past StartsAt → 400; assert SendConfirmationAsync was invoked after a successful create and that a thrown email does not fail the 201 (D-11).
  </action>
  <verify>
    <automated>dotnet test API/ZachHairStudio.Api.Tests --filter "FullyQualifiedName~AppointmentsControllerTests"</automated>
  </verify>
  <acceptance_criteria>
    - ResendEmailService.cs POSTs to Resend, HTML-encodes interpolated client fields, and its send body catches-and-logs without rethrowing.
    - Program.cs registers `AddScoped<AppointmentsService>()` and `AddHttpClient<IEmailService, ResendEmailService>`; the Resend API key is read from `Resend:ApiKey` configuration (user-secrets), not appsettings.
    - AppointmentsController maps success→201, validation→400, and `IsDuplicateRecord()`→`Conflict`/409 with a ProblemDetails body.
    - AppointmentsControllerTests proves 201 (full details) on a valid booking, 400 on an off-grid StartsAt, that the email send is attempted post-commit, and that an email exception does not fail the 201.
    - `dotnet test --filter FullyQualifiedName~AppointmentsControllerTests` exits 0.
  </acceptance_criteria>
  <done>POST /api/appointments creates a booking and returns 201/400/409 correctly; the best-effort HTML-encoded Resend email is wired and attempted after commit; DI is complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove SC4 (concurrency) and SC5 (DST round-trip) on the real SQL Server fixture</name>
  <files>
    API/ZachHairStudio.Api.Tests/Features/Appointments/ConcurrencyTests.cs,
    API/ZachHairStudio.Api.Tests/Features/Appointments/DstRoundTripTests.cs
  </files>
  <read_first>
    API/ZachHairStudio.Api.Tests/SqlServerWebApplicationFactory.cs,
    API/ZachHairStudio.Api.Tests/Features/Appointments/AppointmentsControllerTests.cs,
    .planning/phases/02-booking-core/02-RESEARCH.md,
    .planning/phases/02-booking-core/02-VALIDATION.md
  </read_first>
  <behavior>
    - Two HttpClients POST the identical (stylistId, StartsAt) booking concurrently → the ordered status codes are exactly { Created, Conflict }, and exactly one AppointmentSlot row exists for that (StylistId, SlotStart).
    - Booking at 2026-03-08 10:00 salon-local stores StartsAt with offset -04:00 and reads back the same instant; booking at 2026-11-01 10:00 stores -05:00 and reads back the same instant (datetimeoffset compares by UTC instant).
  </behavior>
  <action>
    Use SqlServerWebApplicationFactory (real LocalDB) — NOT the InMemory fixture, which enforces no unique index (Pitfall 3). These tests must match the `FullyQualifiedName~SqlServer` naming OR live in classes the full-suite command runs; place them so the per-wave full-suite command exercises them.

    ConcurrencyTests (SC4): seed a stylist + working hours for a future date via the real fixture; create two clients; fire two PostAsJsonAsync("/api/appointments", sameRequest) with Task.WhenAll; assert exactly one 201 and one 409; then open a scope and assert AppointmentSlots.CountAsync for that (StylistId, SlotStart) == 1. This proves the guarantee is the DB index, not an app-level check.

    DstRoundTripTests (SC5): book a slot at 10:00 salon-local on 2026-03-08 and on 2026-11-01 through the create path; reload the Appointment from the DB and assert StartsAt.Offset is -04:00 and -05:00 respectively and that the persisted instant equals the submitted instant. (These exercise the real datetimeoffset column round-trip; the pure offset math is already covered by Plan 03's DstBoundaryTests.)

    If RESEND_API_KEY is required for the create path to complete (D-12), these run against the real key configured in Plan 02; if the key is unavailable in CI, mark the email attempt as non-fatal (D-11 already guarantees the booking commits regardless of email outcome) so the concurrency/DST assertions still hold.
  </action>
  <verify>
    <automated>dotnet test API/ZachHairStudio.Api.Tests --filter "FullyQualifiedName~ConcurrencyTests|FullyQualifiedName~DstRoundTripTests"</automated>
  </verify>
  <acceptance_criteria>
    - ConcurrencyTests asserts the two concurrent POSTs produce exactly one Created and one Conflict, AND that exactly one AppointmentSlot row exists for the contested (StylistId, SlotStart).
    - DstRoundTripTests asserts a 2026-03-08 10:00 booking persists offset -04:00 and a 2026-11-01 10:00 booking persists offset -05:00, each round-tripping the same instant.
    - Both test classes use SqlServerWebApplicationFactory (grep shows the real-SQL fixture, not CustomWebApplicationFactory).
    - `dotnet test --filter "FullyQualifiedName~ConcurrencyTests|FullyQualifiedName~DstRoundTripTests"` exits 0.
  </acceptance_criteria>
  <done>SC4 and SC5 are proven against real SQL Server: exactly one of two racing bookings wins with a clean 409, and appointment instants round-trip correctly across both 2026 DST transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (POST /api/appointments) | Untrusted guest input: serviceId, stylistId, StartsAt, name/email/phone |
| API → SQL Server | Multi-row insert whose atomicity + uniqueness the DB enforces |
| API → Resend (external) | Outbound HTTP with a secret bearer token; client strings flow into the email body |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Slot booking race (TOCTOU) | critical | mitigate | Retry loop relies on the Plan-01 unfiltered unique index; SC4 concurrency test proves exactly one 201/one 409; no app-level-only check |
| T-02-07 | Information Disclosure | Unhandled SqlException in a 500 | high | mitigate | Catch DbUpdateException(2601/2627) → ProblemDetails 409 in ALL environments; no SQL detail leaks (V7) |
| T-02-08 | Tampering | Email HTML/header injection via client name/email | medium | mitigate | FluentValidation bounds + WebUtility.HtmlEncode on interpolated client fields before building the html body |
| T-02-03 | Information Disclosure | RESEND_API_KEY | high | mitigate | Read from Resend:ApiKey (user-secrets/env, D-13); never appsettings; gitleaks enforced |
| T-02-09 | Tampering | Client-echoed slot trusted as bookable | high | mitigate | Server re-validates StartsAt against SlotService before insert; the unique index is the final backstop |
| T-02-10 | Denial of Service | Unauthenticated/unthrottled POST | low | accept | Rate limiting is Phase 8 (LAUNCH-05); documented as by-design for this phase |
| T-02-SC | Tampering | NuGet installs | low | accept | No new packages (HttpClient is BCL); no install step in this plan |
</threat_model>

<verification>
- Quick: `dotnet test API/ZachHairStudio.Api.Tests --filter "FullyQualifiedName!~SqlServer"` green (validator, assignment, controller tests).
- Full: `dotnet test API/ZachHairStudio.slnx` green including ConcurrencyTests (SC4) and DstRoundTripTests (SC5) on real SQL Server.
- AppointmentsService catches 2601/2627, opens no manual transaction, and sends email only after commit.
</verification>

<success_criteria>
POST /api/appointments books a real, double-booking-safe appointment (201), rejects invalid input (400), and returns a clean 409 when a slot is taken — proven under real concurrency (SC4). Appointment instants round-trip correctly across both 2026 DST transitions (SC5). The confirmation email is attempted best-effort after commit (BOOK-03/D-11), and Any-stylist assignment is deterministic (BOOK-06/D-07).
</success_criteria>

## Artifacts this phase produces (Plan 04)

- DTO/validator/mapper: `AppointmentCreateDto`, `AppointmentCreateDtoValidator`, `AppointmentResponseDto`, `AppointmentExtensions.ToDto`
- Services: `AppointmentsService.CreateAsync`, `IEmailService`, `ResendEmailService.SendConfirmationAsync`, `IsDuplicateKeyViolation`
- Endpoint: `AppointmentsController` POST `/api/appointments` (201/400/409)
- DI/config: `AddScoped<AppointmentsService>()`, `AddHttpClient<IEmailService, ResendEmailService>`, `Resend:FromEmail`
- Tests: `AppointmentCreateDtoValidatorTests`, `AnyStylistAssignmentTests`, `AppointmentsControllerTests`, `ConcurrencyTests`, `DstRoundTripTests`

<output>
Create `.planning/phases/02-booking-core/02-04-SUMMARY.md` when done. Record the applied booking-horizon/lead-time defaults (owner-reviewable) and the observed SqlException.Number from the concurrency test (expected 2601).
</output>
