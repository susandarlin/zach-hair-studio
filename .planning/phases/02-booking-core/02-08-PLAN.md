---
phase: 02-booking-core
plan: 08
type: execute
wave: 2
depends_on: ["02-07"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [BOOK-03, BOOK-05]
user_setup:
  - service: resend
    why: "D-12: real Resend sends happen in Development AND Testing; a live booking sends a real confirmation email that the human inspects. Requires a verified sending domain (already 'Verified' per 02-06) and the API key present."
    env_vars:
      - name: RESEND_API_KEY
        source: "dotnet user-secrets set RESEND_API_KEY <key> --project API/ZachHairStudio.Api (D-13, never a tracked file)"

must_haves:
  truths:
    - "`dotnet test` runs to completion with zero failures on a fresh run (no leftover build lock, no date-bombed create-path failures) (BOOK-03, BOOK-05)."
    - "A real booking on /book delivers a confirmation email to a real inbox containing all five BOOK-03 fields, including the explicit salon zone label (BOOK-03)."
  artifacts:
    - .planning/phases/02-booking-core/02-08-SUMMARY.md
  key_links:
    - "Live /book booking → Resend delivery → received email carries service, stylist, salon-local time WITH zone label, duration, price (02-VALIDATION.md line 85 bar)"
---

<objective>
Human-verify the three carryover items the 2026-07-10 verification could not close itself, now
that Plan 02-07 has made the suite runnable and the email fix is in place.

The original verification could not run `dotnet test` (a leftover locked `ZachHairStudio.Api.exe`
process held `ZachHairStudio.Shared.dll`), and it flagged two live checks that only a human can do
under D-12's real-send constraint. Plan 02-07 removed the date-bomb that would otherwise fail the
re-run, added the write-path offset proof, and the email fix (ea8eb85) is already in the tree. This
plan is the observation pass that turns "gaps_found" into a clean, human-confirmed close.

Purpose: Obtain a directly-observed green test run and a real-inbox five-field email confirmation.
Output: A SUMMARY recording the observed test result (pass count) and the received-email field checklist.
</objective>

<execution_context>
@D:/repos/vct/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@D:/repos/vct/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-booking-core/02-VERIFICATION.md
@.planning/phases/02-booking-core/02-VALIDATION.md
@API/ZachHairStudio.Shared/Features/Appointments/ResendEmailService.cs
@.claude/skills/dev/SKILL.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 [BLOCKING HUMAN]: Fresh full-suite test run + real booking email inspection</name>
  <what-built>
    Plan 02-07 de-date-bombed the create-path tests, added WritePathOffsetTests (real-SQL offset
    proof), and recorded the SC5 DST descope. The confirmation email already carries all five
    BOOK-03 fields (ResendEmailService.cs, committed ea8eb85) with an offline regression test
    (ResendEmailBodyTests). This checkpoint observes both directly: a fresh full test run, and a
    real booking's delivered email.
  </what-built>
  <read_first>
    - .planning/phases/02-booking-core/02-VERIFICATION.md ("Human Verification Required" section — the three items being closed)
    - .planning/phases/02-booking-core/02-VALIDATION.md (line 85 five-field email bar + the SC5 descope note added by 02-07)
    - .claude/skills/dev/SKILL.md (how to launch the full stack — API + landing-page)
  </read_first>
  <action>
    Observe two things directly, do not infer from prior SUMMARYs: (1) a fresh full `dotnet test` run
    finishing with zero failures, and (2) a real /book booking whose delivered confirmation email carries
    all five BOOK-03 fields. Clear any stale ZachHairStudio.Api.exe build lock first, confirm RESEND_API_KEY
    is set in user-secrets (D-12/D-13), then follow the numbered steps below and record the results.
  </action>
  <how-to-verify>
    1. **Clear any stale lock first.** If a prior session left the API running, a build can fail with
       MSB3027/MSB3021 ("ZachHairStudio.Shared.dll locked by ZachHairStudio.Api.exe"). If so, stop that
       process before building: `Get-Process ZachHairStudio.Api -ErrorAction SilentlyContinue | Stop-Process`.
    2. **Confirm the key is set (D-12/D-13):** `dotnet user-secrets list --project API/ZachHairStudio.Api`
       should show `RESEND_API_KEY`. Without it the test host will not boot.
    3. **Run the full suite fresh:**
       `dotnet test API/ZachHairStudio.Api.Tests/ZachHairStudio.Api.Tests.csproj`
       Expect: zero failures. (The passing count now exceeds the historical 94 — Phase 3 added Identity,
       Schedule, and StatusUpdate tests since that figure, and 02-07 added WritePathOffsetTests.) Record the
       observed passed/failed/skipped counts in the SUMMARY.
    4. **Book a real appointment end-to-end:** launch the stack (dev skill), open `/book`, pick Precision Cut,
       pick a slot, enter YOUR real email, and confirm.
    5. **Inspect the received email.** Confirm it contains all five fields per 02-VALIDATION.md line 85:
       service name, stylist name, salon-local time WITH an explicit zone label (GMT+06:30 for Asia/Yangon),
       duration ("45 min"), and price. Note any field missing or mislabeled.
  </how-to-verify>
  <verify>
    <human-check>Human reports zero test failures (with counts) AND a received email showing all five BOOK-03 fields including the GMT+06:30 zone label.</human-check>
  </verify>
  <done>A human has directly observed a green full-suite run and a real confirmation email carrying all five fields — closing the three "Human Verification Required" items in 02-VERIFICATION.md.</done>
  <resume-signal>Type "approved" with the observed test counts and a five-field email checklist, or describe any failure/missing field.</resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces

- `.planning/phases/02-booking-core/02-08-SUMMARY.md` — records: (1) observed `dotnet test` pass/fail/skip counts from a fresh run, (2) the received-email five-field checklist, (3) confirmation that all three "Human Verification Required" items from 02-VERIFICATION.md are closed.

No source files are modified by this plan (verification-only checkpoint).
</artifacts_produced>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan runs existing tests and drives the existing /book flow. The only
external interaction is the already-shipped Resend send (client booking fields → email HTML), whose
injection mitigation (T-02-08, HtmlEncode) is unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 (pre-existing) | Injection | ResendEmailService HTML body | medium | mitigate | Unchanged; verification only. The human check confirms rendered fields, not raw injection — encoding remains covered by ResendEmailBodyTests. |
| T-2-08-SC | Tampering | npm/pip/cargo/NuGet installs | high | accept | No installs; verification-only plan. |
</threat_model>

<verification>
- Fresh `dotnet test` completes with zero failures, observed directly (not inferred from prior SUMMARYs).
- A real booking's confirmation email is received and shows all five BOOK-03 fields including the zone label.
- All three items in 02-VERIFICATION.md "Human Verification Required" are marked closed in the SUMMARY.
</verification>

<success_criteria>
- Human confirms a green full-suite run with recorded counts.
- Human confirms the received email carries service, stylist, salon-local time + zone label, duration, and price.
- The phase's one blocking gap (email content) and the two behavior/human-verify items are recorded as closed.
</success_criteria>

<output>
Create `.planning/phases/02-booking-core/02-08-SUMMARY.md` when done.
</output>
