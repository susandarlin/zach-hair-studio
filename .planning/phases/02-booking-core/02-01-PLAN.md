---
phase: 02-booking-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - API/ZachHairStudio.Shared/Features/Stylists/Stylist.cs
  - API/ZachHairStudio.Shared/Features/Stylists/StylistResponseDto.cs
  - API/ZachHairStudio.Shared/Features/Stylists/StylistExtensions.cs
  - API/ZachHairStudio.Shared/Features/Stylists/StylistsService.cs
  - API/ZachHairStudio.Shared/Features/Availability/StylistWorkingHours.cs
  - API/ZachHairStudio.Shared/Features/Availability/StylistTimeOff.cs
  - API/ZachHairStudio.Shared/Features/Appointments/Appointment.cs
  - API/ZachHairStudio.Shared/Features/Appointments/AppointmentSlot.cs
  - API/ZachHairStudio.Shared/Features/Appointments/AppointmentStatus.cs
  - API/ZachHairStudio.Api/Controllers/StylistsController.cs
  - API/ZachHairStudio.Shared/Db/BookingDbContext.cs
  - API/ZachHairStudio.Api/Program.cs
  - API/ZachHairStudio.Api/appsettings.json
  - API/ZachHairStudio.Api/appsettings.Development.json
  - API/ZachHairStudio.Api.Tests/Features/Stylists/StylistsControllerTests.cs
  - API/ZachHairStudio.Shared/Migrations
autonomous: true
requirements: [BOOK-04, BOOK-05, BOOK-06]
user_setup: []
must_haves:
  truths:
    - "GET /api/stylists returns the seeded active stylists ordered by DisplayOrder (D-05, BOOK-06)"
    - "StylistsController's constructor takes StylistsService and does NOT take BookingDbContext (PLAT-01)"
    - "The database has five new tables — Stylists, StylistWorkingHours, StylistTimeOff, Appointments, AppointmentSlots — and no longer has a Bookings table (D-14)"
    - "AppointmentSlots has a UNIQUE index on (StylistId, SlotStart) with NO filter predicate, and SlotStart is a datetimeoffset column (D-03, D-04, BOOK-04)"
    - "Appointment.StartsAt and the availability time columns are stored as datetimeoffset (DateTimeOffset), not datetime2 (D-16, BOOK-05)"
    - "The AddBookingCore migration applies cleanly to (localdb)\\MSSQLLocalDB and the API still builds and boots"
  prohibitions:
    - statement: "StylistsController does not inject or reference BookingDbContext"
      status: pending
      verification: "StylistsController.cs constructor takes only StylistsService; grep of StylistsController.cs shows no BookingDbContext"
    - statement: "The unique index on AppointmentSlots has NO filter predicate (no WHERE / HasFilter)"
      status: pending
      verification: "The generated AddBookingCore migration's CreateIndex for AppointmentSlots has unique: true and no filter: argument; BookingDbContext HasIndex(...).IsUnique() has no HasFilter(...)"
    - statement: "No Booking entity, Bookings DbSet, BookingsController, or Admin BookingController survives the phase (retired wholesale, not half-migrated)"
      status: pending
      verification: "grep for 'class Booking'/'DbSet<Booking>'/'BookingsController'/'Admin.Controllers.BookingController' returns nothing; dotnet build API/ZachHairStudio.slnx succeeds"
    - statement: "Seeding uses HasData in OnModelCreating, NOT UseSeeding/UseAsyncSeeding"
      status: pending
      verification: "BookingDbContext.cs and Program.cs contain no UseSeeding/UseAsyncSeeding; seed rows declared via HasData"
  artifacts:
    - API/ZachHairStudio.Shared/Features/Stylists/Stylist.cs
    - API/ZachHairStudio.Shared/Features/Appointments/AppointmentSlot.cs
    - API/ZachHairStudio.Api/Controllers/StylistsController.cs
    - API/ZachHairStudio.Shared/Migrations (AddBookingCore migration)
  key_links:
    - "AppointmentSlot's (StylistId, SlotStart) unique index IS the SC4 double-booking guarantee — Plan 04's retry loop depends on this index existing at the DB level"
    - "The five new DbSets feed SlotService (Plan 03) and AppointmentsService (Plan 04); the Salon:IanaTimeZoneId config feeds every DateTimeOffset conversion"
    - "Seeded Stylist + StylistWorkingHours rows feed the open-slot query (Plan 03) and the stylist picker (Plan 05)"
---

## Phase Goal

**As a** client, **I want to** pick a service and book a real open slot with my chosen stylist, **so that** my appointment is confirmed and never double-booked.

> Vertical slice, part 1 of the phase. Lays the booking data model and the DB-level double-booking guarantee (the unique index), retires the legacy free-text `Booking` wholesale (D-14), and ships one observable end-to-end read path — `GET /api/stylists` returns the seeded team. The heavy correctness work (slot query, retry loop, concurrency proof) rides on the schema this plan creates.

<objective>
Create the booking domain model (Stylist, availability, Appointment + AppointmentSlot with the unfiltered unique index), seed stylists and default working hours via HasData, expose a thin read-only `StylistsController`, wire the `Salon:IanaTimeZoneId` config, retire the legacy `Booking` entity and every consumer of it (API + Admin), and apply the whole thing as one BLOCKING EF Core migration.

Purpose: Establish the schema and the database-enforced double-booking guarantee (SC4) that all later plans depend on, and remove the free-text booking path so nothing half-migrated remains (D-14).
Output: Five new tables (one dropped), a seeded stylist list endpoint, the `AddBookingCore` migration applied to LocalDB, and the salon timezone configured.
</objective>

<execution_context>
@D:/repos/vct/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@D:/repos/vct/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-booking-core/02-CONTEXT.md
@.planning/phases/02-booking-core/02-RESEARCH.md
@.planning/phases/02-booking-core/02-PATTERNS.md
@.claude/skills/ef-migrations/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the booking domain entities and the Stylist read DTO/mapper</name>
  <files>
    API/ZachHairStudio.Shared/Features/Stylists/Stylist.cs,
    API/ZachHairStudio.Shared/Features/Stylists/StylistResponseDto.cs,
    API/ZachHairStudio.Shared/Features/Stylists/StylistExtensions.cs,
    API/ZachHairStudio.Shared/Features/Availability/StylistWorkingHours.cs,
    API/ZachHairStudio.Shared/Features/Availability/StylistTimeOff.cs,
    API/ZachHairStudio.Shared/Features/Appointments/Appointment.cs,
    API/ZachHairStudio.Shared/Features/Appointments/AppointmentSlot.cs,
    API/ZachHairStudio.Shared/Features/Appointments/AppointmentStatus.cs
  </files>
  <read_first>
    API/ZachHairStudio.Shared/Features/Services/Service.cs,
    API/ZachHairStudio.Shared/Features/Services/ServiceResponseDto.cs,
    API/ZachHairStudio.Shared/Features/Services/ServiceExtensions.cs,
    API/ZachHairStudio.Shared/Features/Bookings/Booking.cs,
    API/ZachHairStudio.Shared/Features/Bookings/BookingStatus.cs,
    .planning/phases/02-booking-core/02-PATTERNS.md
  </read_first>
  <action>
    Create the new feature folders and POCOs mirroring the Services feature style (plain classes, DataAnnotations, file-scoped namespaces).

    Stylist (namespace ZachHairStudio.Shared.Features.Stylists): int Id; [Required,StringLength(150)] Slug; [Required,StringLength(150)] Name; bool IsActive = true; int DisplayOrder. Per D-05.

    StylistResponseDto: Id, Slug, Name, DisplayOrder (no IsActive — mirrors ServiceResponseDto excluding internal fields). StylistExtensions: static ToDto(this Stylist) mirroring ServiceExtensions.ToDto.

    StylistWorkingHours (namespace ZachHairStudio.Shared.Features.Availability): int Id; int StylistId; System.DayOfWeek DayOfWeek; TimeOnly StartTime; TimeOnly EndTime. Recurring weekly hours per D-06.

    StylistTimeOff (namespace ZachHairStudio.Shared.Features.Availability): int Id; int StylistId; DateTimeOffset StartsAt; DateTimeOffset EndsAt; [StringLength(200)] string? Reason. Exceptions/holidays per D-06. StartsAt/EndsAt MUST be DateTimeOffset (BOOK-05).

    AppointmentStatus enum (namespace ZachHairStudio.Shared.Features.Appointments): Confirmed, Cancelled, Completed, NoShow — define all four now to avoid a later enum migration (Phase 3 adds the status transitions; D-04 notes NoShow is terminal and releases the slot exactly like Cancelled). Mirror the BookingStatus stored-as-string pattern.

    Appointment (namespace ZachHairStudio.Shared.Features.Appointments): int Id; int ServiceId; int StylistId; DateTimeOffset StartsAt; AppointmentStatus Status = Confirmed; client contact fields matching Booking.cs bounds — [Required,StringLength(100)] FirstName; [Required,StringLength(100)] LastName; [Required,EmailAddress,StringLength(150)] Email; [Phone,StringLength(30)] string? Phone; DateTimeOffset CreatedAt; and a List&lt;AppointmentSlot&gt; Slots = new() navigation. StartsAt MUST be DateTimeOffset (BOOK-05, D-16).

    AppointmentSlot (namespace ZachHairStudio.Shared.Features.Appointments): int Id; int AppointmentId; Appointment Appointment (nav); int StylistId; DateTimeOffset SlotStart. One row per occupied 15-minute grid cell (D-01, D-03). The composite unique index and datetimeoffset column type are configured in BookingDbContext in Task 2, not here.

    Do NOT delete anything yet and do NOT modify BookingDbContext yet — these are additive so the solution still builds after this task.
  </action>
  <verify>
    <automated>dotnet build API/ZachHairStudio.slnx</automated>
  </verify>
  <acceptance_criteria>
    - Stylist.cs, StylistResponseDto.cs, StylistExtensions.cs, StylistWorkingHours.cs, StylistTimeOff.cs, Appointment.cs, AppointmentSlot.cs, AppointmentStatus.cs all exist under the stated namespaces.
    - Appointment.StartsAt, StylistTimeOff.StartsAt, StylistTimeOff.EndsAt, and AppointmentSlot.SlotStart are declared as DateTimeOffset (grep confirms `DateTimeOffset StartsAt` in Appointment.cs and `DateTimeOffset SlotStart` in AppointmentSlot.cs).
    - AppointmentStatus contains exactly the members Confirmed, Cancelled, Completed, NoShow.
    - `dotnet build API/ZachHairStudio.slnx` succeeds (additive-only; nothing removed yet).
  </acceptance_criteria>
  <done>All eight files compile as part of the solution; no existing file modified; DateTimeOffset used for every appointment/availability instant.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the DbContext (DbSets, unfiltered unique index, HasData seed), the StylistsService/Controller + DI + Salon config, and retire the legacy Booking path (API + Admin)</name>
  <files>
    API/ZachHairStudio.Shared/Db/BookingDbContext.cs,
    API/ZachHairStudio.Shared/Features/Stylists/StylistsService.cs,
    API/ZachHairStudio.Api/Controllers/StylistsController.cs,
    API/ZachHairStudio.Api/Program.cs,
    API/ZachHairStudio.Api/appsettings.json,
    API/ZachHairStudio.Api/appsettings.Development.json,
    API/ZachHairStudio.Api.Tests/Features/Stylists/StylistsControllerTests.cs
  </files>
  <read_first>
    API/ZachHairStudio.Shared/Db/BookingDbContext.cs,
    API/ZachHairStudio.Shared/Features/Services/ServicesService.cs,
    API/ZachHairStudio.Api/Controllers/ServicesController.cs,
    API/ZachHairStudio.Api/Program.cs,
    API/ZachHairStudio.Api/appsettings.json,
    API/ZachHairStudio.Api.Tests/CustomWebApplicationFactory.cs,
    API/ZachHairStudio.Api.Tests/Features/Services/ServicesControllerTests.cs,
    landing-page/lib/data.ts,
    API/ZachHairStudio.Admin/Controllers/BookingController.cs
  </read_first>
  <action>
    Modify BookingDbContext: remove the `DbSet&lt;Booking&gt; Bookings` property and the entire `modelBuilder.Entity&lt;Booking&gt;(...)` block (D-14). Add DbSets for Stylist, StylistWorkingHours, StylistTimeOff, Appointment, AppointmentSlot. In OnModelCreating add:
      - Stylist: Slug/Name HasMaxLength(150); HasIndex(Slug).IsUnique(); HasData seeded from landing-page/lib/data.ts `team` (Zin Min, May Yoon, Thiri Cho, Sai Min Htet → Ids 1-4, slug kebab-case of name, IsActive true, DisplayOrder by array index) per D-05.
      - StylistWorkingHours: HasData a sensible default weekly schedule per active stylist — Tuesday–Saturday 09:00–18:00 — with explicit Ids. This is an OWNER-REVIEWABLE placeholder (flag it in the SUMMARY exactly as Phase 1 flagged seed prices, per RESEARCH A4 / D-15 precedent). Store DayOfWeek and TimeOnly with EF defaults.
      - AppointmentSlot: entity.HasIndex(s =&gt; new { s.StylistId, s.SlotStart }).IsUnique() with NO HasFilter — the unfiltered index IS the SC4/BOOK-04 guarantee and D-04 requires no filter predicate. entity.Property(s =&gt; s.SlotStart).HasColumnType("datetimeoffset(0)").
      - Appointment: Status HasConversion&lt;string&gt;().HasMaxLength(50) (mirror the old Booking.Status config); FirstName/LastName HasMaxLength(100); Email HasMaxLength(150); Phone HasMaxLength(30); relationship Appointment HasMany(Slots) WithOne(Appointment) with cascade delete so cancelling deletes slot rows (D-04).

    Create StylistsService (mirror ServicesService read shape): constructor injects BookingDbContext only; GetActiveStylistsAsync() => Stylists.Where(IsActive).OrderBy(DisplayOrder).Select(ToDto).ToListAsync().

    Create StylistsController (mirror the GET-only shape of ServicesController): [ApiController][Route("api/[controller]")]; constructor injects StylistsService only (NOT BookingDbContext — PLAT-01); single [HttpGet] returning Ok(await GetActiveStylistsAsync()).

    Program.cs: add builder.Services.AddScoped&lt;StylistsService&gt;() next to the existing AddScoped&lt;ServicesService&gt;(). Bind the salon timezone: read Salon:IanaTimeZoneId (a Salon options class or GetSection("Salon")) so later plans resolve it. Do NOT add any Resend key here (that is Plan 04 / user-secrets).

    appsettings.json and appsettings.Development.json: add a "Salon" section with "IanaTimeZoneId": "America/New_York" (D-16). Never put secrets here.

    StylistsControllerTests.cs (mirror ServicesControllerTests, use CustomWebApplicationFactory / InMemory): GET /api/stylists returns 200 and 4 seeded stylists ordered by DisplayOrder; add a reflection assertion that StylistsController has no constructor parameter of type BookingDbContext (PLAT-01).

    Retire the legacy Booking path wholesale (D-14) so the solution still compiles after the DbSet is gone:
      - Delete API/ZachHairStudio.Shared/Features/Bookings/ (Booking.cs, BookingCreateDto.cs, BookingResponseDto.cs, BookingExtensions.cs, BookingStatus.cs).
      - Delete API/ZachHairStudio.Api/Controllers/BookingsController.cs.
      - Delete the legacy Admin consumers that reference the deleted entity (a required consequence of D-14 — without this the Admin project fails to build and breaks `dotnet build API/ZachHairStudio.slnx`): delete API/ZachHairStudio.Admin/Controllers/BookingController.cs, delete API/ZachHairStudio.Admin/Views/Booking/Index.cshtml and Details.cshtml, and remove the "View Bookings" anchor (the `asp-controller="Booking"` link) from API/ZachHairStudio.Admin/Views/Home/Index.cshtml. NOTE in the SUMMARY: full retirement of the ZachHairStudio.Admin scaffold remains Phase 8 scope; this deletes only the Booking-coupled files needed to keep the build green.
  </action>
  <verify>
    <automated>dotnet build API/ZachHairStudio.slnx && dotnet test API/ZachHairStudio.Api.Tests --filter "FullyQualifiedName~StylistsControllerTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build API/ZachHairStudio.slnx` succeeds with the Booking entity, BookingsController, and Admin BookingController all deleted.
    - grep for `DbSet<Booking>` and `Entity<Booking>` in BookingDbContext.cs returns nothing.
    - BookingDbContext.cs contains `HasIndex(` over `StylistId` and `SlotStart` with `.IsUnique()` and NO `.HasFilter(`.
    - BookingDbContext.cs contains `HasColumnType("datetimeoffset(0)")` on the AppointmentSlot SlotStart property.
    - appsettings.json and appsettings.Development.json each contain a Salon section with IanaTimeZoneId set to America/New_York.
    - `dotnet test --filter FullyQualifiedName~StylistsControllerTests` exits 0 (GET returns 4 seeded stylists; PLAT-01 reflection assertion passes).
    - grep of StylistsController.cs shows no BookingDbContext.
  </acceptance_criteria>
  <done>DbContext exposes the five new sets with the unfiltered unique index and datetimeoffset column; GET /api/stylists returns the seeded team; the Salon timezone is configured; every Booking-coupled file (API + Admin) is gone and the solution builds.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: Add and apply the AddBookingCore EF Core migration (creates the double-booking guarantee at the DB level)</name>
  <files>API/ZachHairStudio.Shared/Migrations</files>
  <read_first>
    .claude/skills/ef-migrations/SKILL.md,
    API/ZachHairStudio.Shared/Migrations/20260707190502_AddServices.cs,
    API/ZachHairStudio.Api/Program.cs
  </read_first>
  <action>
    This is the mandatory schema-push gate. Program.cs already calls db.Database.Migrate() at startup (skipped in the Testing environment) — migrations already own the schema, so no EnsureCreated()→Migrate() switch is needed; confirm that is still the case after Task 2's Program.cs edits.

    From the repo root run the ef-migrations skill commands: `dotnet ef migrations add AddBookingCore --project API/ZachHairStudio.Shared --startup-project API/ZachHairStudio.Api`, then `dotnet ef database update --project API/ZachHairStudio.Shared --startup-project API/ZachHairStudio.Api` against (localdb)\MSSQLLocalDB.

    Inspect the generated migration file before applying: it MUST DropTable(name: "Bookings"), CreateTable for Stylists, StylistWorkingHours, StylistTimeOff, Appointments, AppointmentSlots, and CreateIndex for AppointmentSlots over columns { StylistId, SlotStart } with unique: true and NO filter: argument (D-03, D-04, BOOK-04). It MUST also contain InsertData for the seeded Stylist and StylistWorkingHours rows.

    Per RESEARCH Open Question 1, note in the SUMMARY that EF Core's HasIndex().IsUnique() emits CREATE UNIQUE INDEX (SQL Server error 2601 on violation), which corrects CONTEXT.md D-03's stated 2627 — Plan 04 catches both defensively. Do not add 2601/2627 to EnableRetryOnFailure errorNumbersToAdd (they are data-integrity, not transient, errors).
  </action>
  <verify>
    <automated>dotnet ef database update --project API/ZachHairStudio.Shared --startup-project API/ZachHairStudio.Api && dotnet build API/ZachHairStudio.slnx</automated>
  </verify>
  <acceptance_criteria>
    - A new migration file matching `API/ZachHairStudio.Shared/Migrations/*_AddBookingCore.cs` exists.
    - That migration contains `unique: true` on a CreateIndex over `"StylistId", "SlotStart"` and does NOT contain a `filter:` argument on that index.
    - That migration contains `DropTable(` referencing `"Bookings"`.
    - That migration contains InsertData for the `Stylists` table (4 rows) and the `StylistWorkingHours` table.
    - `dotnet ef database update` completes without error against (localdb)\MSSQLLocalDB.
  </acceptance_criteria>
  <done>The AddBookingCore migration is generated, verified to contain the unfiltered unique index and the Bookings drop, and applied to the dev database; the API builds and boots on the new schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (GET /api/stylists) | Unauthenticated read of public stylist list (guest booking, same posture as Phase 1) |
| API → SQL Server | EF Core writes/reads; the AppointmentSlots unique index is the integrity boundary for SC4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | AppointmentSlots (StylistId, SlotStart) | critical | mitigate | Unfiltered composite UNIQUE index created in this migration is the DB-level double-booking guarantee (D-03, BOOK-04); Plan 04 translates the violation to 409 |
| T-02-02 | Information Disclosure | GET /api/stylists | low | accept | Public marketing data (names already on the homepage); no PII exposed. Endpoint unauthenticated/unthrottled by design until Phase 8 (LAUNCH-05) |
| T-02-SC | Tampering | npm/NuGet installs | low | accept | No new NuGet packages in this plan; no `dotnet add package` runs. Legitimacy audit in RESEARCH shows zero new third-party dependencies |
</threat_model>

<verification>
- `dotnet build API/ZachHairStudio.slnx` green (Booking retired, five entities added).
- `dotnet test API/ZachHairStudio.Api.Tests --filter "FullyQualifiedName!~SqlServer"` green (StylistsControllerTests included).
- `dotnet ef database update` applies AddBookingCore to LocalDB; migration file contains the unfiltered unique index and the Bookings drop.
- grep confirms no `class Booking`, `DbSet<Booking>`, `BookingsController`, or `Admin.Controllers.BookingController` remain.
</verification>

<success_criteria>
GET /api/stylists returns the four seeded stylists; the AppointmentSlots table carries an unfiltered UNIQUE(StylistId, SlotStart) index over datetimeoffset columns; the Bookings table and all its consumers are gone; the salon timezone is configured. The schema is ready for the slot query (Plan 03) and the booking write (Plan 04).
</success_criteria>

## Artifacts this phase produces (Plan 01)

New symbols/files created by this plan (exclude from external-drift verification):
- Entities: `Stylist`, `StylistWorkingHours`, `StylistTimeOff`, `Appointment`, `AppointmentSlot`; enum `AppointmentStatus`
- DTO/mapper: `StylistResponseDto`, `StylistExtensions.ToDto`
- Service/controller: `StylistsService`, `StylistsController` (GET `/api/stylists`)
- DbContext: `DbSet<Stylist> Stylists`, `DbSet<StylistWorkingHours> StylistWorkingHours`, `DbSet<StylistTimeOff> StylistTimeOff`, `DbSet<Appointment> Appointments`, `DbSet<AppointmentSlot> AppointmentSlots`; unique index `IX_AppointmentSlots_StylistId_SlotStart`
- Config: `Salon:IanaTimeZoneId`
- Migration: `AddBookingCore`
- Test: `StylistsControllerTests`
- Removed (retired, D-14): `Booking`, `BookingStatus`, `BookingCreateDto`, `BookingResponseDto`, `BookingExtensions`, `BookingsController`, `Admin.Controllers.BookingController`, `Admin/Views/Booking/*`

<output>
Create `.planning/phases/02-booking-core/02-01-SUMMARY.md` when done. Flag the seeded default working-hours schedule (Tue–Sat 09:00–18:00) as an owner-reviewable placeholder, and record that the generated unique index raises SQL error 2601 (correcting D-03's 2627).
</output>
