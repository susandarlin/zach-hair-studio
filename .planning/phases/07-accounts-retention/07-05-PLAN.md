---
phase: 07-accounts-retention
plan: 05
type: execute
wave: 5
depends_on:
  - 07-02
  - 07-03
files_modified:
  - API/ZachHairStudio.Shared/Features/Appointments/AppointmentsService.cs
  - API/ZachHairStudio.Api/Controllers/AppointmentsController.cs
  - API/ZachHairStudio.Api.Tests/Features/Account/ClientOwnedBookingCreateTests.cs
  - landing-page/lib/appointments.ts
  - landing-page/components/ClaimHistoryPanel.tsx
  - landing-page/app/account/bookings/page.tsx
autonomous: true
gap_closure: true
requirements:
  - ACCT-01
  - ACCT-02
  - ACCT-03
  - ACCT-04
  - ACCT-05
  - ACCT-06
  - ACCT-07
must_haves:
  truths:
    - "When a Client JWT Bearer is present on POST /api/appointments, the new Appointment.ClientUserId equals that client's NameIdentifier (register→book owns the row)"
    - "Anonymous (guest) POST /api/appointments still creates with ClientUserId null — guest checkout/booking path unchanged"
    - "A Client-owned booking created via public POST appears in GET /api/account/bookings and can be cancelled via POST /api/account/bookings/{id}/cancel (ACCT-02, ACCT-04)"
    - "Staff JWT or non-Client principal on public create does not set ClientUserId (only Client role + NameIdentifier; D-08 / D-12)"
    - "Landing /book attaches Client Bearer when zhs.client.auth session exists so logged-in clients own new appointments without a separate claim step"
    - "Account Bookings shell can re-run D-04 claim-confirm for guest-email rows still unattached (does not trap users when preview is empty)"
  artifacts:
    - API/ZachHairStudio.Shared/Features/Appointments/AppointmentsService.cs (CreateAsync accepts optional clientUserId)
    - API/ZachHairStudio.Api/Controllers/AppointmentsController.cs (TryGetClientUserId → CreateAsync)
    - API/ZachHairStudio.Api.Tests/Features/Account/ClientOwnedBookingCreateTests.cs
    - landing-page/lib/appointments.ts (optional Bearer on createAppointment)
    - landing-page/components/ClaimHistoryPanel.tsx (embedded/account-shell mode)
    - landing-page/app/account/bookings/page.tsx (claim entry + refresh)
  key_links:
    - "AppointmentsController must resolve ownership solely from ClaimTypes.NameIdentifier + Client role — never from request body (mirror OrdersController.TryGetClientUserId; D-08)"
    - "CreateAsync must forward clientUserId into TryBookNewAsync (already sets Appointment.ClientUserId when HasValue) — stop hard-coding null at the public create seam"
    - "landing-page createAppointment must send Authorization Bearer from getToken() when session present (same pattern as cart.ts checkoutHeaders)"
    - "Cancel/list path already ownership-gated; this plan only establishes ClientUserId at create so ACCT-04 UI can see the row"
---

## Phase Goal

**As a** client, **I want to** book while logged in and then cancel or reschedule that appointment from my account, **so that** I do not have to call the salon for schedule changes I can handle myself.

> Gap closure (MVP) — closes VERIFICATION failed must-have: self-service blocked because public create always books with `clientUserId: null`. Does not invent LocalDB human UAT for loyalty (`behavior_unverified`); ownership fix also unblocks earn for post-login bookings (ACCT-07).

## Source audit (this plan)

| SOURCE | ID | Item | Status |
|--------|-----|------|--------|
| GOAL | — | Manage upcoming appointments myself (self-service) | COVERED |
| REQ | ACCT-01 | Create account + log in | COVERED (prior 07-01; no regression — auth used by test) |
| REQ | ACCT-02 | Booking history | COVERED (owned create → list) |
| REQ | ACCT-03 | Order history | COVERED (prior 07-02; out of this gap — listed for phase req completeness) |
| REQ | ACCT-04 | Cancel/reschedule self-service | COVERED (primary gap) |
| REQ | ACCT-05 | Single Identity schema | COVERED (prior 07-01; Client role reused) |
| REQ | ACCT-06 | IDOR prevention | COVERED (NameIdentifier-only attach; Staff ignored) |
| REQ | ACCT-07 | Loyalty earn/redeem | COVERED (earn needs ClientUserId — create attach unblocks; no new ledger work) |
| CONTEXT | D-04 | Claim-by-email confirm when matches | COVERED (re-claim on account bookings shell) |
| CONTEXT | D-08 | Server ownership only; never trust body owner ids | COVERED |
| CONTEXT | D-09–D-12 | Cancel/reschedule ownership gates | COVERED (exercise existing 07-03 cancel after owned create) |
| RESEARCH | A2 / ownership column | Nullable ClientUserId set on create when Client JWT | COVERED |
| VERIFICATION | Gap | Public CreateAsync null ownership | COVERED |

Deferred elsewhere (not gaps): OAuth/RETN/refresh (CONTEXT Deferred); loyalty LocalDB human UAT (`behavior_unverified` — do not invent plans).

<objective>
Close the Phase 7 ownership gap so register→book→account-manage works end-to-end (ACCT-02, ACCT-04; unblocks ACCT-07 earn on those visits).

Purpose: Public POST /api/appointments currently hard-codes `clientUserId: null`, so logged-in clients never own new bookings and cannot cancel/reschedule/earn.
Output: Client-owned create path (API + landing Bearer) + regression test + optional claim re-entry on /account/bookings.
Decision IDs: D-04, D-08, D-09, D-12 (exercise); ACCT-01–ACCT-07 phase coverage.
</objective>

<execution_context>
@/workspaces/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-accounts-retention/07-CONTEXT.md
@.planning/phases/07-accounts-retention/07-RESEARCH.md
@.planning/phases/07-accounts-retention/07-PATTERNS.md
@.planning/phases/07-accounts-retention/07-VERIFICATION.md
@.planning/phases/07-accounts-retention/07-02-SUMMARY.md
@.planning/phases/07-accounts-retention/07-03-SUMMARY.md

@API/ZachHairStudio.Api/Controllers/AppointmentsController.cs
@API/ZachHairStudio.Api/Controllers/OrdersController.cs
@API/ZachHairStudio.Shared/Features/Appointments/AppointmentsService.cs
@API/ZachHairStudio.Api.Tests/Features/Account/AccountBookingsTests.cs
@API/ZachHairStudio.Api.Tests/Features/Account/ClientRescheduleTests.cs
@API/ZachHairStudio.Api.Tests/Features/Identity/ClientAuthTests.cs
@API/ZachHairStudio.Api.Tests/TestSupport/BookingDates.cs
@landing-page/lib/appointments.ts
@landing-page/lib/auth.ts
@landing-page/lib/cart.ts
@landing-page/components/ClaimHistoryPanel.tsx
@landing-page/app/account/bookings/page.tsx
@landing-page/app/account/register/page.tsx
</context>

## Artifacts this phase produces

New/updated by THIS gap-closure plan (07-05):
- `AppointmentsService.CreateAsync(AppointmentCreateDto request, int? clientUserId = null)` — forward `clientUserId` into existing `TryBookNewAsync` (stop hard-coding null); anonymous callers keep null (guest path)
- `AppointmentsController.CreateAppointment` — optional Client JWT resolution mirroring `OrdersController.TryGetClientUserId` (Client role + `ClaimTypes.NameIdentifier` only; Staff/anonymous → null); pass into `CreateAsync` (D-08)
- Integration test class `ClientOwnedBookingCreateTests` — SqlServerWebApplicationFactory; Client JWT books → `ClientUserId` set → appears in `GET /api/account/bookings` → cancel succeeds; guest create still null; Staff Bearer does not own
- `landing-page/lib/appointments.ts` `createAppointment` — when `getToken()` present, set Authorization Bearer (cart checkoutHeaders pattern); Content-Type JSON unchanged
- `ClaimHistoryPanel` embedded/account-shell mode — empty preview renders nothing (no forced navigate away from bookings); confirm/skip refreshes parent list via callback (D-04)
- `/account/bookings` mounts embedded claim panel above the list and reloads bookings after claim

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Client JWT book → own → list → cancel regression</name>
  <files>API/ZachHairStudio.Api.Tests/Features/Account/ClientOwnedBookingCreateTests.cs</files>
  <read_first>
    - .planning/phases/07-accounts-retention/07-VERIFICATION.md (failed must-have + missing items)
    - API/ZachHairStudio.Api.Tests/Features/Account/AccountBookingsTests.cs (Client register/JWT + GET /api/account/bookings patterns)
    - API/ZachHairStudio.Api.Tests/Features/Account/ClientRescheduleTests.cs (cancel route + ownership asserts)
    - API/ZachHairStudio.Api.Tests/Features/Identity/ClientAuthTests.cs (EnsureClientRoleAsync + register)
    - API/ZachHairStudio.Api.Tests/Features/Appointments/StatusUpdateTests.cs (anonymous POST /api/appointments body shape)
    - API/ZachHairStudio.Api.Tests/TestSupport/BookingDates.cs (NextBookableSlot / SlotOn)
    - API/ZachHairStudio.Api/Controllers/OrdersController.cs (TryGetClientUserId contract to mirror)
  </read_first>
  <behavior>
    - Register Client → Authorization Bearer on POST /api/appointments with valid future slot → 201; response ClientUserId equals authenticated user id (or DB row ClientUserId matches NameIdentifier)
    - Same Client GET /api/account/bookings includes that appointment id (ACCT-02)
    - Same Client POST /api/account/bookings/{id}/cancel → 200; status Cancelled (ACCT-04 / D-09)
    - Anonymous POST /api/appointments (no Bearer) → 201 with ClientUserId null (guest path preserved)
    - Staff-role JWT Bearer on POST /api/appointments → 201 with ClientUserId null (only Client role attaches; D-08 / D-12)
  </behavior>
  <action>
    Create `ClientOwnedBookingCreateTests` under Features/Account using SqlServerWebApplicationFactory only (RESEARCH Pitfall 1 — never InMemory). Reuse Client role ensure + register/login JWT helpers from AccountBookingsTests / ClientAuthTests. Use BookingDates for a bookable StartsAt and a seeded stylist/service id consistent with other appointment create tests.

    Write failing integration coverage for the behaviors above against the live public create route and existing account cancel route. Prefer asserting ownership via response DTO ClientUserId and/or reloading through GET /api/account/bookings — do not seed ClientUserId in the DB for the happy path (the gap is that create must set it).

    Do not change AppointmentsController or CreateAsync in this task — RED must fail while CreateAsync still passes null into TryBookNewAsync.
  </action>
  <verify>
    <automated>test -f API/ZachHairStudio.Api.Tests/Features/Account/ClientOwnedBookingCreateTests.cs &amp;&amp; grep -q "ClientOwnedBookingCreateTests" API/ZachHairStudio.Api.Tests/Features/Account/ClientOwnedBookingCreateTests.cs &amp;&amp; grep -q "/api/appointments" API/ZachHairStudio.Api.Tests/Features/Account/ClientOwnedBookingCreateTests.cs &amp;&amp; grep -q "/api/account/bookings" API/ZachHairStudio.Api.Tests/Features/Account/ClientOwnedBookingCreateTests.cs &amp;&amp; grep -q "SqlServerWebApplicationFactory" API/ZachHairStudio.Api.Tests/Features/Account/ClientOwnedBookingCreateTests.cs &amp;&amp; echo RED-READY</automated>
  </verify>
  <acceptance_criteria>
    - ClientOwnedBookingCreateTests.cs exists with SqlServerWebApplicationFactory
    - Test file exercises POST /api/appointments under Client Bearer and asserts account list + cancel path
    - Guest-null and Staff-non-attach cases are named in the test class
  </acceptance_criteria>
  <done>Failing regression defines owned-create → list → cancel contract (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Attach Client NameIdentifier on public appointment create</name>
  <files>API/ZachHairStudio.Shared/Features/Appointments/AppointmentsService.cs, API/ZachHairStudio.Api/Controllers/AppointmentsController.cs</files>
  <read_first>
    - API/ZachHairStudio.Shared/Features/Appointments/AppointmentsService.cs (CreateAsync hard-codes clientUserId null; TryBookNewAsync already applies ClientUserId when HasValue; RescheduleForClientAsync already passes clientUserId into TryBookNewAsync)
    - API/ZachHairStudio.Api/Controllers/AppointmentsController.cs (CreateAppointment currently calls CreateAsync(request) only)
    - API/ZachHairStudio.Api/Controllers/OrdersController.cs (private TryGetClientUserId — authenticated + Client role + parse NameIdentifier)
    - API/ZachHairStudio.Shared/Features/Identity/StaffRoles.cs
    - API/ZachHairStudio.Shared/Features/Appointments/AppointmentExtensions.cs (ClientUserId on response DTO)
    - Any direct CreateAsync call sites under API/ZachHairStudio.Api.Tests (optional clientUserId default must keep unit/service tests compiling)
  </read_first>
  <action>
    Change `AppointmentsService.CreateAsync` to accept optional `int? clientUserId = null` and pass that value into `TryBookNewAsync` instead of a hard-coded null (gap root cause). Keep validation/error mapping identical. Default null preserves guest and existing service-level test call sites.

    In `AppointmentsController`, add a private `TryGetClientUserId` mirroring OrdersController: require authenticated User, `StaffRoles.Client`, parse `ClaimTypes.NameIdentifier` to int; otherwise leave null. Before calling the service, resolve optional clientUserId and call `CreateAsync(request, clientUserId)`. Do not add [Authorize] on public create — anonymous booking must remain available. Do not accept owner ids from AppointmentCreateDto body (D-08). Staff JWT must not attach ownership.

    Run ClientOwnedBookingCreateTests until green. Do not modify loyalty ledger or cancel/reschedule service methods in this task.
  </action>
  <verify>
    <automated>grep -q "TryGetClientUserId" API/ZachHairStudio.Api/Controllers/AppointmentsController.cs &amp;&amp; grep -q "ClaimTypes.NameIdentifier" API/ZachHairStudio.Api/Controllers/AppointmentsController.cs &amp;&amp; grep -q "StaffRoles.Client" API/ZachHairStudio.Api/Controllers/AppointmentsController.cs &amp;&amp; grep -E "CreateAsync\(AppointmentCreateDto request, int\? clientUserId" API/ZachHairStudio.Shared/Features/Appointments/AppointmentsService.cs &amp;&amp; awk '/CreateAsync\(AppointmentCreateDto/,/^    public |^    private /' API/ZachHairStudio.Shared/Features/Appointments/AppointmentsService.cs | grep -q "TryBookNewAsync(request, clientUserId)" &amp;&amp; cd API &amp;&amp; dotnet test ZachHairStudio.Api.Tests/ZachHairStudio.Api.Tests.csproj --filter "FullyQualifiedName~ClientOwnedBookingCreateTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - AppointmentsController contains TryGetClientUserId using Client role + NameIdentifier
    - CreateAsync signature accepts optional clientUserId and forwards it to TryBookNewAsync (no hard-coded null at that call site)
    - Filtered ClientOwnedBookingCreateTests pass on a host with LocalDB/SQL Server available
  </acceptance_criteria>
  <done>Public create sets ClientUserId for Client JWT; guest/Staff remain unowned (ACCT-02/04/06, D-08).</done>
</task>

<task type="auto">
  <name>Task 3: Landing Bearer on book + claim re-entry on Bookings</name>
  <files>landing-page/lib/appointments.ts, landing-page/components/ClaimHistoryPanel.tsx, landing-page/app/account/bookings/page.tsx</files>
  <read_first>
    - landing-page/lib/appointments.ts (createAppointment headers — Content-Type only today)
    - landing-page/lib/auth.ts (getToken / STORAGE_KEY zhs.client.auth)
    - landing-page/lib/cart.ts (checkoutHeaders optional Bearer pattern)
    - landing-page/components/AppointmentBookingForm.tsx (calls createAppointment — no API change needed if fetch attaches token)
    - landing-page/components/ClaimHistoryPanel.tsx (register-only UX; empty preview router.replace to /account)
    - landing-page/app/account/register/page.tsx (how ClaimHistoryPanel is mounted after register)
    - landing-page/app/account/bookings/page.tsx (list load/refresh)
    - .planning/phases/07-accounts-retention/07-CONTEXT.md (D-04)
  </read_first>
  <action>
    Update `createAppointment` in landing-page/lib/appointments.ts so that when `getToken()` returns a Client session token, the POST includes Authorization Bearer (same optional-attach pattern as cart.ts checkoutHeaders). When no token, keep today's anonymous JSON POST. Do not require login to book.

    Extend ClaimHistoryPanel for account-shell / embedded use (D-04, VERIFICATION missing item 2): add a prop (e.g. variant or stayOnPage) so empty claim-preview does not navigate away from /account/bookings — render null instead. On Confirm/Skip in embedded mode, invoke onFinished (parent reloads bookings) and avoid forcing navigation to /account when the parent wants to stay on Bookings. Preserve existing register-page behavior (redirect after claim/skip) so 07-02 register UX stays intact.

    Mount the embedded ClaimHistoryPanel on /account/bookings above the history list; after successful claim, call the existing load() refresh so newly attached rows appear with Cancel/Reschedule. Do not add claim UI to dashboard.
  </action>
  <verify>
    <automated>grep -q "getToken" landing-page/lib/appointments.ts &amp;&amp; grep -q "Authorization" landing-page/lib/appointments.ts &amp;&amp; grep -q "ClaimHistoryPanel" landing-page/app/account/bookings/page.tsx &amp;&amp; grep -E "variant|stayOnPage|embedded|mode" landing-page/components/ClaimHistoryPanel.tsx &amp;&amp; cd landing-page &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - createAppointment attaches Bearer when getToken() is non-null
    - account/bookings imports and mounts ClaimHistoryPanel
    - ClaimHistoryPanel supports a non-redirecting empty/finish path for the account shell
    - landing-page TypeScript check passes
  </acceptance_criteria>
  <done>Logged-in /book owns appointments; Bookings shell can re-claim guest-email history (D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/appointments | Untrusted; optional Client JWT may be present; body must never supply owner id |
| JWT → Appointment.ClientUserId | Authenticated Client NameIdentifier crosses into ownership FK |
| Client → GET/cancel account bookings | Existing ownership filter; depends on FK set at create |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05-01 | Elevation of Privilege | AppointmentsController create | high | mitigate | Resolve owner only via TryGetClientUserId (Client role + NameIdentifier); ignore body owner fields (D-08) |
| T-07-05-02 | Spoofing | Public create with Staff JWT | medium | mitigate | Staff/non-Client → clientUserId null; regression asserts Staff Bearer does not own |
| T-07-05-03 | Information Disclosure | Account history after attach | medium | mitigate | Existing AccountService ClientUserId filter + 404 cross-client (unchanged); create only sets own id |
| T-07-05-04 | Tampering | ClientUserId after create | low | accept | No client-writable owner field; FK set server-side once at book |
| T-07-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan — N/A install gate |
</threat_model>

<verification>
- ClientOwnedBookingCreateTests green (owned create → list → cancel; guest null; Staff null)
- CreateAsync no longer hard-codes null into TryBookNewAsync at the public create seam
- Landing createAppointment sends Bearer when session present; bookings page hosts embedded claim
- Re-run phase verification truths 2 (self-service) and booking-history caveat for post-login books
</verification>

<success_criteria>
1. Logged-in Client booking via public API owns the appointment (ClientUserId set).
2. That appointment appears under /api/account/bookings and cancel works (ACCT-04 path unblocked).
3. Guest booking without Bearer remains ClientUserId null.
4. Landing /book with Client session attaches Bearer; /account/bookings can claim leftover guest-email rows (D-04).
</success_criteria>

<output>
Create `.planning/phases/07-accounts-retention/07-05-SUMMARY.md` when done
</output>
