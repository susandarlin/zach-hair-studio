---
phase: 05-product-catalog
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - landing-page/lib/products.ts
  - landing-page/lib/services.ts
  - landing-page/lib/data.ts
  - landing-page/app/products/page.tsx
  - landing-page/app/products/[slug]/page.tsx
  - landing-page/app/services/[slug]/page.tsx
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03]
must_haves:
  truths:
    - "Visiting /products renders the seeded catalog with name, teaser, category, price, and stock state for each active product, category-grouped (PROD-01, mirrors CAT-01's /services template)"
    - "The catalog lives at a dedicated /products route on landing-page/, mirroring Phase 1's /services route placement (D-01)"
    - "Zero products renders the 'Products Are Being Curated' empty-state box (UI-SPEC Copywriting Contract) instead of an empty grid or a crash (empty edge, UI-SPEC covered)"
    - "A product with stock === 0 renders the neutral 'Out of Stock' badge and remains a fully clickable, browsable card/detail page — never hidden or greyed out (UI-SPEC covered, D-06)"
    - "Visiting /products/{slug} renders a single active product's detail page with its long description, category, price, and stock state (PROD-02)"
    - "Visiting /products/{unknown-slug} or the slug of an inactive/discontinued product renders Next.js notFound() (404) (UI-SPEC covered — not-found)"
    - "Product API responses are parsed through a Zod schema (ProductSchema/ProductListSchema) in lib/products.ts before rendering, following the exact fetchServices/fetchServiceBySlug try/catch-to-empty-array and 404-to-null conventions (PLAT-02 frontend, D-18)"
    - "Catalog and detail pages render server-side with ISR (revalidate: 60, D-03) — no 'use client', no useState/useEffect data fetching, no client-side loading spinner"
    - "Visiting a service detail page whose service has 1+ recommended products renders a 'Recommended Products' section (SectionHeading + ProductCard grid) below the existing description (PROD-03, D-13)"
    - "Visiting a service detail page whose service has ZERO recommended products renders NO 'Recommended Products' section at all — no heading, no empty box (PROD-03, D-14, UI-SPEC covered)"
    - "The navLinks array includes a 'Products' entry pointing at /products (D-04)"
    - { statement: "Long-text — a product with an unusually long shortDescription does not visually break the ProductCard grid's row alignment on /products (UI-SPEC backstop consideration).", verification: backstop }
  prohibitions:
    - statement: "Catalog and detail pages do NOT use client-side useEffect/useState data fetching or loading spinners"
      status: resolved
      verification: "app/products/page.tsx and app/products/[slug]/page.tsx are async Server Components with no 'use client' directive and no useState/useEffect"
    - statement: "recommendedProducts is added as an optional field on the existing ServiceSchema in lib/services.ts, NOT a second Zod schema/fetch pair in a separate module"
      status: resolved
      verification: "lib/services.ts imports ProductSchema from lib/products.ts and adds recommendedProducts: z.array(ProductSchema).optional() to ServiceSchema; no new fetch function is added to lib/services.ts for recommendations"
  artifacts:
    - landing-page/lib/products.ts
    - landing-page/app/products/page.tsx
    - landing-page/app/products/[slug]/page.tsx
  key_links:
    - "lib/products.ts ProductSchema field names must exactly match Plan 05-01's ProductResponseDto (id, slug, name, shortDescription, longDescription, category, price, stock, imageUrl) or Zod .parse throws"
    - "ServiceSchema's new recommendedProducts field must parse the same shape ServicesService.GetBySlugAsync now returns (Plan 05-01) or the /services/[slug] page throws on services with recommendations"
    - "navLinks 'Products' entry is the only new homepage-adjacent surface this phase adds (D-04 — no homepage teaser section)"
---

## Phase Goal

**As a** client, **I want to** browse a curated, stylist-recommended product catalog tied to the services I care about, **so that** I can find products my stylist actually recommends without wading through a general storefront.

> Frontend vertical — the visible half of the phase goal. Consumes Plan 05-01's live API to deliver the public `/products` catalog, `/products/[slug]` detail pages, and the "Recommended Products" section on the existing service detail page. After this plan a real client can browse the curated catalog and see stylist-recommended products on any service page — the phase's three success criteria are all observable in the browser.

<objective>
Build the shared frontend data layer (`lib/products.ts` with Zod schemas, mirroring `lib/services.ts` exactly) and the two public catalog routes as React Server Components (PROD-01/02), then extend `ServiceSchema` and the existing `/services/[slug]` page with a "Recommended Products" section (PROD-03) fed by Plan 05-01's extended `ServiceResponseDto`. Add the "Products" nav link per D-04.

Purpose: Make the entire Product Catalog + Recommendations experience real and browsable, closing out all three phase requirements end-to-end.
Output: `/products` list page (category-grouped, empty/out-of-stock states), `/products/[slug]` detail page (404-safe), a shared Zod-validated `lib/products.ts`, an extended `ServiceSchema`/`/services/[slug]` page with the omit-when-empty "Recommended Products" section, and a new "Products" nav link.
</objective>

<execution_context>
@C:/repos/vct/zach-hair-studio/.claude/gsd-core/workflows/execute-plan.md
@C:/repos/vct/zach-hair-studio/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-product-catalog/05-CONTEXT.md
@.planning/phases/05-product-catalog/05-RESEARCH.md
@.planning/phases/05-product-catalog/05-PATTERNS.md
@.planning/phases/05-product-catalog/05-UI-SPEC.md
@.planning/phases/05-product-catalog/05-01-SUMMARY.md

# Analog + integration files:
@landing-page/lib/services.ts
@landing-page/app/services/page.tsx
@landing-page/app/services/[slug]/page.tsx
@landing-page/components/SectionHeading.tsx
@landing-page/lib/data.ts
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan (excluded from cross-plan drift verification):
- Functions: `fetchProducts()`, `fetchProductBySlug(slug)` in `lib/products.ts`
- Zod schemas + type: `ProductSchema`, `ProductListSchema`, `Product` (z.infer) in `lib/products.ts`
- Routes: `/products` (`app/products/page.tsx`), `/products/[slug]` (`app/products/[slug]/page.tsx`)
- Component: `ProductCard` (local to `app/products/page.tsx`, mirrors the existing local `ServiceCard`)
- Function: `groupProductsByCategory` (local to `app/products/page.tsx`, mirrors `groupServicesByCategory`)
- Field: `ServiceSchema.recommendedProducts` (optional array) in `lib/services.ts`
- New section markup in `app/services/[slug]/page.tsx`: "Recommended Products"
- `navLinks` entry: `{ label: "Products", href: "/products" }` in `lib/data.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared products data layer and extend ServiceSchema</name>
  <files>landing-page/lib/products.ts, landing-page/lib/services.ts, landing-page/lib/data.ts</files>
  <read_first>
    - landing-page/lib/services.ts (the exact fetch/Zod/ISR pattern to clone field-for-field)
    - .planning/phases/05-product-catalog/05-01-SUMMARY.md (confirm the exact ProductResponseDto field names and seeded slugs to sanity-check the schema against)
    - .planning/phases/05-product-catalog/05-RESEARCH.md (Code Examples: lib/products.ts full listing; Extending ServiceSchema example)
    - landing-page/lib/data.ts (navLinks array shape and existing entries)
  </read_first>
  <action>
    Create `landing-page/lib/products.ts` as a direct clone of `lib/services.ts`'s structure: same `API_BASE_URL` constant derivation (`process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:5236"`, trailing slash stripped), a `PRODUCT_REVALIDATE_SECONDS = 60` constant (D-03, same window as services). Define `ProductSchema = z.object({ id: z.number(), slug: z.string(), name: z.string(), shortDescription: z.string(), longDescription: z.string(), category: z.string(), price: z.number(), stock: z.number(), imageUrl: z.string().nullable() })` (field-for-field match to Plan 05-01's `ProductResponseDto` — no `durationMinutes`/`displayOrder`). Define `ProductListSchema = z.array(ProductSchema)` and `export type Product = z.infer<typeof ProductSchema>`. Export `fetchProducts(): Promise<Product[]>` — fetches `${API_BASE_URL}/api/products` with `{ next: { revalidate: PRODUCT_REVALIDATE_SECONDS } }`, wraps in try/catch returning `[]` on any failure (including a non-ok response), parses with `ProductListSchema.parse`. Export `fetchProductBySlug(slug: string): Promise<Product | null>` — fetches `${API_BASE_URL}/api/products/${encodeURIComponent(slug)}`, returns `null` on fetch throw or `response.status === 404 || !response.ok`, otherwise parses with `ProductSchema.parse`.

    Extend `landing-page/lib/services.ts`: add `import { ProductSchema } from "@/lib/products";` at the top, then add one field to the existing `ServiceSchema` object literal: `recommendedProducts: z.array(ProductSchema).optional()`. Do not touch any other field, export, or function in this file — `fetchServices`/`fetchServiceBySlug` behavior is otherwise unchanged.

    Add one entry to the `navLinks` array in `landing-page/lib/data.ts`: `{ label: "Products", href: "/products" }` (D-04), placed after the existing `"Services"` entry and before `"Gallery"`.
  </action>
  <verify>
    <automated>cd landing-page && test -f lib/products.ts && grep -q "ProductSchema" lib/services.ts && grep -q "/products" lib/data.ts && npx tsc --noEmit && echo TS-OK</automated>
  </verify>
  <acceptance_criteria>
    - `lib/products.ts` exports `fetchProducts`, `fetchProductBySlug`, `ProductSchema`, `ProductListSchema`, and a `Product` type
    - `ProductSchema` fields match `ProductResponseDto` exactly (id, slug, name, shortDescription, longDescription, category, price, stock, imageUrl nullable) — no durationMinutes/displayOrder
    - Both fetchers pass `{ next: { revalidate: 60 } }`; `fetchProductBySlug` returns null on 404 or fetch failure; `fetchProducts` returns `[]` on failure
    - `ServiceSchema` in `lib/services.ts` gains exactly one new optional field, `recommendedProducts`, sourced from `ProductSchema` — no second schema/fetch pair added anywhere for recommendations
    - `navLinks` contains `{ label: "Products", href: "/products" }`
    - `npx tsc --noEmit` reports no type errors
  </acceptance_criteria>
  <done>Zod-validated products data layer exists; ServiceSchema recognizes recommendedProducts; Products nav link added; no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build the /products catalog list page and /products/[slug] detail page</name>
  <files>landing-page/app/products/page.tsx, landing-page/app/products/[slug]/page.tsx</files>
  <read_first>
    - landing-page/app/services/page.tsx (full file — category grouping, ServiceCard markup, empty-state block to clone and adapt)
    - landing-page/app/services/[slug]/page.tsx (full file — notFound guard, Image usage, two-column detail layout to clone and adapt)
    - landing-page/lib/products.ts (fetchProducts/fetchProductBySlug/Product type from Task 1)
    - landing-page/components/SectionHeading.tsx (eyebrow/title/highlight/subtitle prop shape)
    - .planning/phases/05-product-catalog/05-UI-SPEC.md (Copywriting Contract — catalog eyebrow/title/subtitle, empty-state heading/body, Out of Stock badge copy; Color section — Out of Stock badge classes; Additional Notes — ProductCard/catalog page/detail page structure)
  </read_first>
  <action>
    Create `landing-page/app/products/page.tsx` as an async Server Component (NO `use client`, no useState/useEffect — D-03), structurally cloned from `app/services/page.tsx`: a local `groupProductsByCategory(products: Product[])` helper (same grouping-by-`category`-then-sort logic, but sorted by `name` within each group since `Product` has no `displayOrder`), a local `ProductCard` component using the exact `card-hover bg-charcoal border border-white/5 hover:border-gold/30 rounded-2xl p-7` wrapper from `ServiceCard`, the same "Z" monogram fallback badge (`w-14 h-14 bg-gold/10 rounded-xl` serif "Z") when `imageUrl` is null, `category` eyebrow + `name` (`h3 text-lg font-semibold`) + `shortDescription`, and a footer row showing the price (`text-gold font-bold text-lg`) opposite either nothing (in stock) or the "Out of Stock" badge (`bg-white/5 border border-white/10 text-gray-400 text-xs uppercase tracking-wider px-3 py-1 rounded-full`, rendered only when `stock === 0`, per UI-SPEC Color section). Use `SectionHeading` with `eyebrow="Stylist Picks"`, `title="Recommended"`, `highlight="Products"`, `subtitle="Hair care and styling products our stylists personally recommend — the same tools and treatments we use in the chair."` (UI-SPEC Copywriting Contract). Render the empty-state box (mirroring `Services.tsx`'s pattern) with heading "Products Are Being Curated" and body "Our product recommendations are temporarily unavailable. Please check back soon, or ask your stylist during your next visit." when `fetchProducts()` returns `[]`.

    Create `landing-page/app/products/[slug]/page.tsx` as an async Server Component (NO `use client`), structurally cloned from `app/services/[slug]/page.tsx`: read the async `slug` param, call `fetchProductBySlug(slug)`, call Next.js `notFound()` when it returns null (covers both an unknown slug and an inactive product's slug, since the API 404s both — UI-SPEC not-found consideration). Render the same two-column `grid lg:grid-cols-[1fr_320px] gap-8` layout: main `<article>` with `category` eyebrow, `name` as `h1` (`font-serif text-4xl md:text-6xl`), `imageUrl` via `next/image` when present (same-origin path per D-07 — do NOT prefix with `API_BASE_URL`), `longDescription`; sticky `<aside>` with a `dl` showing Price (`text-gold font-bold text-xl`) and Stock as a value row — render `"In Stock"` in `text-gray-400` when `stock > 0`, or the same "Out of Stock" badge markup used on the catalog card when `stock === 0`. No Book/purchase CTA in the aside (no cart exists yet — Phase 6). Include a "&larr; Back to products" link to `/products` at the top, matching the existing "&larr; Back to services" styling.
  </action>
  <verify>
    <automated>cd landing-page && test -f app/products/page.tsx && test -f "app/products/[slug]/page.tsx" && ! grep -q "use client" app/products/page.tsx && ! grep -q "use client" "app/products/[slug]/page.tsx" && grep -q "notFound" "app/products/[slug]/page.tsx" && npm run build 2>&1 | grep -E "Compiled successfully|error|Failed" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/products/page.tsx` and `app/products/[slug]/page.tsx` are async Server Components with no `use client` directive and no useState/useEffect
    - `/products` renders products grouped by category, ordered by name within each group, each card showing name/shortDescription/price and an Out-of-Stock badge only when `stock === 0`
    - `/products` renders the "Products Are Being Curated" empty-state box when `fetchProducts()` returns `[]`
    - `/products/[slug]` calls `notFound()` when `fetchProductBySlug` returns null; otherwise renders name/category/longDescription/price/stock state
    - No `dangerouslySetInnerHTML` anywhere in either file (React's default escaping handles description rendering)
    - `npm run build` compiles successfully
  </acceptance_criteria>
  <done>/products and /products/[slug] are live, category-grouped, empty-state-safe, 404-safe, and Out-of-Stock-aware; build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Add the "Recommended Products" section to the service detail page</name>
  <files>landing-page/app/services/[slug]/page.tsx</files>
  <read_first>
    - landing-page/app/services/[slug]/page.tsx (current full file — insertion point after the existing two-column grid, before the closing `</section>`)
    - landing-page/app/products/page.tsx (the ProductCard markup/grid to reuse — created in Task 2, same session)
    - landing-page/components/SectionHeading.tsx (import + prop usage, not yet imported by this file)
    - .planning/phases/05-product-catalog/05-CONTEXT.md (D-13 — reuse SectionHeading + card styling; D-14 — one-directional, omit-when-empty)
    - .planning/phases/05-product-catalog/05-UI-SPEC.md (Copywriting Contract — "Recommended Products" eyebrow/heading; Additional Notes — section placement and grid reuse; UI Considerations — omit entirely when zero mapped, no empty box)
  </read_first>
  <action>
    In `landing-page/app/services/[slug]/page.tsx`, add `import SectionHeading from "@/components/SectionHeading";` to the imports. After the existing `<div className="mt-8 grid lg:grid-cols-[1fr_320px] gap-8 items-start">...</div>` block and before the closing `</div>` of the `max-w-5xl mx-auto px-6` container, conditionally render a new section ONLY when `service.recommendedProducts && service.recommendedProducts.length > 0` (D-14 — render nothing, not an empty box, when absent or empty): a `<section className="mt-16">` containing `<SectionHeading eyebrow="Stylist Picks for This Service" title="Recommended" highlight="Products" subtitle="" />` (empty subtitle is acceptable — SectionHeading's subtitle prop is required by its type but an empty string renders no visible text) followed by a `grid sm:grid-cols-2 lg:grid-cols-3 gap-6` containing one card per `service.recommendedProducts` entry, using the identical `ProductCard` markup structure from Task 2's `app/products/page.tsx` (category eyebrow, name, shortDescription, price, Out-of-Stock badge when `stock === 0`) linking each to `/products/{slug}`. Do not extract a shared component across files this pass — a small amount of markup duplication between `app/products/page.tsx`'s `ProductCard` and this inline recommended-products card is acceptable (both files are Server Components with no shared client bundle concern); note it as a possible future extraction, not a blocker.
  </action>
  <verify>
    <automated>cd landing-page && grep -q "SectionHeading" "app/services/[slug]/page.tsx" && grep -q "recommendedProducts" "app/services/[slug]/page.tsx" && npm run build 2>&1 | grep -E "Compiled successfully|error|Failed" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/[slug]/page.tsx` imports `SectionHeading` and renders a "Recommended Products" section only when `service.recommendedProducts` is non-empty
    - The section is entirely absent (no heading, no empty box) when `recommendedProducts` is undefined or an empty array
    - Recommended-product cards link to `/products/{slug}` and show the Out-of-Stock badge when applicable, visually consistent with the `/products` catalog cards
    - `npm run build` compiles successfully
  </acceptance_criteria>
  <done>A service with 1+ recommended products shows a "Recommended Products" section on its detail page; a service with zero shows nothing; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API JSON → frontend | `fetchProducts`/`fetchProductBySlug` parse untrusted API JSON through Zod before render; `ServiceSchema.recommendedProducts` parses the extended service-detail response |
| product description → HTML | `shortDescription`/`longDescription` rendered into JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information Disclosure (malformed API data crashes render) | fetchProducts/fetchProductBySlug/ServiceSchema | low | mitigate | Zod `.parse` validates response shape at the fetch boundary for both products and the extended service schema; a mismatch throws a clear build/render-time error instead of a downstream crash (Task 1, D-18) |
| T-05-06 | Tampering (stored XSS via product description fields) | catalog/detail/recommended-products rendering | medium | mitigate | React's default auto-escaping handles all string interpolation; no `dangerouslySetInnerHTML` anywhere in this plan's new or modified files (Tasks 2/3) |
| T-05-SC | Tampering (supply chain) | npm installs | low | accept | Zero new npm packages this phase (RESEARCH Package Legitimacy Audit: not applicable, no installs) |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` succeed in `landing-page/`
- Catalog/detail pages are async Server Components (no `use client`, no useState/useEffect)
- Manual/end-of-phase human-check: visit `/products` (grouped catalog, empty-state fallback if seed data is later cleared, Out-of-Stock badge on the zero-stock seeded product), a product detail page, `/products/not-a-real-slug` (404), a service detail page with recommendations (section renders) and one without (section absent) — consolidated into the phase's end-of-phase UAT per `workflow.human_verify_mode = end-of-phase`
</verification>

<success_criteria>
- PROD-01 satisfied: client can browse the product list showing name, description, price, image (when present), and stock state
- PROD-02 satisfied: client can open a single product's detail page; unknown/inactive slug 404s
- PROD-03 satisfied: a service detail page surfaces its curated recommended products, or nothing when none are mapped
- PLAT-02 (frontend) satisfied: Zod validates all API responses (products list/detail, extended service detail) before render (D-18)
- D-03/D-04 honored: server-rendered ISR with no client loading spinners; "Products" nav link added, no homepage teaser section
</success_criteria>

<output>
Create `.planning/phases/05-product-catalog/05-02-SUMMARY.md` when done. Record: the chosen empty-subtitle SectionHeading usage on the Recommended Products section (or any adjustment made); confirmation that `npm run build` passes; a note that frontend automated tests are deferred (no test framework in `landing-page/`, matching every prior phase's precedent) — verification is `npm run build` plus the end-of-phase UAT pass.
</output>