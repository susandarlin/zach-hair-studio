---
phase: quick
plan: 260809-m2q
type: execute
wave: 1
depends_on: []
files_modified: [landing-page/lib/chat.ts, landing-page/lib/chat.selfcheck.mjs]
autonomous: true
requirements: []

must_haves:
  truths:
    - "Both chat.ts and chat.selfcheck.mjs document why the alias-driven RegExp() constructor is not a ReDoS risk, and semgrep's detect-non-literal-regexp rule no longer blocks on either line"
  artifacts:
    - "landing-page/lib/chat.ts"
    - "landing-page/lib/chat.selfcheck.mjs"
  key_links:
    - "alias always originates from the hardcoded CATEGORY_ALIASES map in the same file, never from normalizedInput or any external source"
---

<objective>
Close PR #43's `sast (semgrep)` CI finding (advisory, `continue-on-error: true`, rule `javascript.lang.security.audit.detect-non-literal-regexp.detect-non-literal-regexp`), flagged twice: `landing-page/lib/chat.ts:104-106` and `landing-page/lib/chat.selfcheck.mjs:34`.

Confirmed false positive: in both files, `new RegExp(\`\\b${alias}\\b\`)` takes `alias` only from `CATEGORY_ALIASES[category] ?? []` — a module-scoped, hardcoded object with 4 keys, each a short static array of lowercase words ("cut", "haircut", "hair cut", "dye", "colour", "style", "treatment"). `alias` is never derived from `normalizedInput` (the actual user-controlled chat message) or any other external input, so there is no ReDoS surface. Semgrep's rule flags any non-literal `RegExp()` constructor argument regardless of taint origin — this repo's own `.github/workflows/security.yml` comment already anticipates this class of noise ("Expect some false positives — triaging them is the lesson").

Purpose: Suppress this specific, justified false positive without weakening the rule elsewhere, and without rewriting working, already-tested matching logic.
Output: A `// nosemgrep: ...` comment plus a one-line rationale directly above the `new RegExp(...)` call in each file — no logic change.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@landing-page/lib/chat.ts
@landing-page/lib/chat.selfcheck.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suppress the alias-RegExp false positive in chat.ts and chat.selfcheck.mjs</name>
  <files>landing-page/lib/chat.ts, landing-page/lib/chat.selfcheck.mjs</files>
  <action>
  In `landing-page/lib/chat.ts`, immediately above the `new RegExp(\`\\b${alias}\\b\`).test(normalizedInput)` call inside `findMatchingService` (around line 104), add:
  ```
  // alias is drawn only from the hardcoded CATEGORY_ALIASES map above, never from
  // user input (normalizedInput) — no ReDoS surface.
  // nosemgrep: javascript.lang.security.audit.detect-non-literal-regexp.detect-non-literal-regexp
  ```

  In `landing-page/lib/chat.selfcheck.mjs`, immediately above the equivalent inlined `new RegExp(\`\\b${alias}\\b\`).test(normalizedInput)` line (around line 34), add the same two-line comment (rationale + nosemgrep directive), keeping this file's copy of `findMatchingService` in sync with `chat.ts` per its own header comment ("Keep in sync with lib/chat.ts").

  Do not change any other logic, imports, or exports in either file. Do not touch `.github/workflows/security.yml` or any other file.
  </action>
  <verify>
    <automated>cd landing-page && node lib/chat.selfcheck.mjs && echo SELFCHECK_OK; cd .. && git diff --stat -- landing-page/lib/chat.ts landing-page/lib/chat.selfcheck.mjs</automated>
  </verify>
  <done>Both files gain only the rationale + `nosemgrep` comment pair directly above their respective `new RegExp(...)` line; `node lib/chat.selfcheck.mjs` (run from `landing-page/`) still exits 0. `git diff` is confined to these two files and contains no logic changes — comment lines only.</done>
</task>

</tasks>

<verification>
`git diff` shows only added comment lines in `chat.ts` and `chat.selfcheck.mjs`, no other files touched. `node lib/chat.selfcheck.mjs` still passes. A local semgrep run (if available) on the two lines shows the two prior `detect-non-literal-regexp` findings suppressed; if semgrep is unavailable locally, rely on the `nosemgrep` directive syntax being the standard, documented suppression mechanism.
</verification>

<success_criteria>
The two `detect-non-literal-regexp` findings from the pasted CI output are suppressed via a targeted, justified `nosemgrep` comment in each flagged file, with no change to matching behavior and no weakening of the rule elsewhere in the codebase.
</success_criteria>

<output>
Create `.planning/quick/260809-m2q-fix-semgrep-redos-false-positive-alias-r/260809-m2q-SUMMARY.md` when done. Note this closes the `sast (semgrep)` finding the user pasted directly (same session as the gitleaks `.codex` allowlist fix, quick task 260809-k3d) — both are real (but false-positive) advisory security-scan findings, addressed with targeted, documented suppressions rather than code rewrites.
</output>
