---
phase: 260809-sz1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/components/AdminChatWidget.tsx
autonomous: true
requirements: [QUICK-260809-sz1]

must_haves:
  truths:
    - "The chat panel's desktop footprint (sm: breakpoint) is larger than the previous 24rem x 32rem."
    - "`npx tsc --noEmit -p .` (run from dashboard/) reports no errors."
  artifacts:
    - "dashboard/components/AdminChatWidget.tsx — dialog panel className has larger sm:w-*/sm:h-* values"
---

<objective>
User asked to increase the AdminChat widget's chat box size — the panel felt cramped, especially now that the starter-prompt row is always visible alongside the message list.
</objective>

<context>
@dashboard/components/AdminChatWidget.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enlarge the chat panel dimensions</name>
  <files>dashboard/components/AdminChatWidget.tsx</files>
  <action>
On the dialog panel div, increase desktop size from `sm:w-96 sm:h-[32rem] sm:max-h-[70vh]` to `sm:w-[28rem] sm:h-[40rem] sm:max-h-[85vh]`, and give the mobile layout more vertical room by changing `bottom-24 top-24` to `bottom-16 top-16`.
  </action>
  <verify>
    <automated>cd dashboard && npx tsc --noEmit -p .  # expect no output / exit 0</automated>
  </verify>
  <done>Panel renders larger on desktop and mobile; tsc reports no errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p .` (from `dashboard/`) — no errors.
2. Manual visual check via `npm run dev -- -p 3001` (deferred to user).
</verification>

<output>
Create `.planning/quick/260809-sz1-increase-adminchat-box-size/260809-sz1-SUMMARY.md` when done.
</output>
