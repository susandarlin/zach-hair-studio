---
phase: 260809-4d3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - start-dev.sh
  - stop-dev.sh
  - tests/dev-stack-scripts.test.sh
autonomous: true
requirements: [QUICK-260809-4d3]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Running ./start-dev.sh from any working directory launches the API, landing page, and dashboard with the same repository paths, commands, and default ports as start-dev.bat."
    - "The start command returns only after all three ports accept connections, prints the three URLs, and leaves each service detached with a dedicated log and tracked PID under the gitignored temp/dev-stack directory."
    - "A partial startup failure is reported with the failing service's log path and cleans up only the processes started by that invocation."
    - "Running ./stop-dev.sh terminates tracked process trees for all three services, falls back to the three known listener ports when PID state is missing or stale, removes stale PID metadata, and succeeds when run repeatedly."
    - "The existing start-dev.bat and stop-dev.bat files remain byte-for-byte untouched, including the user's uncommitted edits."
  artifacts:
    - "start-dev.sh — executable Bash launcher for API :5236, landing :3000, and dashboard :3001"
    - "stop-dev.sh — executable Bash shutdown command with PID-first and port-fallback cleanup"
    - "tests/dev-stack-scripts.test.sh — isolated lifecycle regression harness using fake service executables and dynamically allocated ports"
  key_links:
    - "start-dev.sh -> API/ZachHairStudio.Api via dotnet run, landing-page via npm run dev, and dashboard via npm run dev -- -p 3001"
    - "start-dev.sh -> temp/dev-stack/*.pid and *.log so detached processes remain observable and stop-dev.sh has ownership metadata"
    - "stop-dev.sh -> start-dev.sh PID-file schema and service/port mapping; drift between these scripts would orphan processes"
    - "tests/dev-stack-scripts.test.sh -> both root scripts through test-only environment overrides, exercising the real lifecycle logic without starting the application or using ports 5236/3000/3001"
---

<objective>
Create executable Bash counterparts to the user-edited `start-dev.bat` and `stop-dev.bat` launchers, preserving the same three services, working directories, command lines, default ports, startup ordering, status output, and whole-stack shutdown outcome while adding the detached-process bookkeeping Bash requires.

Purpose: Linux and macOS developers need one root command to start the complete Zach Hair Studio stack, inspect per-service output, wait for usable listeners, and stop every spawned process reliably.

Output: Root-level `start-dev.sh` and `stop-dev.sh` plus an isolated lifecycle regression test. The two BAT files are authoritative read-only inputs and are not modified.
</objective>

<execution_context>
@/workspaces/zach-hair-studio/.codex/gsd-core/workflows/execute-plan.md
@/workspaces/zach-hair-studio/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@start-dev.bat
@stop-dev.bat
@.gitignore
@API/ZachHairStudio.Api/Properties/launchSettings.json
@landing-page/package.json
@dashboard/package.json

<interfaces>
Authoritative service contract extracted from the BAT sources:

| Service | Working directory | Command | Default port | URL |
|---------|-------------------|---------|--------------|-----|
| API | `API/ZachHairStudio.Api` | `dotnet run` | 5236 | `http://localhost:5236` |
| Landing | `landing-page` | `npm run dev` | 3000 | `http://localhost:3000` |
| Dashboard | `dashboard` | `npm run dev -- -p 3001` | 3001 | `http://localhost:3001` |

`start-dev.bat` gives the API a three-second head start before launching the two Next.js applications. `stop-dev.bat` force-stops listener process trees found on ports 5236, 3000, and 3001. Preserve those observable defaults while implementing Bash-native readiness, log, and PID handling.

Runtime state belongs below `temp/dev-stack/`; root `.gitignore` already ignores `/temp`, so logs and PID files must not become source changes.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Ship and prove the complete Bash dev-stack lifecycle</name>
  <files>start-dev.sh, stop-dev.sh, tests/dev-stack-scripts.test.sh</files>
  <behavior>
    - Test 1: With three fake long-running services and dynamically allocated ports, `start-dev.sh` exits 0 only after every listener is reachable, writes one non-empty log and valid PID record per service, and prints the three effective URLs.
    - Test 2: Invoking `start-dev.sh` again while the tracked listeners are alive exits non-zero without spawning duplicates or replacing live PID ownership metadata.
    - Test 3: `stop-dev.sh` sends graceful termination, escalates after a bounded wait when required, removes PID state, closes all three listeners, and a second stop exits 0 with an already-stopped message.
    - Test 4: When a fake service exits before readiness, `start-dev.sh` exits non-zero, identifies that service and its log, removes its current invocation's PID state, and leaves no listener/process started by that invocation.
    - Test 5: When PID metadata is absent or stale but fake listeners occupy the configured service ports, `stop-dev.sh` discovers and terminates those listener processes, matching the BAT shutdown outcome.
  </behavior>
  <action>
Treat `start-dev.bat` and `stop-dev.bat` as immutable, user-owned reference files: do not format, patch, stage, or restore them. Begin by recording their working-tree content hashes and confirm the same hashes after all work. Create the test harness first so the happy path starts red, then implement both root scripts until every lifecycle case passes. Use `#!/usr/bin/env bash`, strict quoting, and arrays/functions rather than `eval`; make all three new files executable.

In `start-dev.sh`, resolve the repository root from `BASH_SOURCE[0]` so invocation works outside the repository directory. Define one service table for the names, authoritative working directories, commands, ports, log paths, and PID paths. Default runtime state to `$REPO_ROOT/temp/dev-stack`, create it with owner-only permissions, and support environment overrides for the state directory, three ports, API head-start duration, readiness timeout, and the single executable paths used for `dotnet` and `npm`. These overrides are testability seams: validate port/range and timeout values as integers, treat executable overrides as one quoted path rather than shell text, and keep the no-environment behavior exactly equal to the table in `<interfaces>`.

Preflight the required directories and executables, reject a duplicate start when a tracked PID is alive or a configured port already has a listener, and clear only demonstrably stale PID records. Launch the API detached first with stdout/stderr appended to `api.log`, preserve the BAT's three-second default API head start, then launch landing and dashboard detached with their own logs. Record the immediate launcher PID atomically as soon as each process starts. Use `nohup` plus an `exec` boundary so the recorded PID is stable, and ensure the stop path can terminate descendants such as the `node` process spawned by npm. Poll each configured TCP port with a bounded Bash readiness helper; also detect an early launcher exit rather than waiting for the full timeout. On success, print the same API/Landing/Dashboard labels and URLs as the BAT file plus the runtime log directory. On any launch/readiness failure, print the failed service and its log path, terminate only processes launched during this invocation, remove their PID files, retain logs for diagnosis, and exit non-zero.

In `stop-dev.sh`, resolve and validate the identical state-directory, service, and port schema without sourcing or executing writable runtime data. For each service, accept only decimal positive PIDs from the expected PID file, terminate the recorded process and descendants with `TERM`, wait for a bounded grace period, then use `KILL` only for survivors. After the PID-first pass, discover remaining listeners on that service's exact configured port using `lsof`, with `fuser` as the fallback when available, and terminate those process trees too; this preserves `stop-dev.bat` behavior when metadata is missing while never scanning unrelated ports. Deduplicate PIDs, use `--` before PID operands, never turn PID-file contents into shell syntax, delete stale PID files, and report each service as stopped or already stopped. Absence of all services is a successful, idempotent result.

In `tests/dev-stack-scripts.test.sh`, use only temporary state, dynamically allocated loopback ports, and temporary fake `dotnet`/`npm` executables that run lightweight Python HTTP listeners; never start the real API or Next.js applications and never bind the authoritative default ports. Add bounded polling and a cleanup trap so a failed assertion cannot leak processes. Exercise success, duplicate prevention, startup rollback, PID-first shutdown, stale/missing-PID port fallback, idempotent stop, log creation, URL output, and executable-bit expectations. Keep test timeouts short enough for the automated gate to finish in under 60 seconds.
  </action>
  <verify>
    <automated>bash -n start-dev.sh stop-dev.sh tests/dev-stack-scripts.test.sh &amp;&amp; test -x start-dev.sh &amp;&amp; test -x stop-dev.sh &amp;&amp; test -x tests/dev-stack-scripts.test.sh &amp;&amp; bash tests/dev-stack-scripts.test.sh &amp;&amp; git diff --check -- start-dev.sh stop-dev.sh tests/dev-stack-scripts.test.sh</automated>
  </verify>
  <done>`./start-dev.sh` provides a detached, logged, readiness-gated three-service startup on ports 5236/3000/3001; `./stop-dev.sh` reliably and idempotently removes the tracked/fallback process trees; the isolated automated lifecycle harness passes in under 60 seconds; both BAT source hashes are unchanged from task start.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment and shell caller -> launcher configuration | Port, timeout, state-directory, and executable overrides are untrusted strings consumed by root scripts. |
| `temp/dev-stack/*.pid` -> process signaling | Writable runtime metadata determines which local process receives a signal. |
| Configured TCP ports -> fallback shutdown | Listener discovery can identify processes not represented by current PID metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q01 | Tampering / Elevation of Privilege | PID-file parsing in `stop-dev.sh` | high | mitigate | Create runtime state owner-only, accept only positive decimal PIDs, pass operands after `--`, never source/evaluate PID files, deduplicate targets, and signal only the three explicit service trees/listeners (Task 1). |
| T-Q02 | Tampering | Environment overrides in both scripts | medium | mitigate | Quote every expansion, validate ports as 1-65535 and timing values as bounded integers, and accept executable overrides as a single executable path rather than shell fragments; no `eval` or generated shell command strings (Task 1). |
| T-Q03 | Denial of Service | Duplicate startup and partial readiness failure | medium | mitigate | Preflight live PID/port ownership, reject duplicates, detect early process exit, bound readiness waits, and roll back only processes created by the failing invocation (Task 1). |
| T-Q04 | Denial of Service | Listener-port fallback in `stop-dev.sh` | medium | accept | The stop command deliberately mirrors the BAT contract of stopping listeners on the three documented dev ports. Scope discovery to those exact configured ports, show per-service outcomes, and invoke it only through the explicit stop command. |
| T-Q05 | Information Disclosure | Per-service logs | low | mitigate | Place logs under the gitignored owner-only runtime directory and do not print log content automatically; surface only file paths on failure (Task 1). |
</threat_model>

<verification>
1. `bash -n start-dev.sh stop-dev.sh tests/dev-stack-scripts.test.sh` exits 0.
2. All three files have executable mode.
3. `bash tests/dev-stack-scripts.test.sh` exits 0 in under 60 seconds and its cleanup trap leaves none of its temporary listener PIDs alive.
4. `git diff --check -- start-dev.sh stop-dev.sh tests/dev-stack-scripts.test.sh` exits 0.
5. Hashes of `start-dev.bat` and `stop-dev.bat` match the values recorded before Task 1; their existing uncommitted diffs remain user-owned and are not staged by this task.
</verification>

<source_audit>

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Root-level Bash files provide the same start/stop functionality as the two authoritative BAT files | 01 | COVERED | One end-to-end tracer implements and proves both sides of the lifecycle. |
| REQ | QUICK-260809-4d3 | Preserve startup commands/ports plus logging, readiness, PID tracking, and shutdown semantics | 01 | COVERED | Observable defaults and Bash lifecycle requirements appear in Task 1 behavior and done criteria. |
| RESEARCH | — | No RESEARCH.md exists for this quick task | 01 | COVERED | Level 0 discovery: no dependency or external integration is added. |
| CONTEXT | — | No CONTEXT.md or D-XX decisions exist for this quick task | 01 | COVERED | The task brief and BAT sources are the controlling inputs; both BAT files remain read-only. |

</source_audit>

<success_criteria>
- Root `start-dev.sh` and `stop-dev.sh` exist, pass Bash syntax checks, and are executable.
- Default start behavior launches `dotnet run` in `API/ZachHairStudio.Api`, `npm run dev` in `landing-page`, and `npm run dev -- -p 3001` in `dashboard`, exposing ports 5236, 3000, and 3001.
- Startup is detached, logged, PID-tracked, duplicate-safe, readiness-gated, and rollback-safe.
- Shutdown is PID-first, process-tree aware, port-fallback capable, bounded, and idempotent.
- The automated fake-service lifecycle harness passes without starting real project services or using their default ports.
- `start-dev.bat` and `stop-dev.bat` retain their exact pre-task content and uncommitted status.
- Every threat has a severity and disposition; mitigations for T-Q01, T-Q02, T-Q03, and T-Q05 are implemented and exercised where applicable.
</success_criteria>

<output>
Create `.planning/quick/260809-4d3-implement-shell-files-for-the-same-funct/260809-4d3-SUMMARY.md` when done.
</output>
