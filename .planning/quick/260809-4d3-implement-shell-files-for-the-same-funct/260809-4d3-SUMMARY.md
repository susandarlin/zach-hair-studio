---
phase: 260809-4d3
plan: 01
subsystem: developer-infrastructure
tags: [bash, dev-stack, process-management, lifecycle-testing]
requires: []
provides:
  - Bash launch and shutdown commands for the complete local development stack
  - Detached process logging, readiness gating, PID tracking, and bounded cleanup
affects: [local-development, contributor-onboarding]
actuals:
  tokens: 3750
  tasks: 1
  commits: 2
tech-stack:
  added: []
  patterns:
    - Environment-overridable Bash lifecycle scripts with validated values
    - Isolated fake-service regression harness using temporary loopback ports
key-files:
  created:
    - start-dev.sh
    - stop-dev.sh
    - tests/dev-stack-scripts.test.sh
  modified: []
key-decisions:
  - "Runtime state is owner-only under temp/dev-stack by default, with a test-only state-directory override."
  - "Shutdown uses tracked PID trees first and exact-port listener discovery only as a fallback."
patterns-established:
  - "Shell launchers resolve repository paths from BASH_SOURCE so callers may run them from any directory."
  - "Lifecycle tests must use fake executables and dynamically allocated ports, never project services or default ports."
requirements-completed: [QUICK-260809-4d3]
coverage:
  - id: D1
    description: "Bash lifecycle controls start and stop the API, landing page, and dashboard safely."
    requirement: QUICK-260809-4d3
    verification:
      - kind: integration
        ref: "bash tests/dev-stack-scripts.test.sh"
        status: pass
    human_judgment: false
duration: 32min
completed: 2026-08-09
status: complete
---

# Quick Task 260809-4d3 Plan 01: Bash Dev-Stack Lifecycle Summary

**Portable Bash controls now launch the API, landing page, and dashboard with readiness-gated detached processes, per-service logs, PID tracking, and reliable idempotent shutdown.**

## Performance

- **Duration:** 32 min
- **Tasks:** 1
- **Files modified:** 3
- **Verification:** `bash -n start-dev.sh stop-dev.sh tests/dev-stack-scripts.test.sh && bash tests/dev-stack-scripts.test.sh`

## Accomplishments

- Added executable `start-dev.sh`, preserving the BAT launch commands and default ports while waiting for every listener before returning.
- Added executable `stop-dev.sh` with PID-tree termination, bounded TERM-to-KILL escalation, exact-port fallback discovery, and repeat-safe status reporting.
- Added an isolated fake-service lifecycle harness covering startup, duplicate prevention, rollback, tracked shutdown, stale PID fallback, logs, and idempotence.
- Preserved `start-dev.bat` and `stop-dev.bat` byte-for-byte; their pre-existing uncommitted edits remain user-owned.

## Task Commits

1. **Task 1: Ship and prove the complete Bash dev-stack lifecycle (RED)** — `6621f01` (`test`)
2. **Task 1: Ship and prove the complete Bash dev-stack lifecycle (GREEN)** — `2c5e7c6` (`feat`)

## Files Created

- `start-dev.sh` — validates safe configuration, launches the three authoritative services, records PID/log metadata, waits for readiness, and rolls back partial starts.
- `stop-dev.sh` — stops recorded process trees, falls back only to the three configured listener ports, and clears stale PID files.
- `tests/dev-stack-scripts.test.sh` — self-cleaning regression harness with fake `dotnet` and `npm` listeners on dynamically allocated ports.

## Decisions Made

- Runtime state defaults to the gitignored `temp/dev-stack` directory, created with owner-only permissions; test seams use explicit environment overrides.
- PID metadata is parsed only as positive decimal values and never executed; port fallback is restricted to the three configured service ports.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected strict-mode lifecycle failures found by the harness**
- **Found during:** Task 1
- **Issue:** Bash `local` declarations derived paths before dependent variables were initialized under `set -u`; shutdown could return failure after a successful termination; fresh service logs were empty.
- **Fix:** Split dependent local assignments, return success after a completed termination, and write a launch marker to each per-service log.
- **Files modified:** `start-dev.sh`, `stop-dev.sh`, `tests/dev-stack-scripts.test.sh`
- **Verification:** `bash tests/dev-stack-scripts.test.sh` passed in 1.8 seconds.
- **Committed in:** `2c5e7c6`

**Total deviations:** 1 auto-fixed (Rule 1)

## Issues Encountered

The sandbox required temporary elevated permission to bind the test harness's loopback ports. The harness uses only dynamically allocated localhost ports and its cleanup trap left no services running.

## User Setup Required

None.

## Next Phase Readiness

Developers on Bash-capable systems can use `./start-dev.sh` and `./stop-dev.sh` from any directory. Per-service diagnostics are retained in `temp/dev-stack`.

## Self-Check: PASSED

- Required files exist and are executable.
- TDD commits `6621f01` and `2c5e7c6` exist.
- The lifecycle suite passed, and the authoritative BAT hashes matched their pre-task values.
