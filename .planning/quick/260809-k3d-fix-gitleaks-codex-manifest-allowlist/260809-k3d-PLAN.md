---
phase: quick
plan: 260809-k3d
type: execute
wave: 1
depends_on: []
files_modified: [.gitleaks.toml]
autonomous: true
requirements: []

must_haves:
  truths:
    - "GSD SHA-256 checksums under both .claude and .codex tool directories are ignored only when they match generic-api-key and the existing 64-hex JSON-line pattern"
  artifacts:
    - ".gitleaks.toml"
  key_links:
    - "The existing rule-targeted manifest allowlist includes equivalent .claude and .codex paths while retaining condition = AND and regexTarget = line"
---

<objective>
Close PR #43's confirmed gitleaks false-positive gap. The completed binary scan reports three `generic-api-key` findings in `.codex/gsd-file-manifest.json`; each is a GSD-generated SHA-256 file-integrity checksum matching the exact pattern already allowlisted for `.claude/gsd-file-manifest.json`.

Purpose: Keep full-history secret scanning strict while allowing only known GSD manifest checksum lines in the parallel `.codex` tool directory.
Output: Two additional path regexes in the existing SHA-256 manifest allowlist in `.gitleaks.toml`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.gitleaks.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the GSD manifest checksum allowlist to .codex</name>
  <files>.gitleaks.toml</files>
  <action>
  In the second `[[allowlists]]` block, described as `SHA-256 file-integrity checksums in GSD manifest files`, add these two entries to `paths` after the equivalent `.claude` entries:

      '''\.codex/gsd-file-manifest\.json''',
      '''\.codex/gsd-local-patches/backup-meta\.json''',

  Preserve `condition = "AND"`, `targetRules = ["generic-api-key"]`, `regexTarget = "line"`, and the existing `'''"[a-f0-9]{64}"'''` regex. Do not alter the first test-fixture allowlist, `.github/workflows/security.yml`, or any other implementation file.
  </action>
  <verify>
    <automated>git diff -- .gitleaks.toml && git diff --numstat -- .gitleaks.toml</automated>
  </verify>
  <done>The implementation diff in `.gitleaks.toml` contains exactly two added `.codex` path entries in the second allowlist and no removals or unrelated changes. Run `gitleaks detect --source . --redact --verbose` if a local binary is available. The CI-flagged commit `dbc3f0d0d7ee47376bfe4525f981ec0ceaeacf7c` is absent from this clone, so a clean local scan cannot prove that exact historical finding; record that limitation rather than blocking. The path and line regexes are otherwise byte-equivalent to the established `.claude` allowlist.</done>
</task>

</tasks>

<verification>
Review the constrained TOML diff and, when available, run the same gitleaks command as CI. Confirm no broader global path exclusion was introduced: the allowlist remains rule-targeted and requires both the selected path and the 64-hex JSON-line pattern.
</verification>

<success_criteria>
The existing GSD checksum allowlist covers `.codex/gsd-file-manifest.json` and `.codex/gsd-local-patches/backup-meta.json` without weakening detection for other rules, values, or files.
</success_criteria>

<output>
Create `.planning/quick/260809-k3d-fix-gitleaks-codex-manifest-allowlist/260809-k3d-SUMMARY.md` when done. Note that this diagnosis became possible only after obtaining the actual CI step log; prior quick tasks 260809-hui and 260809-ipz lacked that evidence. Record the operational lesson to obtain exact CI error output before pursuing further environment hypotheses.
</output>
