---
phase: 260809-sf1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/lib/adminChat.ts
  - dashboard/lib/adminChat.test.mjs
  - dashboard/components/AdminChatWidget.tsx
autonomous: true
requirements: [QUICK-260809-sf1]

must_haves:
  truths:
    - "After the assistant asks 'Which service would you like to check?', a bare reply naming a real service (e.g. 'Scalp Treatment') is resolved as the answer to that question, not misclassified as an unrelated/help message."
    - "Bare date/time follow-ups ('Tomorrow', '2 PM') after a service is already known reuse that service instead of re-asking."
    - "An explicit topic switch mid slot-fill (e.g. 'who's booked today') is still honored, not swallowed as the awaited answer."
    - "Service names are only ever resolved against the live /api/Services catalog (matchService) — never invented."
    - "`npx tsc --noEmit -p .` (dashboard/) and `node lib/adminChat.test.mjs` both pass."
  artifacts:
    - "dashboard/lib/adminChat.ts exports ChatSession; sendChatMessage(text, session) returns { reply, session }"
    - "dashboard/components/AdminChatWidget.tsx threads a ChatSession ref across sendChatMessage calls"
  key_links:
    - "AdminChatWidget.tsx sessionRef <-> adminChat.ts sendChatMessage's session param/return — session must round-trip every turn or slot-filling silently resets."
    - "adminChat.test.mjs mirrors adminChat.ts's routing helpers per the file's own header comment."
---

<objective>
Fix the AdminChat widget's broken multi-turn flow: after the assistant asks "Which service would you like to check?", a bare answer like "Scalp Treatment" fell through to the generic help text because `sendChatMessage` was stateless — every message was reclassified from scratch with no memory of the question just asked. Add minimal slot-filling state (`ChatSession`) so short follow-ups resolve against the pending question instead of requiring the full original sentence repeated.
</objective>

<context>
@dashboard/lib/adminChat.ts
@dashboard/lib/adminChat.test.mjs
@dashboard/components/AdminChatWidget.tsx
@API/ZachHairStudio.Api/Mcp/ScheduleTools.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ChatSession slot-filling state to adminChat.ts</name>
  <files>dashboard/lib/adminChat.ts</files>
  <action>
Add `ChatSession = { awaiting?: "service", lastService?, lastDate? }`. Add `looksLikeTopicSwitch` (explicit bookings/services keyword regex, excluding date words) and `isAvailabilityFollowUp(text, session)` (true when awaiting a service answer, or a bare date/time word follows a known `lastService`). Change `answerAvailability` to accept `session`, resolve the service via `matchService` first against the awaited-answer text, falling back to `session.lastService`; resolve the date from the message or `session.lastDate`; return `{ reply, session }` on every branch, setting `awaiting: "service"` when still missing a service. Change `sendChatMessage` to accept an optional `session` param, route to availability handling when `isAvailabilityFollowUp` is true (bypassing `classifyIntent`), and return `{ reply, session }`.
  </action>
  <verify>
    <automated>cd dashboard && npx tsc --noEmit -p .</automated>
  </verify>
  <done>sendChatMessage/answerAvailability are session-aware; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Thread the session through AdminChatWidget and mirror the test file</name>
  <files>dashboard/components/AdminChatWidget.tsx, dashboard/lib/adminChat.test.mjs</files>
  <action>
In AdminChatWidget.tsx, add a `sessionRef` (useRef&lt;ChatSession&gt;({})), pass `sessionRef.current` into `sendChatMessage`, store the returned `session` back into the ref, and reset it to `{}` on error so a failed turn never leaves the assistant stuck mid slot-fill. In adminChat.test.mjs, mirror `looksLikeTopicSwitch`/`isAvailabilityFollowUp`/`DATE_WORD` and add assertions covering: bare service-name continuation, topic-switch override, bare date/time follow-up with a known service, and no-op when no prior service exists.
  </action>
  <verify>
    <automated>cd dashboard && npx tsc --noEmit -p . && node lib/adminChat.test.mjs</automated>
  </verify>
  <done>Widget round-trips session across turns; mirrored test file passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p .` (dashboard/) — no errors.
2. `node lib/adminChat.test.mjs` — all assertions pass, including new slot-filling cases.
3. Manual conversation trace (reasoned through, not run in a live LLM — this is keyword routing, not an LLM):
   "Open slots" -> awaiting=service -> "Scalp Treatment" -> resolves service via matchService, calls /api/Appointments/slots -> "Tomorrow" -> reuses lastService, re-resolves date -> new slots call.
</verification>

<success_criteria>
- "Scalp Treatment" alone, after "Which service?", is treated as the answer — no need to repeat "open slots for Scalp Treatment today".
- Bare "Tomorrow" / "2 PM" reuse the last known service.
- An explicit topic switch (bookings/services keywords) is never swallowed as the awaited service answer.
- No service name is ever fabricated — always resolved via matchService against the live catalog.
- Existing MCP tool (`get_appointment_slots`) and REST endpoints are untouched.
</success_criteria>

<output>
Create `.planning/quick/260809-sf1-adminchat-slot-filling/260809-sf1-SUMMARY.md` when done.
</output>
