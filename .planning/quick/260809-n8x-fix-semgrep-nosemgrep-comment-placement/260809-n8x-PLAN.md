---
phase: quick
plan: 260809-n8x
type: execute
wave: 1
depends_on: [260809-m2q]
files_modified: [landing-page/lib/chat.ts]
autonomous: true
requirements: []

must_haves:
  truths:
    - "The nosemgrep suppression in chat.ts actually attaches to the new RegExp(...) line semgrep flags — verified via a real local semgrep run, not just plausible comment placement"
  artifacts:
    - "landing-page/lib/chat.ts"
  key_links:
    - "semgrep's nosemgrep: <rule-id> comment must sit on the line immediately above the flagged expression; a comment two lines above (separated by an intervening line of code) does not suppress the finding"
---

<objective>
260809-m2q's `nosemgrep` suppression in `chat.ts` did not actually work — PR #43's live `sast (semgrep)` check run (workflow run 31308767427, against commit fe26590) still failed, and a local `semgrep scan --config=auto --error --skip-unknown-extensions .` reproduction confirmed why: the `nosemgrep` comment was placed 2 lines above the flagged `new RegExp(...)` call (separated by the `return aliases.some((alias) =>` line), so semgrep's line-adjacency matching didn't attach it. `chat.selfcheck.mjs`'s equivalent fix worked because that file's call is single-line, with the comment directly above it.

Fix: move the rationale + `nosemgrep` comment from directly above `return aliases.some(...)` to directly above `new RegExp(...)` inside the arrow function body, so it sits on the line immediately preceding the flagged expression.

Purpose: Get the `sast (semgrep)` check-run to a genuine, locally-reproduced 0-findings pass — not a re-guess.
Output: comment-only relocation in chat.ts; local semgrep re-run confirms 0 findings (exit 0).
</objective>

<context>
@.planning/STATE.md
@landing-page/lib/chat.ts
@.planning/quick/260809-m2q-fix-semgrep-redos-false-positive-alias-r/260809-m2q-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the nosemgrep comment to sit directly above the flagged new RegExp(...) line</name>
  <files>landing-page/lib/chat.ts</files>
  <action>
  In `findMatchingService`, move the rationale + `nosemgrep` comment pair from above `return aliases.some((alias) =>` to inside the arrow function, directly above `new RegExp(\`\\b${alias}\\b\`).test(normalizedInput)`. No logic change.
  </action>
  <verify>
    <automated>cd landing-page && node lib/chat.selfcheck.mjs; cd ..; python -m pip install --quiet semgrep 2>&1 | tail -1; semgrep scan --config=auto --error --skip-unknown-extensions . 2>&1 | tail -20</automated>
  </verify>
  <done>Local semgrep run reports 0 findings (exit 0); node lib/chat.selfcheck.mjs still passes; git diff confined to chat.ts, comment-only relocation.</done>
</task>

</tasks>

<verification>
Local semgrep reproduction (same `--config=auto --error --skip-unknown-extensions .` command CI runs) shows `Findings: 0 (0 blocking)`, exit 0 — the same command that reproduced the live CI failure before the fix.
</verification>

<success_criteria>
The `sast (semgrep)` check on PR #43 passes against the next pushed commit, verified locally first via a real semgrep run rather than inferred from comment placement alone.
</success_criteria>

<output>
Create `.planning/quick/260809-n8x-fix-semgrep-nosemgrep-comment-placement/260809-n8x-SUMMARY.md` when done. Note this corrects 260809-m2q, whose suppression comment was misplaced relative to the flagged line and did not actually take effect on the live CI run.
</output>
